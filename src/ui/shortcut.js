import { g } from '../runtime/scope.ts';

const code = "// ── 홈화면/즐겨찾기 제안 ───────────────────────────────────\n// 노출: 설문 팝업이 없을 때 · gameover/victory 또는 prep(round≥2)\n// 재노출: 「나중에」→ 7일 스누즈, 최대 3회 · 「이미 추가/확인」→ 영구 숨김\n// 자동 설치: beforeinstallprompt 있을 때만 · 그 외는 OS별 안내\ng.SHORTCUT_DONE_KEY = 'hwatro_shortcut_done';\ng.SHORTCUT_LATER_KEY = 'hwatro_shortcut_later';\ng.SHORTCUT_SHOWS_KEY = 'hwatro_shortcut_shows';\ng.SHORTCUT_SNOOZE_MS = 7 * 86400000;\ng.SHORTCUT_MAX_SHOWS = 3;\n\ng._deferredInstallPrompt = null;\ng._shortcutShowLogged = false;\ng._shortcutAutoInstallTried = false;\n\ng.registerShortcutServiceWorker = function registerShortcutServiceWorker() {\n  if (!('serviceWorker' in navigator)) return;\n  try {\n    const swUrl = new URL('sw.js', location.href).href;\n    navigator.serviceWorker.register(swUrl).catch(() => {});\n  } catch (_) {}\n}\n\ng.hookShortcutInstallPrompt = function hookShortcutInstallPrompt() {\n  registerShortcutServiceWorker();\n  window.addEventListener('beforeinstallprompt', (e) => {\n    e.preventDefault();\n    _deferredInstallPrompt = e;\n    if (shouldShowShortcutPrompt() && !_shortcutAutoInstallTried) {\n      tryAutoInstallShortcut();\n    }\n  });\n  window.addEventListener('appinstalled', () => {\n    _deferredInstallPrompt = null;\n    markShortcutDone();\n    state.shortcutSessionDismissed = true;\n    render();\n  });\n}\n\ng.isStandaloneDisplay = function isStandaloneDisplay() {\n  try {\n    if (window.matchMedia && window.matchMedia('(display-mode: standalone)').matches) return true;\n    if (window.navigator.standalone === true) return true;\n  } catch (_) {}\n  return false;\n}\n\ng.shortcutPlatform = function shortcutPlatform() {\n  const ua = navigator.userAgent || '';\n  const isIOS = /iPad|iPhone|iPod/.test(ua)\n    || (navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1);\n  if (isIOS) return 'ios';\n  if (/Android/i.test(ua)) return 'android';\n  return 'desktop';\n}\n\ng.shortcutMode = function shortcutMode() {\n  if (_deferredInstallPrompt) return 'install';\n  const p = shortcutPlatform();\n  if (p === 'ios') return 'guide_ios';\n  if (p === 'android') return 'guide_android';\n  return 'guide_desktop';\n}\n\ng.hasShortcutDone = function hasShortcutDone() {\n  try { return localStorage.getItem(SHORTCUT_DONE_KEY) === '1'; }\n  catch (_) { return false; }\n}\n\ng.markShortcutDone = function markShortcutDone() {\n  try { localStorage.setItem(SHORTCUT_DONE_KEY, '1'); }\n  catch (_) {}\n}\n\ng.shortcutShowCount = function shortcutShowCount() {\n  try { return Math.max(0, parseInt(localStorage.getItem(SHORTCUT_SHOWS_KEY) || '0', 10) || 0); }\n  catch (_) { return 0; }\n}\n\ng.bumpShortcutShowCount = function bumpShortcutShowCount() {\n  try { localStorage.setItem(SHORTCUT_SHOWS_KEY, String(shortcutShowCount() + 1)); }\n  catch (_) {}\n}\n\ng.isShortcutSnoozed = function isShortcutSnoozed() {\n  try {\n    const raw = localStorage.getItem(SHORTCUT_LATER_KEY);\n    if (!raw) return false;\n    const t = Date.parse(raw);\n    if (!Number.isFinite(t)) return false;\n    return Date.now() - t < SHORTCUT_SNOOZE_MS;\n  } catch (_) { return false; }\n}\n\ng.snoozeShortcutPrompt = function snoozeShortcutPrompt() {\n  try { localStorage.setItem(SHORTCUT_LATER_KEY, new Date().toISOString()); }\n  catch (_) {}\n}\n\ng.isShortcutPreview = function isShortcutPreview() {\n  try { return new URLSearchParams(location.search).get('shortcutPreview') === '1'; }\n  catch (_) { return false; }\n}\n\ng.shouldShowShortcutPrompt = function shouldShowShortcutPrompt() {\n  if (isShortcutPreview()) {\n    return !state.shortcutSessionDismissed && !isStandaloneDisplay();\n  }\n  if (isStandaloneDisplay() || hasShortcutDone()) return false;\n  if (state.shortcutSessionDismissed) return false;\n  if (shouldShowSurveyPrompt()) return false;\n  if (isShortcutSnoozed()) return false;\n  if (shortcutShowCount() >= SHORTCUT_MAX_SHOWS && !state.shortcutGuideOpen) return false;\n  const end = state.screen === 'gameover' || state.screen === 'victory';\n  const prepGate = state.screen === 'prep' && (state.round || 1) >= 2;\n  return end || prepGate;\n}\n\ng.shortcutTrackParams = function shortcutTrackParams(extra) {\n  const params = {\n    platform: shortcutPlatform(),\n    mode: shortcutMode(),\n    locale: getLocale(),\n    screen: state.screen || '',\n  };\n  if (extra) Object.assign(params, extra);\n  return params;\n}\n\ng.tryAutoInstallShortcut = async function tryAutoInstallShortcut() {\n  if (!_deferredInstallPrompt || _shortcutAutoInstallTried) return false;\n  _shortcutAutoInstallTried = true;\n  const ev = _deferredInstallPrompt;\n  try {\n    if (!isShortcutPreview()) {\n      trackEvent('shortcut_prompt_click', shortcutTrackParams({ action: 'install_auto' }));\n    }\n    await ev.prompt();\n    const choice = await ev.userChoice;\n    _deferredInstallPrompt = null;\n    if (choice && choice.outcome === 'accepted') {\n      if (!isShortcutPreview()) {\n        trackEvent('shortcut_prompt_close', shortcutTrackParams({ reason: 'installed' }));\n      }\n      markShortcutDone();\n      state.shortcutSessionDismissed = true;\n      render();\n      return true;\n    }\n  } catch (_) {\n    // 제스처 없이 prompt 거부되는 브라우저 → 설치 버튼으로 폴백\n  }\n  render();\n  return false;\n}\n\ng.shortcutPrimaryAction = async function shortcutPrimaryAction() {\n  if (!_deferredInstallPrompt) return;\n  if (!isShortcutPreview()) {\n    trackEvent('shortcut_prompt_click', shortcutTrackParams({ action: 'install' }));\n  }\n  const ev = _deferredInstallPrompt;\n  _deferredInstallPrompt = null;\n  _shortcutAutoInstallTried = true;\n  try {\n    await ev.prompt();\n    const choice = await ev.userChoice;\n    if (choice && choice.outcome === 'accepted') {\n      if (!isShortcutPreview()) {\n        trackEvent('shortcut_prompt_close', shortcutTrackParams({ reason: 'installed' }));\n      }\n      markShortcutDone();\n      state.shortcutSessionDismissed = true;\n    }\n  } catch (_) {\n    _deferredInstallPrompt = ev;\n  }\n  render();\n}\n\ng.dismissShortcutPrompt = function dismissShortcutPrompt(reason) {\n  const why = reason || 'dismiss';\n  if (!isShortcutPreview()) {\n    trackEvent('shortcut_prompt_close', shortcutTrackParams({ reason: why }));\n    snoozeShortcutPrompt();\n  }\n  state.shortcutSessionDismissed = true;\n  state.shortcutGuideOpen = false;\n  render();\n}\n\ng.markShortcutAlreadyDone = function markShortcutAlreadyDone() {\n  if (!isShortcutPreview()) {\n    trackEvent('shortcut_prompt_close', shortcutTrackParams({ reason: 'already' }));\n    markShortcutDone();\n  }\n  state.shortcutSessionDismissed = true;\n  state.shortcutGuideOpen = false;\n  render();\n}\n\ng.shortcutStepsHtml = function shortcutStepsHtml() {\n  const p = shortcutPlatform();\n  if (p === 'ios') return tu('shortcutStepsIos');\n  if (p === 'android') return tu('shortcutStepsAndroid');\n  return tu('shortcutStepsDesktop');\n}\n\ng.shortcutPromptHTML = function shortcutPromptHTML() {\n  const canInstall = shortcutMode() === 'install';\n  const mobile = shortcutPlatform() !== 'desktop';\n  const body = mobile ? tu('shortcutBodyMobile') : tu('shortcutBodyDesktop');\n  const actions = canInstall\n    ? `<div class=\"survey-actions\">\n        <button type=\"button\" class=\"survey-yes\" onclick=\"shortcutPrimaryAction()\">${tu('shortcutCtaInstall')}</button>\n      </div>`\n    : '';\n  return `\n    <div class=\"survey-prompt\" role=\"dialog\" aria-labelledby=\"shortcut-title\">\n      <button type=\"button\" class=\"shortcut-close\" onclick=\"dismissShortcutPrompt('dismiss')\" aria-label=\"${tu('close')}\">×</button>\n      <div class=\"survey-corners\" aria-hidden=\"true\"></div>\n      <div class=\"survey-badge\" aria-hidden=\"true\"><img src=\"Assets/Icons/icon-192.png\" alt=\"\"></div>\n      <div class=\"survey-title\" id=\"shortcut-title\">${tu('shortcutTitle')}</div>\n      <div class=\"survey-rule\" aria-hidden=\"true\"></div>\n      <div class=\"survey-body\">${body}</div>\n      <div class=\"shortcut-steps\">${shortcutStepsHtml()}</div>\n      ${actions}\n      <button type=\"button\" class=\"survey-done-link\" onclick=\"markShortcutAlreadyDone()\">${tu('shortcutDone')}</button>\n    </div>`;\n}\n\ng.renderShortcutPrompt = function renderShortcutPrompt() {\n  const ov = $('shortcut-overlay');\n  if (!ov) return;\n  if (!shouldShowShortcutPrompt()) {\n    ov.classList.remove('show');\n    ov.setAttribute('aria-hidden', 'true');\n    ov.innerHTML = '';\n    _shortcutShowLogged = false;\n    _shortcutAutoInstallTried = false;\n    return;\n  }\n  if (!_shortcutShowLogged) {\n    _shortcutShowLogged = true;\n    if (!isShortcutPreview()) {\n      bumpShortcutShowCount();\n      trackEvent('shortcut_prompt_show', shortcutTrackParams({\n        show_n: shortcutShowCount(),\n      }));\n    }\n    if (_deferredInstallPrompt) {\n      queueMicrotask(() => { tryAutoInstallShortcut(); });\n    }\n  }\n  ov.innerHTML = shortcutPromptHTML();\n  ov.classList.add('show');\n  ov.setAttribute('aria-hidden', 'false');\n}\n\ng.hasSeenHowto = function hasSeenHowto() {\n  try { return localStorage.getItem('hwatro_howto_seen') === '1'; }\n  catch (_) { return false; }\n}\n\ng.markHowtoSeen = function markHowtoSeen() {\n  try { localStorage.setItem('hwatro_howto_seen', '1'); }\n  catch (_) {}\n}\n\ng.hasSeenJanCoach = function hasSeenJanCoach() {\n  try { return localStorage.getItem('hwatro_jan_coach_v1') === '1'; }\n  catch (_) { return false; }\n}\ng.markJanCoachSeen = function markJanCoachSeen() {\n  try { localStorage.setItem('hwatro_jan_coach_v1', '1'); }\n  catch (_) {}\n}\ng.clearJanCoachSeen = function clearJanCoachSeen() {\n  try { localStorage.removeItem('hwatro_jan_coach_v1'); }\n  catch (_) {}\n}\ng.hasSeenShopCoach = function hasSeenShopCoach() {\n  try { return localStorage.getItem('hwatro_shop_coach_v1') === '1'; }\n  catch (_) { return false; }\n}\ng.markShopCoachSeen = function markShopCoachSeen() {\n  try { localStorage.setItem('hwatro_shop_coach_v1', '1'); }\n  catch (_) {}\n}\ng.clearShopCoachSeen = function clearShopCoachSeen() {\n  try { localStorage.removeItem('hwatro_shop_coach_v1'); }\n  catch (_) {}\n}\ng.hasSeenGoStopCoach = function hasSeenGoStopCoach() {\n  try { return localStorage.getItem('hwatro_gostop_coach_v1') === '1'; }\n  catch (_) { return false; }\n}\ng.markGoStopCoachSeen = function markGoStopCoachSeen() {\n  try { localStorage.setItem('hwatro_gostop_coach_v1', '1'); }\n  catch (_) {}\n}\ng.clearGoStopCoachSeen = function clearGoStopCoachSeen() {\n  try { localStorage.removeItem('hwatro_gostop_coach_v1'); }\n  catch (_) {}\n}\ng.hasSeenDeckCoach = function hasSeenDeckCoach() {\n  try { return localStorage.getItem('hwatro_deck_coach_v1') === '1'; }\n  catch (_) { return false; }\n}\ng.markDeckCoachSeen = function markDeckCoachSeen() {\n  try { localStorage.setItem('hwatro_deck_coach_v1', '1'); }\n  catch (_) {}\n}\ng.clearDeckCoachSeen = function clearDeckCoachSeen() {\n  try { localStorage.removeItem('hwatro_deck_coach_v1'); }\n  catch (_) {}\n}\n\n/** 사이드/탑바 — 튜토리얼 다시 보기 (런 처음부터) */\ng.abortTransientUI = function abortTransientUI() {\n  // 진행 중 내기/드로우/점수게이트 취소\n  state.playFadeGen = (state.playFadeGen || 0) + 1;\n  state.dealGen = (state.dealGen || 0) + 1;\n  releaseCoachGate();\n  try {\n    JUICE.skip = true;\n    if (JUICE.onSkip) JUICE.onSkip();\n    JUICE.running = false;\n    JUICE.skip = false;\n    JUICE.onSkip = null;\n  } catch (_) {}\n\n  document.body.classList.remove('juicing', 'screen-shake', 'punch');\n  const banner = $('hand-banner');\n  if (banner) {\n    banner.className = '';\n    banner.textContent = '';\n    banner.style.left = '';\n    banner.style.top = '';\n  }\n  const go = $('go-cascade');\n  if (go) {\n    go.classList.remove('on');\n    go.setAttribute('aria-hidden', 'true');\n    const stage = $('go-cascade-stage');\n    if (stage) stage.innerHTML = '';\n  }\n  settleJuiceSkip = true;\n  settleJuiceBusy = false;\n  const jumak = $('jumak-cascade');\n  if (jumak) {\n    jumak.classList.remove('on');\n    jumak.setAttribute('aria-hidden', 'true');\n    const stage = $('jumak-cascade-stage');\n    if (stage) stage.classList.remove('slam');\n  }\n  const coach = $('coach-root');\n  if (coach) {\n    coach.classList.remove('on');\n    coach.setAttribute('aria-hidden', 'true');\n    coach.innerHTML = '';\n  }\n}\n\ng.replayTutorials = function replayTutorials() {\n  clearJanCoachSeen();\n  clearShopCoachSeen();\n  clearGoStopCoachSeen();\n  clearDeckCoachSeen();\n  try { localStorage.removeItem('hwatro_howto_seen'); } catch (_) {}\n  abortTransientUI();\n  resetRun();\n  state.helpReturn = null;\n  state.deckPeekReturn = null;\n  state.coachStep = null;\n  state.coachPendingScored = false;\n  state.coachAfterDeal = null;\n  state.janTutorJunkPending = false;\n  state.juicing = false;\n  state.dealing = false;\n  state.screen = 'welcome';\n  render();\n}\n\n/** 1월 추천 활성? — 무료 3회는 강제 ON, 이후는 토글 */\ng.hintsActive = function hintsActive() {\n  if (state.round !== 1) return false;\n  if (state.screen !== 'play' || state.juicing || state.dealing) return false;\n  if (state.coachStep === 'junk') return true;\n  if (state.janAuraLeft > 0) return true;\n  return !!state.hintEnabled;\n}\n\n/** 내기 추천 — 실제 최적 점수(`evaluateHand`) 기준, 하이라이트는 코어만 */\ng.findBestPlayHint = function findBestPlayHint() {\n  if (state.hand.length < 1) return null;\n  const env = scoreEnv();\n  const best = evaluateHand(state.hand, env);\n  if (!best || !best.cards.length) return null;\n\n  if (best.handId && best.handId !== 'none') {\n    const core = best.core;\n    const picks = (core && core.length) ? core : best.cards;\n    if (!picks.length) return null;\n    return {\n      kind: 'play',\n      uids: picks.map((c) => c.uid),\n      handId: best.handId,\n      name: uiHandName(best.handId, core),\n      score: best.score,\n    };\n  }\n\n  let top = null;\n  for (const c of state.hand) {\n    if (c.faceDown) continue;\n    if (!top || cardChip(c, env) > cardChip(top, env)) top = c;\n  }\n  if (!top) return null;\n  return {\n    kind: 'play',\n    uids: [top.uid],\n    handId: 'none',\n    name: t('bestChipHand'),\n    score: computeScore([top], env).score,\n  };\n}\n\n/** 버리기 후보인지 — 광·고도리 소재는 절대 추천 버림 금지 */\ng.isJunkDiscardCandidate = function isJunkDiscardCandidate(c, env) {\n  if (c.faceDown) return false;\n  if (effType(c) === 'kwang') return false;\n  if ((c.tags || []).includes('godori')) return false;\n  const t = effType(c);\n  if (t === 'pi' || t === 'ssangpi') return true;\n  // 열끗·띠는 칩이 정말 낮을 때만 (밤/박 디버프 등)\n  return cardChip(c, env) <= 3;\n}\n\n/**\n * 버리기 추천 uid\n * - 튜토: 피·쌍피\n * - 일반: 내기 코어·광·고도리 밖 약패만 (없으면 빈 배열 — 억지 추천 금지)\n */\ng.getJanJunkSuggest = function getJanJunkSuggest() {\n  const env = scoreEnv();\n  const face = state.hand.filter((c) => !c.faceDown);\n  if (!face.length) return [];\n  if (state.coachStep === 'junk') {\n    const pis = face\n      .filter((c) => {\n        const t = effType(c);\n        return t === 'pi' || t === 'ssangpi';\n      })\n      .sort((a, b) => cardChip(a, env) - cardChip(b, env));\n    if (pis.length) return pis.slice(0, 5).map((c) => c.uid);\n  }\n  const play = findBestPlayHint();\n  const keep = new Set(play ? play.uids : []);\n  const junk = face\n    .filter((c) => !keep.has(c.uid) && isJunkDiscardCandidate(c, env))\n    .sort((a, b) => cardChip(a, env) - cardChip(b, env));\n  if (!junk.length) return [];\n  return junk.slice(0, Math.min(3, junk.length)).map((c) => c.uid);\n}\n\n/**\n * 내기 vs 버리기 추천\n * - 덱 피크 금지 (다음 장 엿보면 추천이 들쭉날쭉해 보임)\n * - 페이스·족보 강하면 내기, 약손+쓰레기 패 있을 때만 버리기\n */\ng.getActiveCoachHint = function getActiveCoachHint() {\n  if (state.coachStep === 'junk') {\n    const uids = getJanJunkSuggest();\n    return uids.length ? { kind: 'junk', uids, name: t('discardWord') } : null;\n  }\n  if (!hintsActive()) return null;\n  const play = findBestPlayHint();\n  if (!play) return null;\n\n  // 튜토 조합·내기 단계는 내기 추천만\n  if (state.coachStep === 'hand' || state.coachStep === 'play' || state.coachStep === 'scored') {\n    return play;\n  }\n\n  const canDiscard = state.discardsLeft > 0 && state.boss !== 'bibaram' && state.deck.length > 0;\n  if (!canDiscard) return play;\n\n  const junkUids = getJanJunkSuggest();\n  if (!junkUids.length) return play;\n\n  const pace = Math.max(1, state.target - state.roundScore) / Math.max(1, state.playsLeft);\n  const playHasHand = !!(play.handId && play.handId !== 'none');\n  const tier = playHasHand ? HANDS.findIndex((h) => h.id === play.handId) : -1;\n  // 상위 족보(오광~띠셋)는 점수 부족해도 내기 유도\n  if (playHasHand && tier >= 0 && tier <= 11) return play;\n  // 페이스에 근접한 손은 내기\n  if (play.score >= pace * 0.9) return play;\n  // 약손 + 버릴 약패 있음 → 버리기\n  if (play.score < pace * 0.85) {\n    return { kind: 'junk', uids: junkUids, name: t('discardWord') };\n  }\n  return play;\n}\n\n/** 코치 텍스트용 — 내기 추천만 */\ng.getJanAuraBest = function getJanAuraBest() {\n  const h = getActiveCoachHint();\n  return h && h.kind === 'play' ? h : null;\n}\n\ng.maybeStartJanCoach = function maybeStartJanCoach(isRoundStart) {\n  if (!isRoundStart || state.round !== 1 || state.screen !== 'play') return;\n  if (hasSeenJanCoach()) return;\n  // 1단계: 개국 조합 보장 후 조합→내기 튜토 (버리기는 다음 보충)\n  state.coachStep = 'hand';\n  render();\n}\n\ng.maybeStartShopCoach = function maybeStartShopCoach() {\n  if (state.screen !== 'shop' || state.round !== 1) return;\n  if (hasSeenShopCoach()) return;\n  state.coachStep = 'shop';\n}\n\ng.maybeStartGoStopCoach = function maybeStartGoStopCoach() {\n  if (state.screen !== 'gostop') return;\n  if (hasSeenGoStopCoach()) return;\n  state.coachStep = 'gostop';\n}\n\n/** 1월 · 조합/버리기 튜토 끝난 뒤 — 덱 클릭으로 패 보기 */\ng.maybeStartDeckCoach = function maybeStartDeckCoach() {\n  if (state.screen !== 'play' || state.round !== 1) return;\n  if (state.juicing || state.dealing) return;\n  if (state.coachStep) return;\n  if (hasSeenDeckCoach()) return;\n  if (!hasSeenJanCoach()) return;\n  state.coachStep = 'deck';\n  render();\n}\n\ng.coachGateResolve = null;\ng.releaseCoachGate = function releaseCoachGate() {\n  if (!coachGateResolve) return;\n  const r = coachGateResolve;\n  coachGateResolve = null;\n  r();\n}\ng.waitCoachGate = function waitCoachGate() {\n  return new Promise((r) => { coachGateResolve = r; });\n}\n\n/** 내기 연출 직후 — 점수 설명. 알겠어요/건너뛰기까지 멈춤. true면 이후 중단 */\ng.pauseForCoachScored = async function pauseForCoachScored(playGen) {\n  if (!state.coachPendingScored) return false;\n  state.coachPendingScored = false;\n  state.dealing = true;\n  state.coachStep = 'scored';\n  state.coachAfterDeal = null;\n  render();\n  await waitCoachGate();\n  if (playGen != null && playGen !== state.playFadeGen) {\n    state.dealing = false;\n    state.coachStep = null;\n    return true;\n  }\n  return false;\n}\n\ng.coachNext = function coachNext() {\n  // 다음이 필요한 스텝만 (점수 확인·상점 요약·덱 안내)\n  if (state.coachStep === 'scored') {\n    state.coachStep = null;\n    // 버리기 단계가 남았으면 아직 완료 처리하지 않음\n    if (!state.janTutorJunkPending) markJanCoachSeen();\n    render();\n    releaseCoachGate();\n    maybeStartDeckCoach();\n    return;\n  }\n  if (state.coachStep === 'shop') {\n    state.coachStep = null;\n    markShopCoachSeen();\n    render();\n    return;\n  }\n  if (state.coachStep === 'deck') {\n    state.coachStep = null;\n    markDeckCoachSeen();\n    render();\n    return;\n  }\n  coachSkip();\n}\ng.coachSkip = function coachSkip() {\n  const step = state.coachStep;\n  state.coachStep = null;\n  state.coachPendingScored = false;\n  state.coachAfterDeal = null;\n  if (step === 'shop' || (step && String(step).startsWith('shop'))) markShopCoachSeen();\n  else if (step === 'gostop') markGoStopCoachSeen();\n  else if (step === 'deck') markDeckCoachSeen();\n  else if (step === 'junk') {\n    state.janTutorJunkPending = false;\n    markJanCoachSeen();\n  } else if (step === 'hand' || step === 'play' || step === 'scored') {\n    // 조합·점수 코치만 건너뛰고 버리기 튜토는 첫 보충에서 이어감\n    if (!state.janTutorJunkPending) markJanCoachSeen();\n  } else {\n    state.janTutorJunkPending = false;\n    markJanCoachSeen();\n  }\n  render();\n  releaseCoachGate();\n  if (step === 'junk' || step === 'scored') maybeStartDeckCoach();\n}\n\ng.dismissWelcome = function dismissWelcome() {\n  if (state.screen !== 'welcome') return;\n  markHowtoSeen();\n  state.screen = 'prep';\n  render();\n}\n\ng.restart = function restart() {\n  resetRun();\n  state.screen = 'prep';\n  render();\n}\n\n/** 원본 경로 → blob URL (프리로드된 이미지는 네트워크 재요청 없음) */\ng.IMG_URL_CACHE = Object.create(null);\ng.assetSrc = function assetSrc(url) {\n  return (url && IMG_URL_CACHE[url]) || url;\n}\n\ng.setBootLoadProgress = function setBootLoadProgress(done, total) {\n  const pct = total <= 0 ? 100 : Math.min(100, Math.round((done / total) * 100));\n  const fill = $('boot-load-fill');\n  const pctEl = $('boot-load-pct');\n  const msg = $('boot-load-msg');\n  if (fill) fill.style.width = pct + '%';\n  if (pctEl) pctEl.textContent = pct + '%';\n  if (msg) {\n    msg.textContent = pct < 100\n      ? (pct < 40 ? t('bootShuffle') : pct < 80 ? t('bootUnfurl') : t('bootAlmost'))\n      : t('bootOpen');\n  }\n}\n\ng.mapPool = async function mapPool(items, limit, worker) {\n  let i = 0;\n  const n = Math.min(limit, Math.max(1, items.length));\n  await Promise.all(Array.from({ length: n }, async () => {\n    while (i < items.length) {\n      const idx = i++;\n      await worker(items[idx], idx);\n    }\n  }));\n}\n\n/**\n * 카드/오버레이만 게이트. fetch→blob→decode까지 끝난 뒤 blob URL 캐시.\n * SFX는 화면 전환 후 백그라운드 (소리 때문에 로딩이 끝난 척 하는 일 방지).\n */\ng.preloadCardImages = async function preloadCardImages(onProgress) {\n  const arts = [...new Set(buildDeck().map((c) => c.art).filter(Boolean))];\n  const overlays = [...new Set(Object.values(CARD_OVERLAY))];\n  const urls = ['Assets/Background.webp', 'Assets/jumak.png'].concat(arts, overlays);\n  const total = urls.length || 1;\n  let done = 0;\n  onProgress(0, total);\n\n  await mapPool(urls, 6, async (url) => {\n    try {\n      // file:// 은 쿼리 불가. http(s)는 ?v= 유지해 immutable 에셋 캐시 무효화.\n      const bare = String(url).split('?')[0];\n      const fetchUrl = location.protocol === 'file:' ? bare : url;\n      const res = await fetch(fetchUrl);\n      if (!res.ok) throw new Error(String(res.status));\n      const blob = await res.blob();\n      const obj = URL.createObjectURL(blob);\n      const img = new Image();\n      img.src = obj;\n      if (img.decode) await img.decode();\n      else await new Promise((r) => { img.onload = img.onerror = r; });\n      IMG_URL_CACHE[url] = obj;\n      IMG_URL_CACHE[bare] = obj;\n    } catch (_) {\n      // 캐시 실패 시 원본 URL로 폴백 (DOM이 다시 받음)\n    } finally {\n      done++;\n      onProgress(done, total);\n    }\n  });\n}\n\ng.warmSfxInBackground = function warmSfxInBackground() {\n  try {\n    Object.values(SFX).forEach((url) => {\n      const a = new Audio();\n      a.preload = 'auto';\n      a.src = url;\n      try { a.load(); } catch (_) {}\n    });\n    ensureBgm();\n    try { bgmAudio.load(); } catch (_) {}\n  } catch (_) {}\n}\n\ng.hideBootLoad = function hideBootLoad() {\n  const el = $('boot-load');\n  if (!el) return;\n  el.classList.add('done');\n  el.setAttribute('aria-busy', 'false');\n  document.body.classList.add('booted');\n  setTimeout(() => { if (el.parentNode) el.remove(); }, 500);\n}\n\ng.preloadOverlayFonts = async function preloadOverlayFonts() {\n  try {\n    if (!document.fonts || !document.fonts.load) return;\n    await Promise.all([\n      document.fonts.load('16px BaigeTianxing', '십이화 병풍 고도리'),\n      document.fonts.load('16px SSRock', '십이화 병풍 123'),\n    ]);\n  } catch (_) {}\n}\n\ng.CARD_TUNE_DEFAULTS = { bright: 0.90, sat: 0.96, sepia: 0.20 };\n\ng.cardTuneValues = function cardTuneValues() {\n  const bright = +($('ct-bright')?.value ?? CARD_TUNE_DEFAULTS.bright);\n  const sat = +($('ct-sat')?.value ?? CARD_TUNE_DEFAULTS.sat);\n  const sepia = +($('ct-sepia')?.value ?? CARD_TUNE_DEFAULTS.sepia);\n  return { bright, sat, sepia };\n}\n\ng.cardTuneCss = function cardTuneCss({ bright, sat, sepia }) {\n  return `brightness(${bright.toFixed(2)}) saturate(${sat.toFixed(2)}) sepia(${sepia.toFixed(2)})`;\n}\n\ng.applyCardTune = function applyCardTune() {\n  const v = cardTuneValues();\n  const root = document.documentElement;\n  root.style.setProperty('--card-bright', v.bright.toFixed(2));\n  root.style.setProperty('--card-sat', v.sat.toFixed(2));\n  root.style.setProperty('--card-sepia', v.sepia.toFixed(2));\n  const b = $('ct-bright-val');\n  const s = $('ct-sat-val');\n  const e = $('ct-sepia-val');\n  const css = $('ct-css');\n  if (b) b.textContent = v.bright.toFixed(2);\n  if (s) s.textContent = v.sat.toFixed(2);\n  if (e) e.textContent = v.sepia.toFixed(2);\n  if (css) css.textContent = `filter: ${cardTuneCss(v)};`;\n  try {\n    localStorage.setItem('hwatro_card_tune', JSON.stringify(v));\n  } catch (_) {}\n}\n\ng.resetCardTune = function resetCardTune() {\n  const d = CARD_TUNE_DEFAULTS;\n  if ($('ct-bright')) $('ct-bright').value = d.bright;\n  if ($('ct-sat')) $('ct-sat').value = d.sat;\n  if ($('ct-sepia')) $('ct-sepia').value = d.sepia;\n  applyCardTune();\n}\n\ng.copyCardTune = async function copyCardTune() {\n  const text = cardTuneCss(cardTuneValues());\n  try {\n    await navigator.clipboard.writeText(text);\n    const btn = document.querySelector('#card-tune .ct-actions .primary');\n    if (btn) {\n      const prev = btn.textContent;\n      btn.textContent = '복사됨!';\n      setTimeout(() => { btn.textContent = prev; }, 1200);\n    }\n  } catch (_) {\n    prompt('이 값을 복사하세요', text);\n  }\n}\n\ng.initCardTune = function initCardTune() {\n  const panel = $('card-tune');\n  if (!panel) return;\n  const enabled = new URLSearchParams(location.search).get('cardTune') === '1';\n  if (!enabled) return;\n  try {\n    const saved = JSON.parse(localStorage.getItem('hwatro_card_tune') || 'null');\n    if (saved && typeof saved === 'object') {\n      if ($('ct-bright') && saved.bright != null) $('ct-bright').value = saved.bright;\n      if ($('ct-sat') && saved.sat != null) $('ct-sat').value = saved.sat;\n      if ($('ct-sepia') && saved.sepia != null) $('ct-sepia').value = saved.sepia;\n    }\n  } catch (_) {}\n  ['ct-bright', 'ct-sat', 'ct-sepia'].forEach((id) => {\n    const el = $(id);\n    if (el) el.addEventListener('input', applyCardTune);\n  });\n  applyCardTune();\n  panel.classList.add('on');\n  panel.setAttribute('aria-hidden', 'false');\n}\n\ng.boot = async function boot() {\n  markInviteFromUrl();\n  applyLocaleChrome();\n  hookRunVisibility();\n  hookModalKeys();\n  initSessionStats();\n  initFirebaseTelemetry();\n  hookShortcutInstallPrompt();\n  setCardLabelsHidden(cardLabelsHidden());\n  resetRun();\n  initCardTune();\n  const previewQ = new URLSearchParams(location.search);\n  if (previewQ.get('goPreview') === '1') {\n    hideBootLoad();\n    openGoAnimPreview();\n    return;\n  }\n  if (previewQ.get('juicePreview') === '1') {\n    hideBootLoad();\n    openJuiceScorePreview();\n    return;\n  }\n  if (previewQ.get('shortcutPreview') === '1') {\n    try {\n      localStorage.removeItem(SHORTCUT_DONE_KEY);\n      localStorage.removeItem(SHORTCUT_LATER_KEY);\n      localStorage.removeItem(SHORTCUT_SHOWS_KEY);\n    } catch (_) {}\n    state.shortcutSessionDismissed = false;\n    state.shortcutGuideOpen = false;\n  }\n  const t0 = performance.now();\n  try {\n    await Promise.all([\n      preloadOverlayFonts(),\n      preloadCardImages(setBootLoadProgress),\n    ]);\n  } catch (_) {\n    setBootLoadProgress(1, 1);\n  }\n  // 캐시 히트 시 깜빡임 방지 — 최소 ~0.55초\n  const wait = Math.max(0, 550 - (performance.now() - t0));\n  if (wait) await new Promise((r) => setTimeout(r, wait));\n  hideBootLoad();\n  warmSfxInBackground();\n  logSessionStartTelemetry();\n  state.screen = needsLocalePick()\n    ? 'langpick'\n    : (hasSeenHowto() ? 'prep' : 'welcome');\n  render();\n}\n\n";
new Function('g', 'with (g) {\n' + code + '\n}')(g);
