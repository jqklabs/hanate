import { g } from '../runtime/scope.ts';

const code = "// \u2500\u2500 \ud310 \uc9c4\ud589 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ng.startRound = function startRound() {\n  state.isNightRound = false;\n  state.darkShop = false;\n  state.nightGoLevel = 0;\n  state.settleAfter = 'shop';\n  state.deck = shuffle(buildDeck(), rng);\n  state.hand = [];\n  state.selected = [];\n  state.roundScore = 0;\n  state.playsLeft = startPlays();\n  state.discardsLeft = 4;\n  state.goLevel = 0;\n  state.baseTarget = TARGETS[state.round - 1];\n  state.target = state.baseTarget;\n  state.kwangSoldThisRound = 0;\n  state.drunk = CHUNHYANG_ENABLED && !!state.chunhyangNext;\n  state.chunhyangNext = false;\n  state.hoverReact = null;\n  state.lastPlay = null;\n  state.playLog = [];\n  state.playedCards = [];\n  state.discardedCards = [];\n  state.pendingGoStop = false;\n  state.dealing = false;\n  state.janAuraLeft = state.round === 1 ? JAN_AURA_USES : 0;\n  state.janAuraCharged = false;\n  state.coachStep = null;\n  state.coachPendingScored = false;\n  state.coachAfterDeal = null;\n  // \ud29c\ud1a0 \ubbf8\uc644\uc774\uba74: \uccab \uc190 \uc870\ud569 \u2192 \ubc14\ub85c \ub2e4\uc74c \ubcf4\ucda9\uc740 \ubc84\ub9ac\uae30\n  state.janTutorJunkPending = state.round === 1 && !hasSeenJanCoach();\n\n  if (state.runStats) {\n    state.runStats.playTurnInRound = 0;\n    state.runStats.discardTurnInRound = 0;\n  }\n\n  state.boss = isBossRound(state.round) ? state.nextBoss : null;\n  state.nextBoss = null;\n  state.bossCandidates = null;\n  if (state.boss) {\n    state.usedBosses.push(state.boss);\n  }\n  state.discardsLeft = startDiscards();\n\n  if (hasJoker('morachigi')) {\n    const next = applyMorachigiResources(state.playsLeft, state.discardsLeft);\n    state.playsLeft = next.plays;\n    state.discardsLeft = next.discards;\n  }\n  state.gameunNuneMult = 0;\n\n  state.screen = 'play';\n  render(); // \ube48 \uc190\ud328\ubd80\ud130\n  dealToHand({ isRoundStart: true });\n}\n\n/** \uac19\uc740 \ub2ec \ubc24 \ud310 \u2014 \ubcf4\uc2a4\u00b7\ucd98\ud5a5\uc740 \ub0ae\uc5d0\uc11c \uc774\ubbf8 \uc18c\ube44. \ubaa9\ud45c\ub9cc \u00d72. */\ng.startNightRound = function startNightRound() {\n  state.isNightRound = true;\n  state.darkShop = false;\n  state.settleAfter = 'shop';\n  state.deck = shuffle(buildDeck(), rng);\n  state.hand = [];\n  state.selected = [];\n  state.roundScore = 0;\n  state.playsLeft = startPlays();\n  state.goLevel = 0;\n  state.baseTarget = TARGETS[state.round - 1] * 2;\n  state.target = state.baseTarget;\n  state.kwangSoldThisRound = 0;\n  state.hoverReact = null;\n  state.lastPlay = null;\n  state.playLog = [];\n  state.playedCards = [];\n  state.discardedCards = [];\n  state.pendingGoStop = false;\n  state.dealing = false;\n  state.janAuraLeft = 0;\n  state.janAuraCharged = false;\n  state.coachStep = null;\n  state.coachPendingScored = false;\n  state.coachAfterDeal = null;\n  state.janTutorJunkPending = false;\n  if (state.runStats) {\n    state.runStats.playTurnInRound = 0;\n    state.runStats.discardTurnInRound = 0;\n  }\n  state.discardsLeft = startDiscards();\n\n  if (hasJoker('morachigi')) {\n    const next = applyMorachigiResources(state.playsLeft, state.discardsLeft);\n    state.playsLeft = next.plays;\n    state.discardsLeft = next.discards;\n  }\n  state.gameunNuneMult = 0;\n  state.screen = 'play';\n  render();\n  dealToHand({ isRoundStart: true });\n}\n\ng.applyAngaeFaceDown = function applyAngaeFaceDown(drawn, isRoundStart) {\n  if (state.boss !== 'angae') return;\n  const pool = isRoundStart ? [...state.hand, ...drawn] : [...drawn];\n  let downCount = state.hand.filter((c) => c.faceDown).length;\n  const candidates = pool.filter((c) => !c.faceDown);\n  shuffle(candidates, rng);\n  for (const c of candidates) {\n    if (downCount >= 2) break;\n    c.faceDown = true;\n    downCount++;\n  }\n}\n\ng.refillHand = function refillHand(isRoundStart = false) {\n  const drawn = [];\n  while (state.hand.length < 8 && state.deck.length > 0) {\n    const c = state.deck.pop();\n    state.hand.push(c);\n    drawn.push(c);\n  }\n  applyAngaeFaceDown(drawn, isRoundStart);\n  sortHand();\n  return drawn;\n}\n\n// \ube48 \uc790\ub9ac \uc0c1\ud0dc\uc5d0\uc11c \ud55c \uc7a5\uc529 \ub4dc\ub85c\uc6b0 \u2192 \uc5f0\ucd9c \ud6c4 \uc815\ub82c\n/** 1\uc6d4 \uac1c\uad6d \uc190\ud328\uc5d0 \uc871\ubcf4\uac00 \uc788\ub3c4\ub85d drawn(8\uc7a5)\uc744 \uc7ac\uad6c\uc131. state.deck\ub294 \ub098\uba38\uc9c0. */\ng.ensureJanOpeningCombo = function ensureJanOpeningCombo(drawn) {\n  const env = scoreEnv();\n  const ok = (cards) => {\n    const r = evaluateHand(cards, env);\n    return r && r.handId && r.handId !== 'none';\n  };\n  if (ok(drawn)) return;\n\n  for (let n = 0; n < 48; n++) {\n    const pool = [...drawn, ...state.deck];\n    shuffle(pool, rng);\n    const trial = pool.slice(0, 8);\n    if (ok(trial)) {\n      drawn.length = 0;\n      drawn.push(...trial);\n      state.deck = pool.slice(8);\n      return;\n    }\n  }\n\n  // \ucd5c\ud6c4: \uac19\uc740 \ub2ec 2\uc7a5 \uac15\uc81c\n  const pool = [...drawn, ...state.deck];\n  const byM = {};\n  for (const c of pool) (byM[c.month] = byM[c.month] || []).push(c);\n  let pair = null;\n  for (const g of Object.values(byM)) {\n    if (g.length >= 2) { pair = [g[0], g[1]]; break; }\n  }\n  if (!pair) return;\n  const set = new Set(pair);\n  const rest = shuffle(pool.filter((c) => !set.has(c)), rng);\n  drawn.length = 0;\n  drawn.push(pair[0], pair[1], ...rest.slice(0, 6));\n  state.deck = rest.slice(6);\n}\n\n/** 1\uc6d4 \ubc84\ub9ac\uae30 \ud29c\ud1a0\uc6a9 \u2014 \ubb34\uc870\ud569 + \ubc84\ub9b4 \ud53c(\uc30d\ud53c) \ucd5c\uc18c 2\uc7a5 */\ng.janHandIsNone = function janHandIsNone(cards) {\n  const env = scoreEnv();\n  const r = evaluateHand(cards.filter((c) => !c.faceDown), env);\n  return !r || !r.handId || r.handId === 'none';\n}\ng.countJunkPi = function countJunkPi(cards) {\n  return cards.filter((c) => !c.faceDown && (effType(c) === 'pi' || effType(c) === 'ssangpi')).length;\n}\ng.ensureJanJunkTutorialHand = function ensureJanJunkTutorialHand(drawn) {\n  const ok = (trial) => janHandIsNone(trial) && countJunkPi(trial) >= 2;\n  if (ok(drawn)) return;\n\n  for (let n = 0; n < 128; n++) {\n    const pool = [...drawn, ...state.deck];\n    shuffle(pool, rng);\n    const trial = pool.slice(0, 8);\n    if (ok(trial)) {\n      drawn.length = 0;\n      drawn.push(...trial);\n      state.deck = pool.slice(8);\n      return;\n    }\n  }\n  ensureJanNoCombo(drawn);\n}\n\n/** 1\uc6d4 \ubc84\ub9ac\uae30 \ud29c\ud1a0\uc6a9 \u2014 drawn(8\uc7a5)\uc5d0 \uc871\ubcf4\uac00 \uc5c6\ub3c4\ub85d \uc7ac\uad6c\uc131 */\ng.ensureJanNoCombo = function ensureJanNoCombo(drawn) {\n  const env = scoreEnv();\n  const isNone = (cards) => {\n    const r = evaluateHand(cards.filter((c) => !c.faceDown), env);\n    return !r || !r.handId || r.handId === 'none';\n  };\n  if (isNone(drawn)) return;\n\n  for (let n = 0; n < 64; n++) {\n    const pool = [...drawn, ...state.deck];\n    shuffle(pool, rng);\n    const trial = pool.slice(0, 8);\n    if (isNone(trial)) {\n      drawn.length = 0;\n      drawn.push(...trial);\n      state.deck = pool.slice(8);\n      return;\n    }\n  }\n\n  // \ucd5c\ud6c4: \uac19\uc740 \ub2ec\u00b7\ud53c5 \ud53c\ud558\uae30\n  const pool = [...drawn, ...state.deck];\n  shuffle(pool, rng);\n  const pick = [];\n  const months = {};\n  let piSum = 0;\n  const tryAdd = (c, strict) => {\n    if (pick.includes(c)) return false;\n    const m = months[c.month] || 0;\n    if (strict && m >= 1) return false;\n    const addPi = piCount(c);\n    if (strict && piSum + addPi >= 5) return false;\n    pick.push(c);\n    months[c.month] = m + 1;\n    piSum += addPi;\n    return true;\n  };\n  for (const c of pool) {\n    if (pick.length >= 8) break;\n    tryAdd(c, true);\n  }\n  for (const c of pool) {\n    if (pick.length >= 8) break;\n    tryAdd(c, false);\n  }\n  const set = new Set(pick);\n  drawn.length = 0;\n  drawn.push(...pick);\n  state.deck = pool.filter((c) => !set.has(c));\n}\n\ng.dealToHand = async function dealToHand({ playGen = null, isRoundStart = false, forceJunkTutor = false } = {}) {\n  // \uace0/\uc2a4\ud1b1 \ub300\uae30 \uc911\uc5d0\ub294 \uc808\ub300 \ubcf4\ucda9\ud558\uc9c0 \uc54a\uc74c (\ub2e8, 1\uc6d4 \ubc84\ub9ac\uae30 \ud29c\ud1a0 \ubcf4\ucda9\uc740 \uc608\uc678)\n  const junkTutorDeal = forceJunkTutor\n    || (!isRoundStart && state.round === 1 && state.janTutorJunkPending);\n  if (state.pendingGoStop && !isRoundStart && !junkTutorDeal) return;\n  if (state.screen === 'gostop') return;\n\n  const dealGen = (state.dealGen = (state.dealGen || 0) + 1);\n  const aborted = () =>\n    dealGen !== state.dealGen ||\n    state.screen === 'welcome' || state.screen === 'prep' ||\n    (playGen != null && playGen !== state.playFadeGen);\n\n  let drawn = [];\n  while (state.hand.length + drawn.length < 8 && state.deck.length > 0) {\n    drawn.push(state.deck.pop());\n  }\n\n  // 1\uc6d4 \ud29c\ud1a0 2\ub2e8\uacc4: \uc870\ud569 \uc9c1\ud6c4 \ubcf4\ucda9 \u2192 \uc190\ud328 \ub9ac\uc14b \ud6c4 \ubb34\uc870\ud569\uc73c\ub85c \ubc84\ub9ac\uae30 \uc720\ub3c4\n  if (junkTutorDeal) {\n    for (let i = drawn.length - 1; i >= 0; i--) state.deck.push(drawn[i]);\n    drawn = [];\n    for (let i = state.hand.length - 1; i >= 0; i--) state.deck.push(state.hand[i]);\n    state.hand = [];\n    state.selected = [];\n    while (drawn.length < 8 && state.deck.length > 0) drawn.push(state.deck.pop());\n    applyAngaeFaceDown(drawn, false);\n    ensureJanJunkTutorialHand(drawn);\n  } else if (!drawn.length) {\n    if (!state.pendingGoStop) {\n      sortHand();\n      state.dealing = false;\n      render();\n    } else {\n      state.dealing = false;\n    }\n    return;\n  } else {\n    applyAngaeFaceDown(drawn, isRoundStart);\n    // 1\uc6d4 \uccab \ubc30\ubd84\ub9cc \u2014 \uc190\ud328\uc5d0 \uc871\ubcf4 \ucd5c\uc18c 1\uac1c \ubcf4\uc7a5\n    if (isRoundStart && state.round === 1 && state.hand.length === 0) {\n      ensureJanOpeningCombo(drawn);\n    }\n  }\n\n  if (!drawn.length) {\n    state.dealing = false;\n    render();\n    return;\n  }\n\n  // \uc5f0\ucd9c \uc804\uae4c\uc9c0 \ub371\uc5d0 \ub418\ub3cc\ub824, \uc7a5\uc218 \ud45c\uc2dc\uac00 \ud55c \uc7a5\uc529(33\u219232\u219231) \uc904\uc5b4\ub4e4\uac8c\n  for (let i = drawn.length - 1; i >= 0; i--) state.deck.push(drawn[i]);\n\n  state.dealing = true;\n  render(); // \ud328 \uc5c6\ub294/\ubaa8\uc790\ub780 \uc0c1\ud0dc \ub178\ucd9c (\ub371 \uce74\uc6b4\ud2b8\ub294 \uc544\uc9c1 \uc720\uc9c0)\n  await new Promise((r) => setTimeout(r, Math.max(1, Math.round((isRoundStart ? 113 : 175) / juiceSpeed))));\n  if (aborted() || state.pendingGoStop || state.screen === 'gostop') {\n    // \uc544\uc9c1 \uc190\ud328\ub85c \uc548 \uc62e\uae40 \u2014 \uce74\ub4dc\ub294 \ub371\uc5d0 \uadf8\ub300\ub85c\n    state.dealInUids = null;\n    state.dealing = false;\n    if (junkTutorDeal && !aborted()) state.janTutorJunkPending = true; // \ubcf4\ub958 \uc720\uc9c0\n    render();\n    return;\n  }\n\n  for (let i = 0; i < drawn.length; i++) {\n    if (aborted() || state.pendingGoStop || state.screen === 'gostop') {\n      // \ubbf8\uc9c0\uae09\ubd84\uc740 \ub371\uc5d0 \ub0a8\uc544 \uc788\uc74c\n      state.dealInUids = null;\n      state.dealing = false;\n      render();\n      return;\n    }\n    const card = state.deck.pop(); // drawn[i]\uc640 \ub3d9\uc77c \ucc38\uc870\n    state.hand.push(card);\n    state.dealInUids = [card.uid];\n    render();\n    try { sfxDealLift(); } catch (_) {}\n    await new Promise((r) => setTimeout(r, Math.max(1, Math.round(90 / juiceSpeed))));\n  }\n\n  await new Promise((r) => setTimeout(r, Math.max(1, Math.round(225 / juiceSpeed))));\n  if (aborted()) {\n    state.dealInUids = null;\n    state.dealing = false;\n    render();\n    return;\n  }\n  state.dealInUids = null;\n  render();\n  await new Promise((r) => setTimeout(r, 40));\n  if (aborted()) {\n    state.dealing = false;\n    render();\n    return;\n  }\n  await sortHandAnimated();\n  if (aborted()) {\n    state.dealing = false;\n    render();\n    return;\n  }\n  state.dealing = false;\n  if (junkTutorDeal) {\n    state.janTutorJunkPending = false;\n    if (!hasSeenJanCoach()) {\n      if (state.discardsLeft < 1) state.discardsLeft = 1;\n      state.coachStep = 'junk';\n    }\n  }\n  render();\n  maybeStartJanCoach(isRoundStart);\n  maybeStartDeckCoach();\n}\n\ng.TYPE_ORDER = { kwang: 0, yeol: 1, tti: 2, ssangpi: 3, pi: 4 };\n/** 'month' = \uc6d4\u2192\uc885\ub958 \u00b7 'type' = \uad11\u00b7\ub057\u00b7\ub760\u00b7\ud53c\u2192\uc6d4 */\ng.handSortMode = 'month';\ntry {\n  const saved = localStorage.getItem('hwatro_hand_sort');\n  if (saved === 'type' || saved === 'month') handSortMode = saved;\n} catch (_) {}\ng.sortHand = function sortHand() {\n  state.hand.sort((a, b) => {\n    const ta = TYPE_ORDER[a.type] ?? 9;\n    const tb = TYPE_ORDER[b.type] ?? 9;\n    if (handSortMode === 'type') {\n      if (ta !== tb) return ta - tb;\n      if (a.month !== b.month) return a.month - b.month;\n      return (a.uid ?? 0) - (b.uid ?? 0);\n    }\n    if (a.month !== b.month) return a.month - b.month;\n    if (ta !== tb) return ta - tb;\n    return (a.uid ?? 0) - (b.uid ?? 0);\n  });\n}\ng.syncHandSortThumb = function syncHandSortThumb() {\n  const root = $('hand-sort');\n  const thumb = root?.querySelector('.hand-sort-thumb');\n  const active = root?.querySelector('button.on');\n  if (!root || !thumb || !active) return;\n  thumb.style.width = `${active.offsetWidth}px`;\n  thumb.style.transform = `translateX(${active.offsetLeft}px)`;\n}\ng.syncHandSortBtn = function syncHandSortBtn() {\n  const byType = handSortMode === 'type';\n  for (const id of ['act-sort-month', 'act-sort-type']) {\n    const btn = $(id);\n    if (!btn) continue;\n    const isType = id === 'act-sort-type';\n    btn.classList.toggle('on', isType === byType);\n    btn.textContent = isType ? t('sortType') : t('sortMonth');\n  }\n  const sortGrp = $('hand-sort');\n  if (sortGrp) sortGrp.setAttribute('aria-label', t('settingsSort'));\n  requestAnimationFrame(syncHandSortThumb);\n}\ng.setHandSort = async function setHandSort(mode) {\n  if (mode !== 'month' && mode !== 'type') return;\n  if (handSortMode === mode) {\n    syncHandSortBtn();\n    return;\n  }\n  handSortMode = mode;\n  try { localStorage.setItem('hwatro_hand_sort', handSortMode); }\n  catch (_) {}\n  syncHandSortBtn();\n  if (state.screen === 'play' && !state.juicing && !state.dealing && state.hand.length >= 2) {\n    await sortHandAnimated();\n  } else {\n    sortHand();\n    if (state.screen === 'play') render();\n  }\n}\ng.toggleHandSort = async function toggleHandSort() {\n  await setHandSort(handSortMode === 'month' ? 'type' : 'month');\n}\n\ng.captureHandRects = function captureHandRects() {\n  const map = {};\n  document.querySelectorAll('#handarea [data-uid]').forEach((el) => {\n    map[+el.dataset.uid] = el.getBoundingClientRect();\n  });\n  return map;\n}\n\n// FLIP: \ub4dc\ub85c\uc6b0\u00b7\uc815\ub82c \uc804\ud658 \uc2dc \uc190\ud328\uac00 \ubbf8\ub044\ub7ec\uc9c0\ub4ef \uc774\ub3d9\ng.sortHandAnimated = async function sortHandAnimated() {\n  const handEl = $('handarea');\n  if (!handEl || state.hand.length < 2) {\n    sortHand();\n    render();\n    return;\n  }\n  const from = captureHandRects();\n  const before = state.hand.map((c) => c.uid).join(',');\n  sortHand();\n  if (before === state.hand.map((c) => c.uid).join(',')) {\n    render();\n    return;\n  }\n  render();\n  handEl.classList.add('sorting');\n  const els = [...handEl.querySelectorAll('[data-uid]')];\n  const movers = [];\n  for (const el of els) {\n    const prev = from[+el.dataset.uid];\n    if (!prev) continue;\n    const next = el.getBoundingClientRect();\n    const dx = prev.left - next.left;\n    const dy = prev.top - next.top;\n    if (Math.abs(dx) < 0.5 && Math.abs(dy) < 0.5) continue;\n    el.style.transition = 'none';\n    el.style.transform = `translate(${dx}px, ${dy}px)`;\n    movers.push(el);\n  }\n  if (!movers.length) {\n    handEl.classList.remove('sorting');\n    return;\n  }\n  await new Promise((r) => requestAnimationFrame(() => requestAnimationFrame(r)));\n  for (const el of movers) {\n    el.style.transition = 'transform .48s cubic-bezier(.22,.9,.28,1)';\n    el.style.transform = 'translate(0, 0)';\n  }\n  await new Promise((r) => setTimeout(r, Math.max(1, Math.round(360 / juiceSpeed))));\n  for (const el of movers) {\n    el.style.transition = '';\n    el.style.transform = '';\n  }\n  handEl.classList.remove('sorting');\n}\n\n// \ub0b8 \ud328 \ud45c\uc2dc\uc6a9 \uc815\ub82c: \ucf54\uc5b4(\uc67c\ucabd) \u2192 flat, \uac01 \uadf8\ub8f9\uc740 \uc6d4\u2191 \u00b7 \uce69\u2191 \u00b7 uid\ng.sortPlayedCards = function sortPlayedCards(cards, env) {\n  const biAsNormal = (env.jokerIds || []).includes('bigwang_usan');\n  let { handId, core } = detectHandInfo(cards, { biAsNormal });\n  if (env.boss === 'no_shake' && ['month2', 'month3', 'chongtong'].includes(handId)) {\n    core = [...cards];\n  }\n  const coreSet = new Set(core);\n  const byMonthChip = (a, b) => {\n    if (a.month !== b.month) return a.month - b.month;\n    const ca = cardChip(a, env), cb = cardChip(b, env);\n    if (ca !== cb) return ca - cb;\n    return a.uid - b.uid;\n  };\n  return [\n    ...cards.filter((c) => coreSet.has(c)).sort(byMonthChip),\n    ...cards.filter((c) => !coreSet.has(c)).sort(byMonthChip),\n  ];\n}\n\ng.selectedCards = function selectedCards() {\n  return state.selected.map((uid) => state.hand.find((c) => c.uid === uid)).filter(Boolean);\n}\n\ng.HAND_SWEEP = {\n  pointerId: null,\n  startX: 0,\n  startY: 0,\n  lastX: 0,\n  lastY: 0,\n  startUid: null,\n  painted: [],\n  baseSelected: [],\n  capped: false,\n  dragged: false,\n  suppressClick: false,\n};\n\ng.canHandSweep = function canHandSweep(e) {\n  if (state.screen !== 'play' || state.juicing || state.dealing) return false;\n  if (e.pointerType !== 'mouse' && e.pointerType !== 'pen') return false;\n  if (e.button != null && e.button !== 0) return false;\n  if ($('settings-overlay')?.classList.contains('show')) return false;\n  if ($('modal-overlay')?.classList.contains('show')) return false;\n  const area = $('handarea');\n  return !!(area && area.contains(e.target) && !area.classList.contains('sorting'));\n}\n\ng.sweepUidAt = function sweepUidAt(x, y) {\n  const rects = HAND_SWEEP.rects;\n  if (rects) {\n    for (const r of rects) {\n      if (x >= r.left && x <= r.right && y >= r.top && y <= r.bottom) return r.uid;\n    }\n    return null;\n  }\n  const el = document.elementFromPoint(x, y);\n  const card = el && el.closest('#handarea .card[data-uid]');\n  if (!card || card.classList.contains('static') || card.classList.contains('discarding')) return null;\n  return +card.dataset.uid;\n}\n\ng.cacheSweepRects = function cacheSweepRects() {\n  HAND_SWEEP.rects = [...document.querySelectorAll('#handarea .card[data-uid]')]\n    .filter((el) => !el.classList.contains('static') && !el.classList.contains('discarding'))\n    .map((el) => {\n      const r = el.getBoundingClientRect();\n      return { uid: +el.dataset.uid, left: r.left, right: r.right, top: r.top, bottom: r.bottom };\n    });\n}\n\ng.paintSweepPath = function paintSweepPath(x0, y0, x1, y1) {\n  const dist = Math.hypot(x1 - x0, y1 - y0);\n  const steps = Math.max(1, Math.ceil(dist / 10));\n  const seen = new Set(HAND_SWEEP.painted);\n  for (let i = 0; i <= steps; i++) {\n    const t = i / steps;\n    const uid = sweepUidAt(x0 + (x1 - x0) * t, y0 + (y1 - y0) * t);\n    if (uid != null) seen.add(uid);\n  }\n  if (HAND_SWEEP.startUid != null) seen.add(HAND_SWEEP.startUid);\n  HAND_SWEEP.painted = [...seen];\n}\n\ng.applySweepSelection = function applySweepSelection(uids) {\n  const base = HAND_SWEEP.baseSelected || [];\n  const baseSet = new Set(base);\n  const rangeSet = new Set(uids);\n  const next = base.filter((uid) => !rangeSet.has(uid));\n  let overflow = false;\n  for (const uid of uids) {\n    if (baseSet.has(uid)) continue;\n    if (next.length >= 5) { overflow = true; continue; }\n    next.push(uid);\n  }\n  const prev = state.selected;\n  if (prev.length === next.length && prev.every((u, i) => u === next[i])) {\n    if (overflow && !HAND_SWEEP.capped) {\n      HAND_SWEEP.capped = true;\n      flashActions();\n    }\n    return;\n  }\n  const added = next.some((u) => !prev.includes(u));\n  if (added) sfxCardSelect();\n  else sfxCardDeselect();\n  state.selected = next;\n  state.hoverReact = null;\n  document.querySelectorAll('#handarea .card[data-uid]').forEach((el) => {\n    el.classList.toggle('sel', state.selected.includes(+el.dataset.uid));\n  });\n  renderPreview();\n  syncPlayActions();\n  if (overflow && !HAND_SWEEP.capped) {\n    HAND_SWEEP.capped = true;\n    flashActions();\n  }\n}\n\ng.endHandSweep = function endHandSweep() {\n  HAND_SWEEP.pointerId = null;\n  HAND_SWEEP.dragged = false;\n  HAND_SWEEP.painted = [];\n  HAND_SWEEP.rects = null;\n  document.body.classList.remove('card-sweeping');\n}\n\ng.initHandSweep = function initHandSweep() {\n  document.addEventListener('pointerdown', (e) => {\n    if (!canHandSweep(e)) return;\n    const card = e.target.closest('#handarea .card[data-uid]');\n    HAND_SWEEP.pointerId = e.pointerId;\n    HAND_SWEEP.startX = e.clientX;\n    HAND_SWEEP.startY = e.clientY;\n    HAND_SWEEP.startUid = card && !card.classList.contains('static') && !card.classList.contains('discarding')\n      ? +card.dataset.uid : null;\n    HAND_SWEEP.lastX = e.clientX;\n    HAND_SWEEP.lastY = e.clientY;\n    HAND_SWEEP.painted = [];\n    HAND_SWEEP.baseSelected = state.selected.slice();\n    HAND_SWEEP.capped = false;\n    HAND_SWEEP.dragged = false;\n    HAND_SWEEP.suppressClick = false;\n  });\n  document.addEventListener('pointermove', (e) => {\n    if (HAND_SWEEP.pointerId !== e.pointerId) return;\n    const dx = e.clientX - HAND_SWEEP.startX;\n    const dy = e.clientY - HAND_SWEEP.startY;\n    if (!HAND_SWEEP.dragged && dx * dx + dy * dy < 64) return;\n    if (!HAND_SWEEP.dragged) {\n      HAND_SWEEP.dragged = true;\n      document.body.classList.add('card-sweeping');\n      cacheSweepRects();\n      try { $('handarea').setPointerCapture(e.pointerId); } catch (_) {}\n    }\n    paintSweepPath(HAND_SWEEP.lastX, HAND_SWEEP.lastY, e.clientX, e.clientY);\n    HAND_SWEEP.lastX = e.clientX;\n    HAND_SWEEP.lastY = e.clientY;\n    applySweepSelection(HAND_SWEEP.painted);\n  });\n  const finish = (e) => {\n    if (HAND_SWEEP.pointerId !== e.pointerId) return;\n    const dragged = HAND_SWEEP.dragged;\n    if (dragged) HAND_SWEEP.suppressClick = true;\n    try { $('handarea').releasePointerCapture(e.pointerId); } catch (_) {}\n    endHandSweep();\n    if (dragged) requestAnimationFrame(() => render());\n  };\n  document.addEventListener('pointerup', finish);\n  document.addEventListener('pointercancel', finish);\n  document.addEventListener('click', (e) => {\n    if (!HAND_SWEEP.suppressClick) return;\n    if (!e.target.closest('#handarea')) { HAND_SWEEP.suppressClick = false; return; }\n    e.preventDefault();\n    e.stopImmediatePropagation();\n    HAND_SWEEP.suppressClick = false;\n  }, true);\n  document.addEventListener('dragstart', (e) => {\n    if (e.target.closest && e.target.closest('#handarea .card')) e.preventDefault();\n  });\n}\n\ng.initCursorTrail = function initCursorTrail() {\n  const canvas = $('cursor-trail');\n  if (!canvas) return;\n  const ctx = canvas.getContext('2d', { alpha: true, desynchronized: true }) || canvas.getContext('2d');\n  const pts = [];\n  let raf = 0;\n  let lastDraw = 0;\n  let dirty = null;\n  const LIFE = 180;\n  const PAD = 16;\n\n  const resize = () => {\n    canvas.width = window.innerWidth;\n    canvas.height = window.innerHeight;\n    dirty = null;\n  };\n\n  const grow = (x, y) => {\n    if (!dirty) dirty = { l: x - PAD, t: y - PAD, r: x + PAD, b: y + PAD };\n    else {\n      if (x - PAD < dirty.l) dirty.l = x - PAD;\n      if (y - PAD < dirty.t) dirty.t = y - PAD;\n      if (x + PAD > dirty.r) dirty.r = x + PAD;\n      if (y + PAD > dirty.b) dirty.b = y + PAD;\n    }\n  };\n\n  const draw = (now) => {\n    raf = 0;\n    if (now - lastDraw < 32 && pts.length) {\n      raf = requestAnimationFrame(draw);\n      return;\n    }\n    lastDraw = now;\n    while (pts.length && now - pts[0].t > LIFE) pts.shift();\n    if (dirty) {\n      ctx.clearRect(dirty.l, dirty.t, dirty.r - dirty.l, dirty.b - dirty.t);\n      dirty = null;\n    }\n    if (pts.length < 2) {\n      if (pts.length) raf = requestAnimationFrame(draw);\n      return;\n    }\n    const sweep = HAND_SWEEP.dragged;\n    ctx.lineCap = 'round';\n    ctx.lineJoin = 'round';\n    ctx.beginPath();\n    ctx.moveTo(pts[0].x, pts[0].y);\n    grow(pts[0].x, pts[0].y);\n    for (let i = 1; i < pts.length; i++) {\n      ctx.lineTo(pts[i].x, pts[i].y);\n      grow(pts[i].x, pts[i].y);\n    }\n    ctx.strokeStyle = sweep ? 'rgba(232, 205, 122, 0.55)' : 'rgba(232, 205, 122, 0.26)';\n    ctx.lineWidth = sweep ? 4 : 2;\n    ctx.stroke();\n    if (sweep) {\n      ctx.strokeStyle = 'rgba(192, 57, 43, 0.3)';\n      ctx.lineWidth = 1.4;\n      ctx.stroke();\n    }\n    raf = requestAnimationFrame(draw);\n  };\n\n  const shouldTrail = (e) => {\n    if (e.pointerType && e.pointerType !== 'mouse' && e.pointerType !== 'pen') return false;\n    if (window.matchMedia('(hover: none)').matches) return false;\n    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return false;\n    return true;\n  };\n\n  window.addEventListener('pointermove', (e) => {\n    if (!shouldTrail(e)) return;\n    const now = performance.now();\n    const sweeping = HAND_SWEEP.dragged;\n    const last = pts[pts.length - 1];\n    if (last) {\n      const d = Math.hypot(e.clientX - last.x, e.clientY - last.y);\n      if (d < 6) return;\n    }\n    pts.push({ x: e.clientX, y: e.clientY, t: now });\n    while (pts.length > (sweeping ? 14 : 10)) pts.shift();\n    if (!raf) raf = requestAnimationFrame(draw);\n  }, { passive: true });\n  document.addEventListener('pointerout', (e) => {\n    if (!e.relatedTarget) pts.length = 0;\n  });\n  window.addEventListener('resize', resize);\n  resize();\n}\n\n/** \ucd94\ucc9c \ubb36\uc74c \ud074\ub9ad: \ubbf8\uc120\ud0dd\uc774\uba74 \uc804\uccb4 \uc120\ud0dd, \uc774\ubbf8 \uc120\ud0dd\ub41c \uc7a5\uc774\uba74 \uadf8 \uc7a5\ub9cc \ud574\uc81c */\ng.toggleHintBundle = function toggleHintBundle(uid, uids, { charge = false } = {}) {\n  if (!uids.includes(uid)) return false;\n  if (state.selected.includes(uid)) {\n    state.selected = state.selected.filter((u) => u !== uid);\n    sfxCardDeselect();\n    if (charge && state.janAuraCharged && !uids.some((u) => state.selected.includes(u))) {\n      state.janAuraLeft = Math.min(JAN_AURA_USES, state.janAuraLeft + 1);\n      state.janAuraCharged = false;\n    }\n  } else {\n    state.selected = uids.slice(0, 5);\n    if (charge && !state.janAuraCharged && state.janAuraLeft > 0) {\n      state.janAuraLeft = Math.max(0, state.janAuraLeft - 1);\n      state.janAuraCharged = true;\n    }\n    sfxCardSelect();\n  }\n  return true;\n}\n\n/** \uc120\ud0dd\ub9cc \ubc14\ub010 \uacbd\uc6b0 \u2014 \uc804\uccb4 render() \ub300\uc2e0 \uc190\ud328\u00b7\ud504\ub9ac\ubdf0\ub9cc (\ubaa8\ubc14\uc77c \ub809 \uc644\ud654) */\ng.syncHandSelectionUi = function syncHandSelectionUi() {\n  document.querySelectorAll('#handarea .card[data-uid]').forEach((el) => {\n    el.classList.toggle('sel', state.selected.includes(+el.dataset.uid));\n  });\n  renderPreview();\n  syncPlayActions();\n}\n\ng.toggleSelect = function toggleSelect(uid) {\n  if (HAND_SWEEP.suppressClick) return;\n  if (state.screen !== 'play' || state.juicing || state.dealing) return;\n  // 1\uc6d4 \ubc84\ub9ac\uae30 \ud29c\ud1a0: \ube68\uac04 \ube5b \u2014 \ub204\ub974\uba74 \uc77c\uad04, \uc120\ud0dd\ub41c \uc7a5 \ub2e4\uc2dc \ub204\ub974\uba74 \uadf8 \uc7a5\ub9cc \ud574\uc81c\n  if (state.coachStep === 'junk') {\n    const junk = getJanJunkSuggest();\n    if (toggleHintBundle(uid, junk)) {\n      state.hoverReact = null;\n      render();\n      return;\n    }\n  }\n  // \ucd94\ucc9c \uc624\ub77c: \ubbf8\uc120\ud0dd \ucd94\ucc9c\ud328 \ud074\ub9ad \u2192 \uc804\uccb4 \uc120\ud0dd / \uc120\ud0dd\ub428 \u2192 \uadf8 \uc7a5\ub9cc \ud574\uc81c\n  const hint = state.coachStep === 'junk' ? null : getActiveCoachHint();\n  if (hint && hint.uids.includes(uid)) {\n    // \ubb34\ub8cc 3\ud68c: \ub0b4\uae30\u00b7\ubc84\ub9ac\uae30 \ucd94\ucc9c \uc77c\uad04\uc120\ud0dd \ubaa8\ub450 \ud69f\uc218 \uc18c\ubaa8\n    const charge = state.janAuraLeft > 0;\n    toggleHintBundle(uid, hint.uids, { charge });\n    if (state.coachStep === 'hand' && state.selected.length) state.coachStep = 'play';\n    state.hoverReact = null;\n    render();\n    return;\n  }\n  const i = state.selected.indexOf(uid);\n  if (i >= 0) {\n    state.selected.splice(i, 1);\n    sfxCardDeselect();\n  } else {\n    if (state.selected.length >= 5) { flashActions(); return; }\n    state.selected.push(uid);\n    sfxCardSelect();\n  }\n  state.hoverReact = null;\n  syncHandSelectionUi();\n}\n\ng.toggleHintEnabled = function toggleHintEnabled() {\n  if (state.round !== 1 || state.screen !== 'play') return;\n  // \ubb34\ub8cc 3\ud68c \ub0a8\uc558\uc73c\uba74 \uac15\uc81c ON \u2014 \ub04c \uc218 \uc5c6\uc74c\n  if (state.janAuraLeft > 0) return;\n  state.hintEnabled = !state.hintEnabled;\n  render();\n}\n\n/** \ub0b4\uae30\u00b7\uc815\uc0b0 \ub4f1 juiceMs \uc5f0\ucd9c \ubc30\uc18d (1 \u2194 2) */\ng.juiceSpeed = 1;\ntry {\n  if (+localStorage.getItem('hwatro_fx_speed') === 2) juiceSpeed = 2;\n} catch (_) {}\ng.setJuiceSpeed = function setJuiceSpeed(n) {\n  juiceSpeed = n === 2 ? 2 : 1;\n  try { localStorage.setItem('hwatro_fx_speed', String(juiceSpeed)); }\n  catch (_) {}\n  syncJuiceSpeedBtn();\n}\ng.toggleJuiceSpeed = function toggleJuiceSpeed() {\n  setJuiceSpeed(juiceSpeed === 2 ? 1 : 2);\n}\ng.syncJuiceSpeedBtn = function syncJuiceSpeedBtn() {\n  const fast = juiceSpeed === 2;\n  const btn = $('set-fx');\n  if (!btn) return;\n  btn.classList.toggle('on', fast);\n  btn.setAttribute('aria-pressed', fast ? 'true' : 'false');\n}\n\n/** \uc0ac\uc774\ub4dc '\ub0b4\uae30 \uae30\ub85d' \ud328\ub110 \ud45c\uc2dc */\ng.playLogVisible = true;\ntry {\n  if (localStorage.getItem('hwatro_playlog') === '0') playLogVisible = false;\n} catch (_) {}\ng.setPlayLogVisible = function setPlayLogVisible(on) {\n  playLogVisible = !!on;\n  try { localStorage.setItem('hwatro_playlog', playLogVisible ? '1' : '0'); }\n  catch (_) {}\n  syncPlayLogBtn();\n  const lp = $('playlog-panel');\n  if (!lp) return;\n  if (playLogVisible && state.playLog && state.playLog.length > 0) {\n    lp.style.display = 'block';\n  } else {\n    lp.style.display = 'none';\n  }\n}\ng.togglePlayLogVisible = function togglePlayLogVisible() {\n  setPlayLogVisible(!playLogVisible);\n}\ng.syncPlayLogBtn = function syncPlayLogBtn() {\n  const btn = $('set-playlog');\n  if (!btn) return;\n  btn.classList.toggle('on', playLogVisible);\n  btn.setAttribute('aria-pressed', playLogVisible ? 'true' : 'false');\n}\n\n";
new Function('g', 'with (g) {\n' + code + '\n}')(g);
