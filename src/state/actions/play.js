import { g } from '../../runtime/scope.ts';

const code = "// \u2500\u2500 \ub0b4\uae30 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ng.playSelected = function playSelected() {\n  if (state.screen !== 'play' || state.juicing || state.dealing) return;\n  const cards = selectedCards();\n  if (cards.length < 1 || cards.length > 5 || state.playsLeft <= 0) return;\n\n  if (state.coachStep === 'hand' || state.coachStep === 'play') {\n    state.coachPendingScored = true;\n    state.coachStep = null;\n  } else if (state.coachStep === 'junk') {\n    flashActions();\n    return;\n  } else if (state.coachStep === 'deck') {\n    state.coachStep = null;\n    markDeckCoachSeen();\n  } else if (state.coachStep === 'scored') {\n    state.coachStep = null;\n    // \ubc84\ub9ac\uae30 \ud29c\ud1a0\uac00 \ub0a8\uc558\uc73c\uba74 \uc644\ub8cc \ucc98\ub9ac \ubcf4\ub958\n    if (!state.janTutorJunkPending) markJanCoachSeen();\n  }\n  if (state.round === 1 && !hasSeenJanCoach()) state.janTutorJunkPending = true;\n  // \ub0b4\uae30\ub85c \ud655\uc815 \u2192 \ucd94\ucc9c \ud69f\uc218\ub294 \uc18c\ubaa8\ub41c \ucc44, \ub2e4\uc74c \uc190\uc5d0\uc11c \uc0c8\ub85c \uacfc\uae08\n  state.janAuraCharged = false;\n\n  // \uc548\uac1c: \ub4b7\uba74 \uce74\ub4dc \uacf5\uac1c\n  for (const c of cards) {\n    if (c.faceDown) c.faceDown = false;\n  }\n\n  const env = scoreEnv();\n  const result = computeScore(cards, env);\n  const scoreBefore = state.roundScore;\n  if (hasJoker('gameun_nun')) state.gameunNuneMult = 0;\n\n  if (hasJoker('geumjul')) {\n    const loss = bakChipLoss(cards, env);\n    if (loss > 0) {\n      state.geumjulLost += loss;\n      state.geumjulMult = Math.floor(state.geumjulLost / 10);\n    }\n  }\n\n  // \uacbd\uc81c\u00b7\uc774\ub4dd \uc9d1\uacc4\ub9cc \u2014 \uc2e4\uc81c \uc9c0\uae09/\uc5f0\ucd9c\uc740 \uc8fc\uc2a4 \uc544\uc774\ud15c \uc815\uc0b0 \ud398\uc774\uc988\uc5d0\uc11c\n  const triggeredJokers = [];\n  const jokerScoreGains = [];\n  let pendingGwangpari = 0;\n  if (hasJoker('gwangpari') && result.kwangPlayed > 0) {\n    const times = 1 + (geoulNeighborIds(env.jokerIds).includes('gwangpari') ? 1 : 0);\n    pendingGwangpari = Math.min(result.kwangPlayed * times, 3 - state.kwangSoldThisRound);\n    if (pendingGwangpari > 0) triggeredJokers.push('gwangpari');\n  }\n  for (const id of env.jokerIds) {\n    const g = jokerMarginalGain(cards, env, id);\n    if (g) {\n      jokerScoreGains.push({ id, score: g });\n      if (!triggeredJokers.includes(id)) triggeredJokers.push(id);\n    }\n  }\n\n  state.roundScore += result.score;\n  if (state.runStats) {\n    state.runStats.playTurnInRound++;\n    trackEvent('hand_play', {\n      month: state.round,\n      play_turn: state.runStats.playTurnInRound,\n      hand_id: result.handId,\n      hand_label: uiHandName(result.handId, result.core),\n      cards_played: cardsTelemetrySummary(cards),\n      score: result.score,\n      money: state.money,\n      round_score: state.roundScore,\n    });\n  }\n  if (result.score > state.stats.bestSingle) {\n    state.stats.bestSingle = result.score;\n    state.stats.bestSingleHand = uiHandName(result.handId, result.core);\n  }\n  state.playsLeft--;\n  state.playLog.push({ hand: uiHandName(result.handId, result.core), handId: result.handId, score: result.score });\n  for (const c of cards) {\n    state.playedCards.push({ ...c, tags: [...(c.tags || [])], faceDown: false });\n  }\n\n  // \ub0b8 \ud328 \uc2a4\ub0c5\uc0f7 \u2014 \ucf54\uc5b4\u2192flat, \uc6d4\u00b7\uce69 \uc21c\uc73c\ub85c \uc815\ub82c\ud574 \uc5f0\ucd9c/\ud45c\uc2dc\n  // \uc120\ud0dd transform(\ub4e4\ub9bc\u00b7scale) \uc5c6\uc774 \uce21\uc815\ud574\uc57c \uc2ac\ub86f \ucc29\uc9c0\uac00 \uc548 \uc5b4\uae0b\ub0a8\n  const fromRects = {};\n  for (const c of cards) {\n    const el = document.querySelector(`#handarea [data-uid=\"${c.uid}\"]`);\n    if (!el) continue;\n    const hadSel = el.classList.contains('sel');\n    const prevTr = el.style.transition;\n    el.style.transition = 'none';\n    if (hadSel) el.classList.remove('sel');\n    void el.offsetWidth;\n    fromRects[c.uid] = el.getBoundingClientRect();\n    if (hadSel) el.classList.add('sel');\n    el.style.transition = prevTr;\n  }\n  const sorted = sortPlayedCards(cards, env);\n  state.lastPlay = {\n    cards: sorted.map((c) => ({ ...c, tags: [...c.tags] })),\n    result,\n    scoreBefore,\n    env: { ...env, jokerIds: [...env.jokerIds] },\n    triggeredJokers,\n    jokerScoreGains,\n    pendingGwangpari,\n    _econApplied: false,\n  };\n\n  state.hand = state.hand.filter((c) => !state.selected.includes(c.uid));\n  state.selected = [];\n  // \ubaa9\ud45c \ub2ec\uc131 \uc2dc \uace0/\uc2a4\ud1b1 \ub300\uae30 (\ud328\uac00 \ubaa8\uc790\ub77c\ub3c4 \uc120\ud0dd\uc740 \uc5f0\ub2e4. \ub4dc\ub85c\uc6b0 \ubcf4\ub958)\n  state.pendingGoStop = state.roundScore >= state.target && state.round !== ROUNDS;\n\n  // \uace0/\uc2a4\ud1b1\u00b7\uac8c\uc784\uc624\ubc84\ub294 \uc5f0\ucd9c \ub05d\ub09c \ub4a4\uc5d0 (\ubaa8\ub2ec \uc911\ucca9\u00b7\uc870\uae30 \uc804\ud658 \ubc29\uc9c0)\n  state.juicing = true;\n  document.body.classList.add('juicing');\n  render();\n  const playGen = (state.playFadeGen = (state.playFadeGen || 0) + 1);\n  playJuiceSequence(state.lastPlay, fromRects).then(async () => {\n    state.juicing = false;\n    document.body.classList.remove('juicing');\n\n    // \ubaa9\ud45c \ub2ec\uc131: \ub4dc\ub85c\uc6b0 \uc5c6\uc774 \uace0/\uc2a4\ud1b1\n    if (state.roundScore >= state.target && state.round !== ROUNDS) {\n      if (await pauseForCoachScored(playGen)) return;\n      // 1\uc6d4: \uccab \ub0b4\uae30 \ud6c4 \ubc84\ub9ac\uae30 \ud29c\ud1a0\uac00 \ub0a8\uc558\uc73c\uba74 \ubaa9\ud45c \ub2ec\uc131\ud574\ub3c4 \ubcf4\ucda9\ubd80\ud130\n      if (state.janTutorJunkPending && state.round === 1 && !hasSeenJanCoach()) {\n        state.pendingGoStop = false;\n        state.dealing = true;\n        render();\n        await new Promise((r) => setTimeout(r, 420));\n        if (playGen !== state.playFadeGen) { state.dealing = false; return; }\n        const pa = $('playedarea');\n        if (pa && state.lastPlay) {\n          pa.classList.add('play-fade-out');\n          await new Promise((r) => setTimeout(r, 420));\n        }\n        if (playGen !== state.playFadeGen) { state.dealing = false; return; }\n        state.lastPlay = null;\n        if (pa) pa.classList.remove('play-fade-out');\n        render();\n        await dealToHand({ playGen, forceJunkTutor: true });\n        if (playGen !== state.playFadeGen) { state.dealing = false; return; }\n        if (state.roundScore >= state.target) state.pendingGoStop = true;\n        checkAfterPlay();\n        render();\n        return;\n      }\n      state.coachAfterDeal = null;\n      // \ubc84\ub9ac\uae30 \ud29c\ud1a0 \ub300\uae30 \uc911\uc774\uba74 seen\uc740 \uace0 \ub4dc\ub85c\uc6b0/\uc2a4\ud1b1\uc5d0\uc11c \ucc98\ub9ac\n      checkAfterPlay();\n      render();\n      await new Promise((r) => setTimeout(r, 500));\n      if (playGen !== state.playFadeGen) return;\n      const pa = $('playedarea');\n      if (pa && state.lastPlay) {\n        pa.classList.add('play-fade-out');\n        await new Promise((r) => setTimeout(r, 420));\n      }\n      if (playGen !== state.playFadeGen) return;\n      state.lastPlay = null;\n      if (pa) pa.classList.remove('play-fade-out');\n      render();\n      return;\n    }\n\n    // \ub4dc\ub85c\uc6b0\u00b7\uc815\ub82c \ub05d\ub0a0 \ub54c\uae4c\uc9c0 \uc870\ud569 \ud78c\ud2b8 \uc228\uae40 (\ube48 \uc790\ub9ac \uc190\ud328\uc5d0 \uc624\ub77c \ub728\uc9c0 \uc54a\uac8c)\n    state.dealing = true;\n    render(); // \uc815\ub82c\ub41c \ub0b8 \ud328 + \uacb0\uacfc \uc7a0\uae50 \uace0\uc815\n    if (await pauseForCoachScored(playGen)) return;\n    await new Promise((r) => setTimeout(r, 420));\n    if (playGen !== state.playFadeGen) { state.dealing = false; return; }\n    const pa = $('playedarea');\n    if (pa && state.lastPlay) {\n      pa.classList.add('play-fade-out');\n      await new Promise((r) => setTimeout(r, 420));\n    }\n    if (playGen !== state.playFadeGen) { state.dealing = false; return; }\n    state.lastPlay = null;\n    if (pa) pa.classList.remove('play-fade-out');\n\n    if (state.roundScore >= state.target) {\n      // 12\uc6d4 \ud074\ub9ac\uc5b4 \ub4f1\n      state.dealing = false;\n      checkAfterPlay();\n      render();\n      return;\n    }\n\n    render();\n    await dealToHand({ playGen });\n    if (playGen !== state.playFadeGen) { state.dealing = false; return; }\n    if (state.coachAfterDeal) {\n      state.coachStep = state.coachAfterDeal;\n      state.coachAfterDeal = null;\n    }\n    checkAfterPlay();\n    render();\n  });\n}\n\ng.cardsRemaining = function cardsRemaining() {\n  return state.hand.length + state.deck.length;\n}\n\n/** \uace0/\uc2a4\ud1b1\uc744 \ub744\uc6b8\uc9c0 \u2014 12\uc6d4\uc740 \uace0 \uc5c6\uc774 \uc815\uc0b0 */\ng.canDeclareGo = function canDeclareGo() {\n  return state.round !== ROUNDS;\n}\n\ng.afterClearChoice = function afterClearChoice() {\n  if (state.isNightRound) {\n    state.nightGoLevel = state.goLevel;\n    state.darkShop = true;\n    state.settleAfter = 'shop';\n    settle();\n    return;\n  }\n  if (canAskNight()) {\n    state.screen = 'nightask';\n    return;\n  }\n  state.settleAfter = state.round === ROUNDS ? 'victory' : 'shop';\n  settle();\n}\n\ng.onRoundCleared = function onRoundCleared() {\n  if (state.round === ROUNDS) {\n    state.pendingGoStop = false;\n    afterClearChoice();\n    return;\n  }\n  state.pendingGoStop = true;\n  state.screen = 'gostop';\n  maybeStartGoStopCoach();\n}\n\ng.checkAfterPlay = function checkAfterPlay() {\n  if (state.roundScore >= state.target) {\n    onRoundCleared();\n  } else if (state.playsLeft === 0 || cardsRemaining() === 0) {\n    gameOver(state.goLevel > 0 ? 'gobak' : 'miss');\n  }\n}\n\ng.chooseGo = async function chooseGo() {\n  if (state.screen !== 'gostop' || state.dealing || goBtnCascadeBusy) return;\n  if (state.coachStep === 'gostop') {\n    state.coachStep = null;\n    markGoStopCoachSeen();\n  }\n  const fromLevel = state.goLevel;\n  // \ubc00\uce58\uae30 \uace0: \uc774\ubbf8 \ub118\uc5b4 \uc788\ub294 \ubb38\ud131 \uc804\ubd80\ub97c \uc18c\uae09 \uc778\uc815, \uc120\uc5b8 \ub2e8\uacc4 = \uc544\uc9c1 \ubabb \ub118\uc740 \uccab \ubb38\ud131\n  state.goLevel = goLevelReached(state.baseTarget, state.roundScore, state.goLevel) + 1;\n  state.target = goThreshold(state.baseTarget, state.goLevel);\n  state.playsLeft++; // \uace0 \ubcf4\uc0c1: \ub0b4\uae30 +1 (\ubc00\uce5c \ub2e8\uacc4 \uc218\uc640 \ubb34\uad00\ud558\uac8c \uc120\uc5b8 1\ud68c\ub2f9 1\uac1c)\n  if (hasJoker('gotaryeong')) {\n    state.gotaryeongGoes = (state.gotaryeongGoes || 0) + 1;\n    state.gotaryeongMult = gotaryeongMultFromGoes(state.gotaryeongGoes);\n  }\n  state.pendingGoStop = false;\n  state.screen = 'play';\n  state.dealing = true; // \ub4dc\ub85c\uc6b0\u00b7\uc815\ub82c \uc804 \uc870\ud569 \ud78c\ud2b8 \uc228\uae40\n  render(); // \ubaa8\ub2ec \ub2eb\uace0 \ubaa8\uc790\ub780 \uc190\ud328 \ub178\ucd9c\n  // \ud655\uc815 \ud480\uc2a4\ud06c\ub9b0\uc740 \uc2e4\uc81c \uc810\ud504 \uad6c\uac04\ub9cc (\ubc84\ud2bc \uc5f0\ucd9c\uc740 1\u2192N \uc720\uc9c0)\n  await playGoCascade(fromLevel, state.goLevel);\n  await dealToHand();\n  checkAfterPlay();\n  render();\n}\n\ng.chooseStop = function chooseStop() {\n  if (state.screen !== 'gostop') return;\n  if (state.coachStep === 'gostop') {\n    state.coachStep = null;\n    markGoStopCoachSeen();\n  }\n  if (state.janTutorJunkPending) {\n    state.janTutorJunkPending = false;\n    if (!hasSeenJanCoach()) markJanCoachSeen();\n  }\n  state.pendingGoStop = false;\n  afterClearChoice();\n  render();\n}\n\ng.acceptNight = function acceptNight() {\n  if (state.screen !== 'nightask') return;\n  hideNightAsk();\n  state.settleAfter = 'night';\n  settle();\n  render();\n}\n\ng.declineNight = function declineNight() {\n  if (state.screen !== 'nightask') return;\n  hideNightAsk();\n  state.settleAfter = state.round === ROUNDS ? 'victory' : 'shop';\n  settle();\n  render();\n}\n\n";
new Function('g', 'with (g) {\n' + code + '\n}')(g);
