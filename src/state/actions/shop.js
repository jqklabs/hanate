import { g } from '../../runtime/scope.ts';

const code = "// \u2500\u2500 \uc8fc\ub9c9(\uc0c1\uc810) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ng.jumakAppearBusy = false;\ng.openShop = async function openShop() {\n  if (jumakAppearBusy) return;\n  state.rerollCost = 2;\n  state.shopOffers = genOffers();\n  const next = state.round + 1;\n  state.nextBoss = null;\n  state.bossCandidates = null;\n  if (isBossRound(next)) {\n    const pool = (next === 3 ? MILD_BOSSES : BOSSES.map((b) => b.id))\n      .filter((id) => !state.usedBosses.includes(id));\n    shuffle(pool, rng);\n    if (next === ROUNDS) {\n      state.bossCandidates = pool.slice(0, 2);\n      if (state.bossCandidates.length === 1) state.nextBoss = state.bossCandidates[0];\n    } else {\n      state.nextBoss = pool[0];\n    }\n  }\n  jumakAppearBusy = true;\n  try {\n    await playJumakAppear();\n    // \uc8fc\ub9c9 \ub4f1\uc7a5 \u2192 \uc0c1\uc810 UI \uc0ac\uc774 \uc9e7\uc740 \uc5ec\uc6b4\n    await new Promise((res) => setTimeout(res,\n      window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 64 : 224));\n  } finally {\n    jumakAppearBusy = false;\n  }\n  state.screen = 'shop';\n  maybeStartShopCoach();\n  render();\n}\n\ng.genOffers = function genOffers() {\n  const owned = jokerIds();\n  const dark = !!state.darkShop && !hasDarkJoker();\n  const pool = JOKERS.filter((j) => !owned.includes(j.id) && (j.rarity !== 'dark' || dark));\n  const offers = [];\n  for (let i = 0; i < 3; i++) {\n    const avail = pool.filter((j) => !offers.some((o) => o.joker.id === j.id));\n    if (!avail.length) break;\n    const want = rollShopRarity(rng, state.round, { dark, nightGo: state.nightGoLevel });\n    let cands = avail.filter((j) => j.rarity === want);\n    if (!cands.length) {\n      // \uc554\ud751 \ud480\uc774 \ube44\uc5c8\uac70\ub098 \ud574\ub2f9 \ud2f0\uc5b4\uac00 \uc5c6\uc73c\uba74 \uc77c\ubc18 \ud2f0\uc5b4\ub85c \ud3f4\ubc31\n      for (const r of RARITY_ORDER) {\n        cands = avail.filter((j) => j.rarity === r);\n        if (cands.length) break;\n      }\n    }\n    if (!cands.length) cands = avail;\n    offers.push({ joker: cands[Math.floor(rng() * cands.length)], sold: false });\n  }\n  return offers;\n}\n\ng.buyJoker = function buyJoker(i) {\n  if (state.screen !== 'shop') return;\n  const offer = state.shopOffers[i];\n  if (!offer || offer.sold) return;\n  if (offer.joker.rarity === 'dark' && hasDarkJoker()) { flashActions(); return; }\n  if (state.jokers.length >= openJokerSlots() || state.money < offer.joker.price) { flashActions(); return; }\n  if (offer.joker.id === 'oegil') {\n    state.oegilPickOffer = i;\n    openOegilPick();\n    return;\n  }\n  completeBuyJoker(i);\n}\n\ng.completeBuyJoker = function completeBuyJoker(i, extra = {}) {\n  if (state.screen !== 'shop') return;\n  const offer = state.shopOffers[i];\n  if (!offer || offer.sold) return;\n  if (offer.joker.rarity === 'dark' && hasDarkJoker()) { flashActions(); return; }\n  if (state.jokers.length >= openJokerSlots() || state.money < offer.joker.price) { flashActions(); return; }\n  state.money -= offer.joker.price;\n  trackEvent('shop_buy', {\n    month: state.round,\n    joker_id: offer.joker.id,\n    price: offer.joker.price,\n    money_after: state.money,\n    oegil_kind: extra.oegilKind || '',\n  });\n  state.jokers.push({ id: offer.joker.id, paid: offer.joker.price, ...extra });\n  if (!state.jokerGains[offer.joker.id]) state.jokerGains[offer.joker.id] = { score: 0, money: 0 };\n  offer.sold = true;\n  if (offer.joker.id === 'binjari') {\n    applyBinjariPurchase();\n    render();\n    return;\n  }\n  state.shopFlashOwned = state.jokers.length - 1;\n  render();\n  setTimeout(() => {\n    if (state.shopFlashOwned == null) return;\n    state.shopFlashOwned = null;\n    const el = document.querySelector('.owned-slot.filled.pop');\n    if (el) el.classList.remove('pop');\n  }, 420);\n}\n\ng.OEGIL_ART = {\n  kwang: 'Assets/01_january/1-gwang.webp',\n  yeol: 'Assets/02_february/2-yeolkkeut-warbler.webp',\n  tti: 'Assets/01_january/1-tti-hongdan.webp',\n  pi: 'Assets/01_january/1-pi-1.webp',\n};\ng.oegilKindLabel = function oegilKindLabel(kind) {\n  return {\n    kwang: tu('chipKwang'),\n    yeol: tu('chipYeol'),\n    tti: tu('chipTti'),\n    pi: tu('chipPi'),\n  }[kind] || kind;\n}\ng.jokerDisplayName = function jokerDisplayName(j) {\n  const def = JOKER_BY_ID[j.id];\n  const uj = uiJoker(def);\n  if (j.id === 'paldoyuram' && paldoyuramEvolved((state.scoredMonths || []).length))\n    return t('geumsugangsan');\n  if (j.id === 'oegil' && j.oegilKind) return `${uj.name} \u00b7 ${oegilKindLabel(j.oegilKind)}`;\n  return uj.name;\n}\ng.openOegilPick = function openOegilPick() {\n  const ov = $('oegil-overlay');\n  if (!ov) return;\n  $('oegil-title').textContent = tu('oegilPickH2');\n  $('oegil-sub').textContent = tu('oegilPickSub');\n  $('oegil-cancel').textContent = tu('oegilPickCancel');\n  const grid = $('oegil-grid');\n  grid.innerHTML = OEGIL_KINDS.map((kind) => {\n    const chip = kind === 'pi' ? CHIP.pi : CHIP[kind];\n    return `<button type=\"button\" class=\"oegil-path\" onclick=\"confirmOegil('${kind}')\">\n      <img src=\"${OEGIL_ART[kind]}\" alt=\"\" draggable=\"false\">\n      <span class=\"op-name\">${esc(oegilKindLabel(kind))}</span>\n      <span class=\"op-chip\">${tu('oegilPickChip')(chip)}</span>\n      <span class=\"op-note\">${tu('oegilPickNote')}</span>\n    </button>`;\n  }).join('');\n  ov.classList.add('show');\n  ov.setAttribute('aria-hidden', 'false');\n}\ng.closeOegilPick = function closeOegilPick() {\n  const ov = $('oegil-overlay');\n  if (ov) {\n    ov.classList.remove('show');\n    ov.setAttribute('aria-hidden', 'true');\n  }\n  state.oegilPickOffer = null;\n}\ng.confirmOegil = function confirmOegil(kind) {\n  if (!OEGIL_KINDS.includes(kind)) return;\n  const i = state.oegilPickOffer;\n  closeOegilPick();\n  if (i == null) return;\n  completeBuyJoker(i, { oegilKind: kind });\n}\n\n/** \ubcf4\uc720 \ud2b9\uc218\ud328 \ud234\ud301 \u2014 \ud654\uba74 \uc88c\uc6b0\uc5d0 \uc548 \uc798\ub9ac\uac8c \uc704\uce58 \ubcf4\uc815 */\ng.positionOwnedTip = function positionOwnedTip(slotEl) {\n  const tip = slotEl?.querySelector('.os-tip');\n  if (!tip || isTouchUI()) return;\n  requestAnimationFrame(() => {\n    const open = slotEl.matches(':hover') || slotEl.classList.contains('tip-open');\n    if (!open) {\n      resetOwnedTip(slotEl);\n      return;\n    }\n    tip.classList.add('tip-fixed');\n    const margin = 12;\n    const slotR = slotEl.getBoundingClientRect();\n    const maxW = Math.min(260, window.innerWidth - margin * 2);\n    tip.style.width = '';\n    tip.style.maxWidth = `${maxW}px`;\n    tip.style.top = `${slotR.bottom + 6}px`;\n    tip.style.transform = 'none';\n    const tipW = Math.min(maxW, Math.max(tip.offsetWidth || 180, slotR.width, 180));\n    let left = slotR.left + slotR.width / 2 - tipW / 2;\n    left = Math.max(margin, Math.min(left, window.innerWidth - margin - tipW));\n    tip.style.width = `${tipW}px`;\n    tip.style.left = `${left}px`;\n    const slotCenter = slotR.left + slotR.width / 2;\n    tip.style.setProperty('--tip-arrow-left', `${slotCenter - left}px`);\n  });\n}\n\ng.resetOwnedTip = function resetOwnedTip(slotEl) {\n  const tip = slotEl?.querySelector('.os-tip');\n  if (!tip) return;\n  tip.classList.remove('tip-fixed');\n  tip.style.width = '';\n  tip.style.maxWidth = '';\n  tip.style.top = '';\n  tip.style.left = '';\n  tip.style.right = '';\n  tip.style.transform = '';\n  tip.style.removeProperty('--tip-arrow-left');\n}\n\ng.bindOwnedTipAnchors = function bindOwnedTipAnchors() {\n  document.querySelectorAll('#shop-owned .owned-slot.filled').forEach((slot) => {\n    slot.onmouseenter = () => positionOwnedTip(slot);\n    slot.onmouseleave = () => resetOwnedTip(slot);\n    if (slot.classList.contains('tip-open')) positionOwnedTip(slot);\n  });\n}\n\n/** \ubcf4\uc720 \ud2b9\uc218\ud328 \uc124\uba85 \u2014 \ubaa8\ubc14\uc77c\u00b7\ud130\uce58\ub294 \ud31d\uc5c5, \ub370\uc2a4\ud06c\ud1b1\uc740 \uc778\ub77c\uc778 \ud301 */\ng.toggleOwnedTip = function toggleOwnedTip(i, ev) {\n  if (ev) ev.stopPropagation();\n  if (isTouchUI()) {\n    showJokerPop(i);\n    return;\n  }\n  const slots = document.querySelectorAll('#shop-owned .owned-slot.filled');\n  slots.forEach((el, idx) => {\n    if (idx === i) el.classList.toggle('tip-open');\n    else el.classList.remove('tip-open');\n  });\n  const cur = slots[i];\n  if (cur?.classList.contains('tip-open')) positionOwnedTip(cur);\n  else resetOwnedTip(cur);\n}\n\ng.applyBinjariPurchase = function applyBinjariPurchase() {\n  const n = state.jokers.length;\n  state.binjariMult = n * 10;\n  let refund = 0;\n  for (const j of state.jokers) refund += Math.floor(j.paid / 2);\n  addMoney(refund);\n  state.mitjangChips = 0;\n  state.geumjulLost = 0;\n  state.geumjulMult = 0;\n  state.gotaryeongGoes = 0;\n  state.gotaryeongMult = 0;\n  state.jokerGains = {};\n  state.jokers = [];\n  state.slotsLocked = true;\n}\n\ng.sellJoker = function sellJoker(i) {\n  if (state.screen !== 'shop') return;\n  const j = state.jokers[i];\n  if (!j) return;\n  addMoney(Math.floor(j.paid / 2));\n  if (j.id === 'mitjang') state.mitjangChips = 0;\n  if (j.id === 'geumjul') { state.geumjulLost = 0; state.geumjulMult = 0; }\n  if (j.id === 'gotaryeong') { state.gotaryeongGoes = 0; state.gotaryeongMult = 0; }\n  if (j.id === 'gameun_nun') state.gameunNuneMult = 0;\n  delete state.jokerGains[j.id];\n  state.jokers.splice(i, 1);\n  render();\n}\n\ng.reroll = function reroll() {\n  if (state.screen !== 'shop' || state.money < state.rerollCost) return;\n  state.money -= state.rerollCost;\n  trackEvent('shop_reroll', {\n    month: state.round,\n    cost: state.rerollCost,\n    money_after: state.money,\n  });\n  state.rerollCost++;\n  state.shopOffers = genOffers();\n  render();\n}\n\ng.pickBossCandidate = function pickBossCandidate(id) {\n  if (state.screen !== 'shop' || !state.bossCandidates) return;\n  state.nextBoss = id;\n  render();\n}\n\ng.nextRound = function nextRound() {\n  if (state.screen !== 'shop') return;\n  if (state.round >= ROUNDS) { victory(); return; }\n  if (isBossRound(state.round + 1) && !state.nextBoss) { flashActions(); return; }\n  if (state.coachStep === 'shop' || (state.coachStep && String(state.coachStep).startsWith('shop'))) {\n    state.coachStep = null;\n    markShopCoachSeen();\n  }\n  state.round++;\n  startRound();\n}\n\n";
new Function('g', 'with (g) {\n' + code + '\n}')(g);
