# 정책 정합성

**대상**: 현행 구현(v5) · Tier S → 전 영역 대조
**정책 대장 원본**: `00_concept.md §7` (POL-ID 정의)

> REVISE 모드이므로 대조 대상은 "새 기획"이 아니라 **구현 실물**이다.
> 따라서 이 문서는 두 종류를 찾는다: ①구현이 정책을 어긴 곳 ②**문서가 구현과 달라진 곳(드리프트)**.

---

## 1. 대조 결과 — 준수

| 구현 항목 | 관련 POL | 판정 | 확인 근거 |
|---|---|---|---|
| 단일 `index.html`, 외부 리소스 0 | POL-ARC-01 | 준수 | CDN·번들러 없음. Assets만 상대 경로 |
| 엔진 마커 구간에 DOM 접근 없음 | POL-ARC-02 | 준수 | 테스트가 `new Function`으로 추출 실행 성공 |
| 엔진 테스트 통과 | POL-ARC-03 | 준수 | `node test/test-game.mjs` ✅ 전체 통과 |
| 카드 선택이 uid 기반 | POL-ARC-04 | 준수 | `state.selected`가 uid 배열 |
| 족보·core 판정 단일 지점 | POL-ARC-05 | 준수 | `detectHandInfo` 외 판정 로직 없음 |
| 경제 트리거가 `playSelected`에만 | POL-ARC-06 | 준수 | 광팔이 지급이 `playSelected:3435` 1곳. 프리뷰는 `computeScore` 드라이런 |
| 연쇄 고가 `checkAfterPlay` 재진입 | POL-ARC-07 | 준수 | 재귀 없음. `chooseGo` → `dealToHand` → `checkAfterPlay` |
| 족보 기본칩 0, 코어 카드칩만 배수 | POL-SCR-01 | 준수 | `computeScore`에 기본칩 항 없음 |
| 칩 보정 순서 | POL-SCR-02 | 준수 | `cardChip` 구현 순서 = 정책 순서 (⚠️ 주석은 틀림 → §2 드리프트 #2) |
| `Math.floor` 최종 1회 | POL-SCR-03 | 준수 | `index.html:2855` 단 1곳 |
| core는 필요 장수만 | POL-SCR-04 | 준수 | `topByBaseChip(arr, 3)` — 초과분 flat |
| +칩 flat, +배수 → ×배수 순 | POL-SCR-05 | 준수 | `computeScore:2849-2851` 3단 루프 |
| 고 무제한 · 문턱 `ceil(base×(1+0.6n))` | POL-GO-01 | 준수 | `goThreshold` |
| 밀치기 소급, 목표 > 점수 보장 | POL-GO-02 | 준수 | 엔진 테스트 `[4]` 5케이스 검증 |
| 고 선언 1회당 내기 +1 | POL-GO-03 | 준수 | `chooseGo:3596` — 밀친 단계와 무관 |
| 판 시작 리셋 제외 6항목 | POL-RND-01 | 준수 | `startRound`가 money·jokers·mitjangChips·usedBosses·makgeolli·stats 미변경 |
| 안개는 새 카드만 뒷면 | POL-BOS-01 | 준수 | `applyAngaeFaceDown`이 `!c.faceDown`만 후보 |
| 밸런스 상향 우선 | POL-BAL-01 | 준수 | 목표 커브가 모델 대비 전 구간 +0~9% 상방 (`04_balance §2`) |
| 3월 벽 ~15% · 봇 승률 ~0% | POL-BAL-02 | 준수 | 실측 14% / 0% |
| 고 캡 없음 | POL-BAL-03 | 준수 | 상한 코드 없음 |
| 고스톱·전통 용어 네이밍 | POL-NAM-01 | 준수 | 특수패 22종 전부 전통 용어 (밑장빼기·쪽집게·싹쓸이…) |
| 시작 4냥 / 슬롯 5 / 오퍼 3 | POL-ECO-01 | 준수 | |
| 판매가 = `floor(paid/2)` | POL-ECO-02 | 준수 | `sellJoker:3757` |
| 리롤 2냥 시작 +1 | POL-ECO-03 | 준수 | |
| 이자 = 진입 시점 기준, 상한 5 | POL-ECO-04 | 준수 | `settle:3669` 주석에 명시 |
| 오퍼는 미보유·중복 없음 | POL-SHP-01 | 준수 | `genOffers:3705` |
| 박 런 내 중복 없음, 3월은 약한 박 | POL-BOS-02 | 준수 | `usedBosses` 필터 + `MILD_BOSSES` |
| 모달 비스택 | POL-UX-02 | 준수 | `state.screen` 단일값 |
| 연출 중 입력 잠금 | POL-UX-03 | 준수 | `juicing`/`dealing` + 세대 토큰 |
| localStorage는 튜토·설정만 | POL-DAT-01 | 준수 | 6키 전부 해당 |
| 시드는 URL로만 | POL-DAT-02 | 준수 | `?seed=` |

**준수 33건.** 핵심 HARD 정책(엔진 순수성·스코어링 순서·고 불변식)에 위반이 없다.

---

## 2. 대조 결과 — 문서 드리프트 (구현이 정답, 문서가 낡음)

> 정책 위반은 아니지만 **다음 작업자를 오도한다.** 문서를 구현에 맞춰야 한다.

| # | 항목 | 문서가 말하는 것 | 구현 실제 | 위치 |
|---|---|---|---|---|
| 1 | **춘향 조력자** | "준비/상점에서 3냥에 훈수 예약, 그 달 내내 패를 봐줌" | **전면 비활성** — `CHUNHYANG_ENABLED = false`. UI 숨김, 구매 함수 즉시 return | `README.md:29` · `docs/기획서.md §9` · `index.html:2887` |
| 2 | **밤낮 보정치** | 코드 주석 "광·열끗 **+3**칩 / 띠·피 +3칩" | **+2칩** | 주석 `index.html:2946` vs 구현 `cardChip:2817-2818` |
| 3 | **티어 확률 상수** | `RARITY_WEIGHT`가 확률표인 것처럼 선언 | **아무도 읽지 않는 죽은 상수.** 실제 추첨은 `rollJokerRarity`의 하드코딩 임계값 `0.55/0.83/0.96` | `index.html:2790` vs `2792-2798` |
| 4 | **`makgeolli` 필드** | — | `state.makgeolli`가 "레거시 미사용(호환용)"으로 남아 있고 `resetRun`·POL-RND-01에까지 등재 | `index.html:2918`, `3803` |
| 5 | **상점 상세** | 문서에 슬롯 수·리롤 비용·판매가 기재 없음 | 오퍼 3 / 리롤 2냥+1 / 판매 `floor(paid/2)` | `docs/기획서.md §7` |
| 6 | **시뮬 수치** | "300런 · 무조커 3월 ~14% · 상점봇 6~7월 벽" | **600런** · 무조커 3월 14% / 상점봇 6월 3% · 7월 0% | `README.md:44` · `HANDOFF.md` |
| 7 | **1월 튜토리얼** | 문서에 "Balatro식 스포트라이트 + 조합 오라 3회"만 | 실제로는 **6스텝 코치 + 손패 강제 조작 2종**(`ensureJanOpeningCombo`·`ensureJanJunkTutorialHand`) + hint 토글 | `docs/기획서.md §9` |
| 8 | **12월 고** | 명시 없음 | 12월은 목표 도달 즉시 승리 — **고/스톱 선택지가 나오지 않는다** | `checkAfterPlay:3571` |
| 9 | **개발 도구** | 문서에 `?goPreview=1`만 | `?cardTune=1`(카드 색조 튜너)도 존재 | `CLAUDE.md` |

**조치**: #1~#4는 코드/문서 어느 쪽을 고칠지가 결정 사항(→ §3 이탈 #1, §4 신설 제안 없음, 나머지는 단순 수정). #5~#9는 문서만 갱신하면 된다 — 이 기획서가 그 갱신본이다.

---

## 3. ⚠️ 정책 이탈 — 승인 필요 (3건)

### #1 · POL-CHR-01 / POL-CHR-02 (SOFT) — 춘향의 처우

| 항목 | 내용 |
|---|---|
| 기존 정책 | POL-CHR-01 "캐릭터·연출 요소 환영" · POL-CHR-02 "춘향은 한 상호작용 = 한 효과, 만능 조력자로 되돌리지 않음" |
| 현재 구현 | `CHUNHYANG_ENABLED = false` — **기능 전체가 꺼져 있고, 꺼진 채로 코드는 전부 남아 있다** (`chunhyangHint`·`chunhyangHoverPredict`·`renderChunhyang`·`buyChunhyangNext`·`#chunhyang` DOM·`makgeolli` 필드 등 약 200줄) |
| 이탈 사유 | 구 설계(만능 조력자)가 POL-CHR-02에 어긋나 껐으나, 새 설계(단일 킥)가 아직 없어 **미결 상태로 동결**됨 |
| 영향 범위 | 게임의 유일한 캐릭터 부재 → 킥(개성) 상실. 문서 3곳이 사실과 다름. 죽은 코드 200줄이 리팩터·리뷰 비용 |
| 정책 준수 대안 | **(a) 제거** — 코드·DOM·문서·`makgeolli` 전부 삭제. 되살릴 땐 git에서 복원<br>**(b) 단일 킥으로 재설계** — 막걸리(다음 패 1장 엿보기) 또는 소주(버리기 +1) 중 **하나만** 신규 기획<br>**(c) 현상 유지** — 플래그만 남기고 방치 |
| 권장 | **(a) 제거 + (b)를 별도 기획 단위로 분리.** 지금의 (c)는 "기획서가 거짓말하는 상태"를 유지하는 것이라 가장 나쁘다. 죽은 코드를 지우면 (b)를 백지에서 설계할 수 있고, `00_concept §8` 확장 후보에 이미 등재돼 있어 유실 위험도 없다 |
| 결정 | ☐ (a) 제거  ☐ (b) 재설계 착수  ☐ (c) 현상 유지  ☐ 보류 |

### #2 · POL-UX-01 (관습, 추론) — 예외를 비활성만으로 알린다

| 항목 | 내용 |
|---|---|
| 기존 정책 | (관습) 불가능한 액션은 버튼 비활성 + 흔들림으로 알린다. 팝업 없음 |
| 현재 구현 | 14종 예외 중 **해결 경로를 제시하는 것이 0건**, 원인을 문구로 설명하는 것이 1건(EXC-03의 `title`, 모바일 미표시) |
| 이탈 사유 | 정책 자체는 지켜지고 있으나, **되돌릴 수 없는 두 행동에 확인이 없다**: EXC-13(튜토 재생 → 진행 중 런 즉시 폐기) · EXC-14(특수패 판매 → 밑장빼기 누적 소멸) |
| 영향 범위 | EXC-13은 8월쯤 ✨ 오탭 시 20분 플레이가 증발. EXC-14는 슬롯 안 작은 버튼이라 오탭 확률이 높다 |
| 정책 준수 대안 | **(a) 파괴적 행동에 한해 확인 팝업 2건 추가** (POP-02·POP-03, `02_flow §3`) — 관습을 "파괴적 행동은 예외"로 좁힘<br>**(b) 팝업 없이 해결** — ✨을 상단바에서 빼 사이드 하단으로 이동 + 판매 버튼을 슬롯 탭 후에만 노출<br>**(c) 현상 유지** |
| 권장 | **(a)** — 파괴적 행동 확인은 업계 표준이고, 2건뿐이라 관습 전체를 흔들지 않는다. (b)는 오탭 확률만 낮출 뿐 되돌릴 수 없다는 사실은 그대로 |
| 결정 | ☐ (a) 팝업 2건  ☐ (b) 배치 변경  ☐ (c) 현상 유지  ☐ 보류 |

### #3 · POL-ARC-01 (HARD) — 단일 파일 · 밸런스 레버가 전부 코드

| 항목 | 내용 |
|---|---|
| 기존 정책 | 게임 전체는 단일 `index.html`. 외부 리소스·빌드·서버 금지 |
| 현재 구현 | 정책은 완벽히 준수. **그 결과** 붕괴 리스크 7개 중 6개의 대응 레버가 코드 안에 있고(`04_balance §9`), 콘텐츠 추가 리허설 3종이 전부 코드 편집을 요구한다(`03_data §6`) |
| 이탈 사유 | 이탈이 아니라 **정책의 부작용**. 정책을 어기지 않고 해결하고 싶다는 요청 |
| 영향 범위 | 밸런스 패치마다 재배포 · 족보/박 추가 시 2~5곳 동시 수정 · 로컬라이즈 불가 |
| 정책 준수 대안 | **(a) 인라인 JSON** — `index.html` 안에 `<script type="application/json" id="gamedata">` 블록을 두고 엔진이 파싱. **외부 파일 0, 빌드 0, POL-ARC-01·02 모두 유지**<br>**(b) 부분 적용** — `boss_effects`만 먼저 집약 (리허설 C가 가장 비쌌으므로)<br>**(c) 현상 유지** |
| 권장 | **(b) → (a) 순차.** 박 효과 7행 집약은 단독으로도 산재 버그를 없애고, 성공하면 같은 패턴을 특수패·족보로 확장할 근거가 된다. 한 번에 (a)를 하면 6,749줄을 흔들게 되어 POL-ARC-03(테스트) 부담이 크다 |
| 결정 | ☐ (a) 전면  ☐ (b) 박부터 단계 적용  ☐ (c) 현상 유지  ☐ 보류 |

---

## 4. 신설 제안

### POL-UX-04 (SOFT) — 1차 행동은 화면당 1개

| 항목 | 내용 |
|---|---|
| 정책 문장 | 한 화면의 1차 행동은 하나다. 둘로 보이면 크기·색·위치로 위계를 준다. **단 고/스톱은 명시적 예외** — 선택 자체가 콘텐츠다 |
| 필요 이유 | `SCR-PLAY-01`의 `[내기]`와 `[버리기]`가 같은 크기·같은 스타일로 나란히 있다. 1월 코치가 `junk` 단계에서 내기를 강제 비활성화해야 했던 것 자체가 혼동의 증거다(EXC-12) |
| 적용 범위 | 전 화면. 기존 화면 중 조정 대상은 `SCR-PLAY-01` 1곳 |
| 구체안 | `[내기]`를 accent 컬러 + 폭 1.4배로, `[버리기]`를 중립 컬러로. 기능 변경 없음 |
| 결정 | ☐ 신설  ☐ 반려  ☐ 보류 |

### POL-ECO-06 (SOFT) — 무한 Sink를 하나 이상 유지한다

| 항목 | 내용 |
|---|---|
| 정책 문장 | 냥에는 상한이 없으므로, 런 후반에 잉여율 σ가 25%를 넘지 않도록 **상한 없는 소모처를 최소 하나 유지**한다 |
| 필요 이유 | `04_balance §4` — 현행 σ = **+72~100%**. 특수패는 슬롯 5칸이 곧 지출 상한(최대 75냥)인데 Faucet은 월에 비례해 커진다. 유일한 무한 Sink인 리롤이 매 상점 2냥으로 리셋되어 흡수력이 없다 |
| 적용 범위 | 경제 전반. **정책 §6의 "새 Faucet/Sink 추가는 진행 전 확인" 대상** |
| 구체안 후보 | 슬롯 확장 구매(5→6→7, 가격 지수) / 리롤 비용 판 간 이월 / `00_concept §8`의 보너스피·족보책 |
| 유보 근거 | 봇 실측 6월 도달률 3% — **현재는 도달자가 거의 없는 잠재 문제**다. 밸런스 완화나 숙련자 증가 전까지는 우선순위를 낮게 둬도 된다 |
| 결정 | ☐ 신설  ☐ 반려  ☐ 보류 |

### POL-DAT-03 (관습 → 정식 승격 제안) — 확률은 정수 weight로 단일 정의

| 항목 | 내용 |
|---|---|
| 정책 문장 | 확률·가중치는 **정수 weight 한 곳에만** 정의하고, 누적 임계값은 코드가 파생한다 |
| 필요 이유 | 드리프트 #3 — `RARITY_WEIGHT`(소수)와 `rollJokerRarity`(누적 임계값)가 **이중 정의**되어 있고, 전자는 죽은 상수다. 한쪽만 고치면 조용히 어긋난다 |
| 적용 범위 | 특수패 티어 추첨. 향후 모든 확률 요소 |
| 결정 | ☐ 신설  ☐ 반려  ☐ 보류 |

---

## 5. 특수 케이스 확인 요청

정책 위반은 아니지만 `references/policy.md §6` 기준 **진행 전에 한 번 묻는 항목**이다.

| # | 항목 | 왜 묻는가 | 관련 |
|---|---|---|---|
| 1 | 새 Sink 추가 | 인플레 전파 — 냥 경제 전체의 앵커가 흔들린다 | 신설 POL-ECO-06 |
| 2 | 새 `effect_type`(코드 동사) | `03_data §3`에서 22종이 7개 동사로 전개됨을 확인했다. **8번째 동사가 필요해지면 그때 승인** | POL-ARC-02 |
| 3 | 밸런스 앵커 `curve_r` 변경 | 지배적 파라미터 (±20% → 결과 ±642%) | `04_balance §7-6` |
| 4 | 신규 아트 38장 생성 착수 | 크레딧·시간 소요. 대장 승인 후에만 | `05_art §5` |
| 5 | 12월에도 고를 허용할지 | 현재는 목표 도달 즉시 승리. 최고 점수 경쟁을 열지 말지는 게임 성격 결정 | EXC-05 `[확인필요]` |

---

## 6. 체크리스트

- [x] 정책 대장을 실제 파일에서 수집 (코드 상수 실측 포함)
- [x] 모든 정책에 근거 위치(파일:라인)
- [x] 관습 정책이 "추론"으로 라벨링 (`00_concept §7` 관습 절)
- [x] 구현을 한 줄씩 대조해 준수/드리프트/이탈로 분류 (준수 33 / 드리프트 9 / 이탈 3)
- [x] 모든 이탈에 사유·영향범위·대안·권장안
- [x] 이탈 요약이 `plan.md` 맨 앞에
- [x] HARD 정책 관련 항목(#3)에 특히 강한 근거 — 정책 유지 대안을 우선 제시
- [x] 신설 제안 3건을 별도 승인 항목으로 분리
- [x] 미승인 항목은 `[보류]` 처리 가능하도록 나머지 기획과 독립
