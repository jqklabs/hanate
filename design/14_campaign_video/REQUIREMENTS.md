# 캠페인 영상 — 요구사항 대장

> **이 문서는 매 요청마다 갱신한다.** 작업 전 반드시 읽고, 작업 후 상태를 고친다.
> 같은 지적을 두 번 받는 일이 없도록 하는 것이 이 문서의 목적이다.

---

## 0. 절대 규칙 (반복 지적된 것 — 어기면 안 됨)

| # | 규칙 | 상태 |
|---|---|---|
| R1 | **춘향은 우측 구석 배경.** 화면에 조금씩만 걸친다. 중앙에 박지 말 것 | ⚠ 반복 위반 |
| R2 | **UI는 인게임과 크게 다르지 않게.** 레이아웃을 통째로 엎지 말 것 | ⚠ 위반 중 |
| R3 | **모든 UI가 카메라 안에 있을 필요 없다.** 필요하면 카메라가 포커싱하면 됨 | ⚠ 오해했음 |
| R4 | **줌 인/아웃 반복 금지.** 들어갔다 나왔다 하지 말 것 | ✅ 배율 고정 |
| R5 | **가짜 프레임(보간)으로 60fps 만들지 말 것** | ✅ 슬로모션 촬영 |
| R6 | **화투패가 화면 중심** | ✅ |
| R7 | **손패는 항상 8장** (게임 규칙) | ✅ |
| R8 | **깜빡임 최소화** — 같은 백색 점멸 반복 금지 (광과민성) | ✅ 7→3회 |
| R9 | **장식 트랜지션 금지** (circleopen/slideup 등) | ✅ fade만 |
| R10 | **자막 없음** (이번 편집 한정) | ✅ |
| R11 | **출력은 4:5 네이티브** (구글 캠페인). 크롭 파생 금지 | ✅ |
| R12 | **원본 index.html 수정 금지** — 사본에만 주입 | ✅ |

---

## 1. 시나리오 「숫자가 커진다」

자막 없이 화면만으로 읽혀야 한다. 설명을 버리고 핵심 쾌감 하나만 반복·증폭한다.

**고른다 → 내리꽂는다 → 숫자가 터진다.** 같은 리듬 세 번, 규모만 커진다.

| 구간 | 소스 | 내용 | 체급 |
|---|---|---|---|
| H1 | Higgsfield | 오프닝 — 춘향이 패를 부채처럼 펴들고 정면 응시 | — |
| A1 | 녹화 | 1월 · 고도리(×6) · 조커 0 · 목표 160 → `+180` | 1.0 |
| A2 | 녹화 | 상점 — 특수패 2개 구매 | — |
| H2 | Higgsfield | 브릿지 — 특수패에 불이 붙는 클로즈업 | — |
| A3 | 녹화 | 5월 · 삼광(×5) · 조커 3 · 목표 600 → `+618` | 1.5 |
| A4 | 녹화 | 9월 · **오광(×12)** · 조커 5 · 목표 2,200 → 폭발 | 2.3 |
| H3 | Higgsfield | 리액션 — 오광에 놀란 춘향 | — |
| C4/C6 | OG 아트 | CTA — 금박 로고 + `몇 월까지 깰수있으세요?` | — |

**핵심 메시지:** 특수패를 맞추고 더 높은 족보를 낼수록 점수가 팡팡 터진다.

**미해결:** 사이클 3회가 구조적으로 단조롭다는 지적(Gemini 진단 1순위)은 아직 해소되지 않았다.

---

## 2. 요청 이력 (이번 세션)

### 초기 — 스토리보드
| 요청 | 결과 |
|---|---|
| 30~60초 스토리보드, 기존 리소스 사용 | v1~v5 생성 후 **전면 폐기** |
| 캐릭터 시트 기준 일관성 | 적용했으나 방향 전환으로 무의미해짐 |
| 8컷을 한 이미지로 | 완료 |
| 인게임 UI/리소스를 레퍼런스로 | 완료 — 게임 캡처를 생성에 물림 |
| 훅 3초가 중요 | 리서치 후 반영 |
| **"마케팅 효과 없을 듯, 잘된 게임릴스 구조를 베껴라"** | 리서치 → **생 플레이 녹화로 전면 전환** |

### 전환 — 녹화 파이프라인
| 요청 | 결과 |
|---|---|
| 코드로 구현해 자동 풀 녹화 | ✅ `run.mjs` 한 번으로 완료 |
| 원본 무수정 + 사본 생성 | ✅ |
| PC 뷰로 촬영 | ✅ |
| 60fps | ✅ (아래 R5 참고) |
| 4:5 네이티브 | ✅ 1440×1800 녹화 → 1080×1350 |
| 상점 씬 추가 | ✅ A2 |
| 족보 사다리 (삼광·고도리만 말고) | ✅ 고도리 → 삼광 → 오광 |
| 특수패 발동을 보여줄 것 | ✅ 조커 → 점수판 에너지 빔 |
| 카메라가 사건보다 선행 | ✅ 음수 오프셋 |
| 이펙트 체급을 점수에 비례 | ✅ power 1.0/1.5/2.3 |
| 깜빡임 줄이기 | ✅ 7→3회, glow로 대체 |
| 흔들림 줄이기 | ✅ 9→3회 |
| 자막 제거 | ✅ |
| 이펙트 중복 (고도리/삼광 두 번) | ✅ 게임 배너 숨기고 VFX 금박 글씨만 |
| 손패 8장 | ✅ |
| Higgsfield 슬롯 위치 확인 | ✅ 자리표시자 렌더 |
| 춘향 누끼 교체 | ✅ 파일 교체 |

### 반복 지적 — 아직 못 지킨 것
| 요청 | 횟수 | 현재 |
|---|---|---|
| **춘향을 우측 구석에 조금만** | 5회+ | ❌ 무대 확대로 다시 중앙 크게 |
| **UI를 인게임과 비슷하게** | 2회 | ❌ 레이아웃 통째로 재구성됨 |
| 카메라 안에 다 넣을 필요 없다 | 1회 | ❌ 다 넣으려다 UI를 엎음 |
| 점수 UI가 화면 밖이면 빔이 안 보임 | 1회 | ✅ 해결했으나 R2를 깨는 방식이었음 |

---

## 3. 파이프라인

```
node design/14_campaign_video/capture/run.mjs          # 빌드 → 녹화 → 합성
node design/14_campaign_video/capture/assemble.mjs --ratio 4x5   # 편집만
```

| 파일 | 역할 |
|---|---|
| `build-rig.mjs` | 원본 index.html 사본에 리그 주입 (원본 무수정) |
| `rig.js` | 게임 조작 API — 월·목표·손패·조커 지정, 캡처 CSS |
| `scenes.js` | 씬 정의 (A1~A4) + 마크 |
| `vfx.js` | 영상 전용 이펙트 |
| `record.mjs` | Playwright 녹화 (슬로모션 2.5배) |
| `assemble.mjs` | 컷 편집 · 카메라 · 합성 |
| `make-plates.mjs` | CTA 플레이트 (OG 아트) |
| `plates/H1.mp4` 등 | Higgsfield 삽입 — 넣으면 자동 반영 |

### 하드 학습 (다시 밟지 말 것)
- Playwright `recordVideo.size`는 `deviceScaleFactor` 무시 → 뷰포트와 크기를 일치시킬 것
- ffmpeg 표현식에서 `a+(b-a)*e*iw`는 `*iw`가 마지막 항에만 붙음 → **반드시 괄호**
- 강한 감속 이징을 전 컷에 걸면 컷 끝마다 카메라가 멈춰 정지 프레임 발생
- 슬로모션 캡처 시 `setTimeout`만 늦추면 게임 카운트업이 폭주 → `performance.now`·`Date.now`·`rAF` 전부 늦출 것
- `-ss`/`-t`는 **입력 옵션**으로. 출력 옵션이면 setpts 압축분만큼 길이가 어긋남
- SSRock 폰트는 583자 서브셋 — `몇`·`깰`·라틴 문자 없음

---

## 4. 남은 작업

1. **R1 복구** — 춘향을 우측 구석으로 (최우선)
2. **R2 복구** — UI를 인게임 레이아웃에 가깝게 되돌리고, 필요한 순간만 카메라로 포커싱
3. 사이클 3회 단조로움 해소 (시나리오 재구성)
4. 나머지 비율 렌더 (16:9 / 1:1 / 9:16)
5. SFX 믹싱 (`assets/SFX/`)
6. Higgsfield H1·H2·H3 실제 영상 제작

---

## 5. 근본 원인 확정 (2026-08-05)

**뷰포트가 게임 UI보다 훨씬 크다.** 1440×1800으로 녹화하는데 게임 UI는
상단 26%(y 0.01~0.26)만 사용한다 → 하단 74%가 빈 판.

그 빈 공간을 감추려고 카메라를 1.85배 확대해왔고, 그래서
좌측 UI·상단 조커바·상점 모달·팝업이 전부 잘렸다.
**확대는 원인이 아니라 증상이었다.**

### 해결 방향 (다음 작업의 전제)
**뷰포트를 게임 UI가 실제로 쓰는 크기로 줄인다.**
- 1440×1800 → 1440×약 700~760 (게임 콘텐츠 실측 높이)
- 그러면 게임이 뷰포트를 꽉 채우고 **카메라 확대가 0이 된다**
- 4:5 출력은 그 위/아래에 판 배경을 연장하거나, 4:5에 맞는 뷰포트를 직접 산출
- `zoom`으로 레이아웃을 키우는 방식은 실패했다(춘향·좌측 UI와 어긋남) — 쓰지 말 것

---

## 6. 미착수 요구사항 (2026-08-05 접수)

| # | 요구 | 비고 |
|---|---|---|
| 0 | **전체 게임뷰 기준으로 배치.** 쓸데없는 빈 공간 제거 | ✅ 뷰포트 860×1075 |
| 1 | 상단 특수패를 **카메라 안에 모두** 보이게 | ✅ 뷰포트 축소로 해결 |
| 2 | 카드를 내면 **카메라 중심을 낸 칸으로 옮기고**, 카드가 가는 방향으로 무빙 | ✅ slam 컷 `look: ['hand','played']` |
| 3 | 점수 합산 끝나면 팝업이 뜨고 합산. **팝업은 PC UI 베이스**, 몇 월인지 표시 | ✅ `sum` 마크 (아래 §7) |
| 4 | 「주막 등장」 글씨 **화면 꽉** + 카드 뷰에서 **연속성** 있게 (줌 튐 금지) | ✅ `[1.04 → 1.0]` 연속, fadewhite 제거 |
| 5 | **상점 UI가 잘림** → 카메라에 맞춘다. 모든 팝업은 카메라 기준 | ✅ 상점 컷 배율 1.0 (모달 폭 = 뷰포트의 96%) |
| 6 | 낼 패가 항상 왼쪽 5장 → **손패 여러 곳에 섞여 있다가** 내는 느낌 | ✅ `setHand` 순서 흩어놓음 |
| 7 | **병풍 → 오광 사이에 힉스필드 영상**으로 개연성. 시나리오 필요 | ❌ 새 슬롯 H4 미착수 |
| 8 | **고도리·병풍·오광 이펙트 이미지** 제작 후 글자 뒤에 화려하게 | ✅ 실제 카드 레퍼런스로 재생성 (§7) |
| 9 | 엔딩이 **부메랑(옆으로 갔다 원복)** 금지 → **화투 치듯 팡! 바닥에 내리쳐진 느낌**으로 쾅 | ✅ 한 방향 `brake` |

### 참고 — 폰트 제약
SSRock은 583자 서브셋. `견`·`몇`·`깰`·라틴 문자 없음.
글자를 새로 쓸 때 반드시 커버리지 검사 후 대체어 선택.
확인 명령: `capture/.venv/bin/python`으로 fontTools cmap 조회.


---

## 7. 2026-08-05 2차 수정 (완료)

지적 4건은 전부 **연출의 인과가 끊겨 있던 것**이었다.

| # | 지적 | 무엇을 고쳤나 |
|---|---|---|
| 1 | 점수 팝업이 합산 전에 떠 있다 | 팝업을 `tally`에서 **`sum` 마크(계산·조커 발동이 전부 끝난 뒤)로 이동**. 게임 DOM 폴링을 버리고 씬이 `from`/`gain`을 넘겨 팝업이 스스로 카운트업. `+N`이 수식에서 팝업으로 날아와 꽂힌다(`flyGain`). 좌측 `#side` 전체를 숨김 |
| 2 | 상점 구매에 인과가 없다 | `aim` 컷에서 상품으로 1.0→1.18 줌인 → `flyToJoker()`가 진열 카드를 복제해 **조커바로 퓽 날아가 박힌다** (타격 링·파편·슬롯 발광) → 1.18→1.0으로 빠지며 전 경로를 잡는다 |
| 3 | 엠블럼이 중앙에 안 맞고, 오광이 지어낸 카드 | 엠블럼·팝업 컷을 **배율 1.0 · look 'center'**로 고정(뷰포트 == 프레임). 오광·고도리 엠블럼은 **실제 카드 5장/3장을 레퍼런스로 붙여 GPT Image 재생성** (`make-emblems.sh`) |
| 4 | 오광 후 1고~10고를 빠르게 | `go` 마크 + `goStamp()` — 150ms에서 96ms로 빨라지며 10번 찍힌다. 고/스톱 모달은 `#modal-overlay.gostop-open`만 골라 숨김(상점 모달은 살림) |

같은 패스에서 잡은 영상미 결함:

- **오광 화이트아웃** — `numberBurst`의 섬광을 체급에 비례시켜 알파 0.98·900ms까지 갔다.
  → 세기와 무관하게 **알파 0.55 · 700ms 상한**. 규모는 링·광선·파편으로 낸다
- **엠블럼이 편집에서 통째로 빠짐** — 슬램 컷과 수식 컷 사이 1초에 나타났다 사라졌다.
  → `emblem` 마크를 새로 찍고 **전용 컷**을 줬다
- **글자가 엠블럼을 덮음** — 글자 y 30% / 엠블럼 y 50%로 세로 분리, 글자 크기 상한
  (`160 × min(power, 1.6)` — 오광에서 391px까지 커져 카드를 통째로 가렸다)
- **주막 컷의 흰 프레임** — `fadewhite` 전환이 순백 프레임을 한 장 만들었다(R8) → `fade`

### 하드 학습 추가
- `assemble.mjs`의 `one(rects, 'center')`가 null을 반환해 결국 `focus`로 흘러갔다.
  → **'center'는 명시적으로 CENTER를 돌려줘야** 오버레이 컷이 정중앙에 온다
- `playSelected()`는 `state.roundScore`를 **동기적으로** 더한다.
  "더하기 전 점수"는 `api.play()`가 호출 직전에 찍어둬야 한다
- `codex exec`의 `-i`는 **가변 인자**다. 프롬프트를 뒤에 그냥 붙이면 이미지 목록에 먹혀
  "No prompt provided via stdin"으로 조용히 실패한다 → **`--`로 끊을 것**


---

## 8. 2026-08-05 3차 수정 (완료)

| 지적 | 원인 | 수정 |
|---|---|---|
| **판이 왼쪽/오른쪽으로 치우친다** | ① `#side`를 `opacity:0`으로만 껐더니 286px 폭을 그대로 차지 ② 게임이 `min-width:861px`에서 우측 춘향 자리로 `#main{padding-right:200px}`을 주는데 춘향은 뺐고 여백만 남았다 → `#table`이 816 대신 638px로 쪼그라들어 손패 8장이 **두 줄로 접혔다** | `#side { display:none }`, `#main { padding-right: 22px }`. 손패 8장 한 줄 복구 |
| **엠블럼이 카드를 그대로 재현** | 레퍼런스를 "정확히 재현하라"고 지시했다 | 카드는 **모티프·화풍 레퍼런스로만** 쓰고 결과는 금박 원형 문장(crest). 프롬프트에 `NO rectangular playing cards` 명시. 고도리·병풍·오광 3종을 같은 화풍으로 통일 |
| **주막 등장·상점 구매에서 카메라가 흔들린다** | `discover()`의 `shake(14)`, `flyToJoker()`의 `shake(9)` | 둘 다 제거. 임팩트는 링·파편·슬롯 발광으로 |
| **화면 밖 여유 공간이 없다** | 뷰포트 == 게임 == 프레임이라 여백 0. 확대·팬하면 가장자리가 바로 잘렸다 | **무대(stage) 개념 도입** (아래) |

### 무대(stage)와 여백 — 구조 변경

```
녹화 뷰포트 1080×1350  ← 최종 프레임과 같은 크기
  └ 무대 860×1075      ← 게임은 여기에만 그린다 (rig.js --cap-stage)
     둘레 110px / 137px = 카메라 예비 공간 (게임 배경이 채운다)
```

- `record.mjs` VIEW = 1080×1350, `rig.js` `--cap-stage: 860px / --cap-stage-h: 1075px`
- `assemble.mjs` 기본 배율 `Z = 1080/860 ≈ 1.256` — 무대를 프레임에 꽉 채운다
- 이제 **확대하거나 팬해도 바깥에 진짜 화면이 있어 잘리지 않는다.**
  세 값(VIEW / --cap-stage / STAGE_W)은 **반드시 같이 고쳐야 한다**
- VFX 크기도 뷰포트가 아니라 무대 기준(`calc(var(--cap-stage) * .68)` 등).
  `vw`로 두면 여백까지 계산해 이펙트가 프레임보다 커진다
- 상점 모달은 `max-width:860`이라 무대 폭과 같아 가장자리가 프레임에 딱 붙었다
  → `#modal { max-width: calc(var(--cap-stage) - 96px) }`

### 하드 학습 추가
- `body { will-change: transform }`은 **fixed 자손의 컨테이닝 블록을 body로 만든다.**
  `#modal-overlay`가 뷰포트가 아니라 body 박스 기준이 돼 어긋났다 → 프레이밍 모드에서만 쓸 것
- 요소가 왜 안 채워지는지 모르겠으면 추측하지 말고 **Playwright로 실측**할 것.
  `padding-right:200px` 한 줄을 찾는 데 그게 제일 빨랐다


---

## 9. 2026-08-05 4차 수정 (완료)

| 지적 | 수정 |
|---|---|
| **엠블럼 그림체가 안 맞고, 엠블럼 따로 텍스트 따로** | 화투패 화풍(먹선·주홍·남색 담채)으로 재생성하고, 문양 링 **가운데를 빈 타원 명판**으로 비웠다. 족보 이름은 그 명판 안에 앉는다(같은 좌표·같은 박자) → 한 덩어리 도장으로 읽힌다 |
| **엠블럼 뒤에서 점수가 올라가 카운팅이 안 느껴진다** | `scoreShow`에서 **엠블럼이 완전히 사라질 때까지 기다린 뒤**(`embD + 220`) `tally`를 찍는다. 수식 카운팅이 가려지지 않는다 |
| **상점에서 무엇을 사는지 안 읽힌다 / 최상단 조커바로 날아간다** | ① `api.aim(i)`가 상품에 표식을 달고 카메라가 **배율 2.0으로 그 상품 하나만** 잡는다(이름·설명이 읽힌다) ② 날아가는 곳을 프레임 밖 조커바 → **상점 안 '보유 특수패' 칸**으로 바꿨다. 시선이 상점을 벗어나지 않는다 |
| **상점 카메라가 흔들린다** | 상점 컷을 **전부 고정 프레임**으로. 확대한 채 패닝하면 어두운 모달이 미끄러져 흔들림으로 읽히고, 구매 직후 `render()`가 표식을 지우면 주시점이 사라져 카메라가 모달 뒤 카드로 튀었다 |
| **카드가 놓일 때 팡팡팡** | `vfx.cardPops()` — 착지마다 카드 팝 + 링 + 파편(105ms 간격), 마지막에 큰 한 방(`slam`). 착지~슬램을 **한 컷**으로 이어 붙였다 |
| **고/스톱 화면이 없다** | 게임이 스스로 띄우는 고/스톱 화면을 `cap-gostop`으로 **원하는 박자에** 연다. 1사이클(고도리)·2사이클(병풍)은 **스톱**, 3사이클(오광)만 **고** → 대비로 산다. 1고→10고는 새 이펙트가 아니라 **게임 자체 캐스케이드(`playGoCascade`)** 를 그대로 돌린다 |
| **컷이 끊겨 이동한다** | 배율을 컷 사이에 **이어지게** 재설계 — `ZA`(넓게) → land에서 한 번만 `ZB`로 밀고 사이클 끝까지 유지. 되돌아가는 줌 없음. 전환 시간 0.08 → 0.16s |

### 안전장치 — 컷 겹침·구멍 자동 검사
`assemble.mjs`의 `checkOverlaps()`가 합성 전에 같은 녹화의 이웃 컷이
소스 시간축에서 겹치는지(같은 장면 두 번), 1.2초 이상 비는지 경고한다.
예전엔 이걸 눈으로 찾느라 여러 번 다시 뽑았다.

### 하드 학습 추가
- 게임의 상품 스타일은 `.shop-offers .offer {...}`로 **부모에 스코프**돼 있다.
  복제본을 이펙트 레이어로 옮기면 스타일이 통째로 날아가 배경 없는 맨 글자만 떠다닌다
  → 같은 클래스의 껍데기를 씌워 스코프를 되살릴 것
- 확대한 상태에서 `look`이 가리키는 요소가 사라지면 카메라가 엉뚱한 곳으로 튄다.
  DOM이 다시 그려지는 구간(구매·정산)의 컷은 **고정 프레임**으로 둘 것
- 현재 길이 **42.3초**. 30초 규격이 필요하면 A3(병풍) 사이클을 통째로 빼는 게 가장 깔끔하다


---

## 10. 2026-08-05 5차 수정 (완료)

| 지적 | 원인 | 수정 |
|---|---|---|
| **잔잔바리 무빙이 너무 많다 (특히 고/스톱)** | 카메라는 컷의 **시작·끝 실측 좌표를 잇는다.** 낸 패나 수식이 컷 도중 사라지면 그 좌표가 통째로 바뀌어 카메라가 저 혼자 스르륵 흘러갔다. 고/스톱 컷은 모달과 화면 하단 사이를 오갔다 | 컷에 **`still: true`** 옵션 신설 — 컷 내내 시작 좌표로 못 박는다. 의도적으로 움직이는 컷은 카드가 놓이는 `land` 하나뿐이고, 나머지 게임 컷 31개는 전부 고정 |
| **주막에서 카메라가 흘러내린다** | 카메라가 아니라 **배너 자신**의 keyframe이 1.12 → 1.00 → 1.03 → 1.08로 내내 스멀스멀 커지고 있었다 | 배너는 9% 지점에 들이치고 **88%까지 완전히 정지**. 카메라는 한 방향으로 줌 인 + 위로 올리며 강한 감속(`brake`) |
| **UI엔 7고인데 연출은 10고** | 연출 단계를 손으로 `10`이라 박았다. 실제 선언 단계는 `goLevelReached(2200, 10680)+1 = 7`이었다 | `api.goLevelNow()`가 게임과 **같은 식**으로 계산하고 캐스케이드는 그 값을 쓴다. 겸해서 A4 시작 점수를 6,200으로 올려 최종 15,700점 — 10고 문턱(2,200 × 7 = 15,400)을 실제로 넘긴다 |
| **점수 안 넘는데 스톱** | — | `goStop()`이 `score < target`이면 콘솔 경고. 현재 세 판 모두 초과(180/160, 614/600, 15,700/2,200) |
| **엠블럼 가운데 검은 원이 쓸데없다** | 명판을 비워두고 글자를 얹는 구조였다 | 엠블럼 **가운데를 완전히 투명하게** 뚫고 문양을 링 바깥으로만 배치(무게 중심을 덜어냄). 글자는 외곽 스트로크 + 3단 발광으로 어떤 그림 위에서도 읽히게 하고 크기도 키웠다 |

### 새 도구
- `cut.still` — 컷 좌표 고정 (위 참고)
- `cut.cam.pan: [[x0,y0],[x1,y1]]` — 실측 좌표에 얹는 의도적 이동량.
  "줌을 땡기면서 위로 올린다"처럼 **잡을 요소가 없는 연출**에 쓴다


---

## 11. 2026-08-05 6차 수정 (완료)

| 지적 | 수정 |
|---|---|
| **줌인이 뻑뻑하다** | 모든 의도적 줌을 `brake`(확 튀어나갔다 정지) → **`inout`**(부드럽게 붙었다 부드럽게 멈춤)으로. 주막·상점 전환 시간도 0.18~0.22 → 0.30s |
| **엠블럼이 화려한 맛이 없다** | 화투 붓 화풍은 유지하되 **밀도를 올렸다** — 두꺼운 링에 모티프를 겹쳐 채우고 금박 하이라이트, 뒤로 금빛 방사광, 구름·물결 장식 띠. 가운데는 여전히 완전히 뚫려 있다 |
| **엠블럼 글자가 안 읽힌다** | 뚫린 가운데로 낸 패가 비쳐 글자와 뒤엉켰다 → 엠블럼 뒤에 **원형 딤**을 깔아 가운데를 가라앉히고, 글자를 구멍 폭에 꽉 채웠다 |
| **엠블럼·고/스톱에서 카메라가 움직인다** | 카메라는 이미 고정이었다. 움직인 건 **오버레이 자신의 keyframe** — 엠블럼이 1.02 → 1.12로, 글자가 1.03 → 1.12로 끝까지 계속 커지고 있었다. 들이친 뒤 **88~90%까지 완전 정지**하도록 고쳤다 |
| **카드를 낸 뒤 카메라가 내기 전 자리로 돌아온다** | **앵커 개념 도입.** 카드가 놓인 순간 그 자리를 한 번 재서 얼려두고(`api.anchor()`), 그 판의 남은 컷 21개가 전부 이 앵커를 본다. 낸 패가 화면에서 사라져도 카메라는 제자리다. 엠블럼·점수 팝업도 같은 y(`--cap-focus-y`)에 떠서 프레임 정중앙에 온다 |
| **점수판이 두 번 빛난다** | `numberBurst`(섬광·광선·링)와 `boom`(글로우·광선·링)이 1.4초 간격으로 연달아 터지고 있었다 → **`boom` 제거.** 합산이 끝나는 마지막 순간에 `numberBurst` 한 방만 |
| **구매 인터랙션이 약하다** | 착지에 링 2겹 + 방사광선 + 파편 24 + 슬롯 발광(밝기 2.1×) + 화면 글로우 + `＋1` 표시 |
| **UI엔 10고인데 실제 11고** | 선언 단계는 '**아직 못 넘은** 첫 문턱'이다. 15,700점은 10고 문턱(15,400)을 이미 넘어서 11고로 선언됐다 → 시작 점수 6,200 → **5,000**(최종 14,500). 9고 문턱 14,080은 넘고 10고 문턱 15,400은 안 넘어 정확히 **10고** |

### 새 도구
- `api.anchor()` / `--cap-focus-y` — 낸 패 자리를 얼려 카메라와 오버레이가 같은 곳을 보게 한다
- `cut.still`, `cut.cam.pan` (§10 참고)

### 하드 학습 추가
- "카메라가 움직인다"는 지적의 절반은 **카메라가 아니라 오버레이 CSS keyframe**이었다.
  정지해야 할 연출은 keyframe 끝까지 같은 transform 값을 유지시킬 것
- 고 단계는 `goLevelReached(...)+1`이라 **문턱을 넘기면 한 단계 더 올라간다.**
  N고를 띄우려면 점수가 `[threshold(N-1), threshold(N))` 구간에 있어야 한다


---

## 12. 2026-08-06 7차 수정 (완료)

| 지적 | 수정 |
|---|---|
| **카드를 더 크게, 화면 절반은 카드로** | 손패 카드 80 → **96px**(8장 한 줄이 무대 폭 816px에 들어가는 상한), 낸 패 카드 → **150px**. 낸 패 영역 여백도 300 → 250px. 카드가 차지하는 세로가 240 → 약 400px |
| **고/스톱 UI가 거추장스럽다** | 인게임 선택 모달을 아예 띄우지 않는다. `vfx.callout()`으로 **「스톱!」 「10고!」 한 마디**만 화면 가득 외친다 |
| **주막에 카메라 무빙 없이, 이어지듯** | discover 컷의 줌·팬을 **전부 제거**하고 앞 컷이 끝난 배율(ZB) 그대로 받는다. 앞뒤 배율이 같아야 "씬이 바뀐 게 아니라 이어진 것"으로 읽힌다. 전환도 0.34s로 길게 물렸다 |
| **엠블럼 가운데를 비우지 말 것** | 가운데까지 꽉 찬 **원형 메달**로 재생성. 정중앙만 짙은 먹 면이라 그 위의 족보 글자가 그대로 읽힌다 |
| **엠블럼이 사라지기도 전에 점수 계산이 끝나 있다** | ← **진짜 원인을 찾았다.** 게임은 `playSelected()` 직후 `playJuiceSequence()`를 **제 타임라인으로** 돌린다. 씬이 아무리 기다려도 엠블럼 뒤에서 숫자가 이미 다 올라가 있었고, 씬의 `mark('tally')`는 카메라 신호일 뿐 실제 카운팅과 아무 상관이 없었다 → **집계 게이트** 도입 |
| **엠블럼이 곱하기 값으로 들어가는 이펙트** | `vfx.absorb()` — 엠블럼과 족보 글자가 `#jt-mult`(×배수) 자리로 빨려 들어가 거기를 때린다(타격 링·파편·펄스). 족보가 곧 배수라는 인과가 눈에 보인다 |
| **특수패 구매 연출** | 상품으로 부드럽게 줌 인 → 카드가 금빛을 두르고 떠올라 → 상점 안 보유 칸으로 포물선 비행 → 링 2겹·방사광선·파편 24·슬롯 발광·`＋1` |

### 집계 게이트 (`rig.js`)
게임 시퀀스 안에서 멈출 수 있는 자리는 하나뿐이다 — 카드가 다 착지하고
족보 배너를 띄우는 지점. `sfxBanner()`가 정확히 거기서 한 번 불리므로 그것을 신호로
바로 다음 `juiceWait()`에서 붙잡는다.

```
api.holdTally()  →  api.play()  →  착지 팡팡팡 · 슬램 · 엠블럼 · 흡수
                 →  api.releaseTally()  →  칩×배수 카운팅 시작
```

### 하드 학습 추가
- **씬이 기다린다고 게임이 기다리는 게 아니다.** 게임의 연출 함수는 제 타임라인으로
  돈다 — 순서를 바꾸려면 그 함수 자체를 붙잡아야 한다.
  `mark()`는 카메라 신호일 뿐 게임 상태를 지연시키지 않는다
- 게임의 top-level `function foo(){}`는 전역 객체 속성이므로
  `window.foo = wrapper`로 감싸면 게임 내부의 `foo()` 호출도 wrapper를 탄다


---

## 13. 2026-08-06 8차 수정 (완료)

| 지적 | 수정 |
|---|---|
| **손패 8장·낸 패는 무조건 한 줄** | `flex-wrap: nowrap` 강제 + 카드 폭을 무대 폭 안에 맞춤(손패 92px × 8 + gap 5 = 771 < 816, 낸 패 148px × 5 + gap 9 = 776 < 816). `flex: 0 0 auto`로 줄어들지도 않게 |
| **흡수 이펙트가 왜 0으로 가나** | 이펙트는 `#jt-mult`로 갔지만 **엠블럼이 수식을 덮고 있어** 배수가 안 보였고, 엠블럼이 사라진 뒤엔 칩 `0`만 남아 있었다 → 흡수 시작과 동시에 `cap-premult`로 **수식에서 ×배수만** 남기고(칩·＝·점수 숨김) 2.5배로 키워 발광. 엠블럼이 줄어들면서 그 자리에서 `×6`이 드러나고, 도착과 함께 얻어맞은 뒤 한 번 더 튄다 |
| **로고를 크게 줌 → 뚝 떨어지며 쾅** | C6 배율 1.60 → **2.35**, 위(-10%)에서 아래로 떨어지며 강한 감속. 착지하는 **0.62초 지점에만** 짧게 흔들림(`fx: { shake: 13, at: 0.62 }`) |
| **주변 UI가 미세하게 바뀌고 카메라 잔무빙이 남는다** | 카드가 빠지고 보충될 때마다 영역 높이가 변해 판 전체가 위아래로 밀리고 있었다 → `#playedarea` 262px, `#handarea` 158px, `#actions` 74px **고정** |

### 하드 학습 추가
- **`animation: none`을 걸면 요소가 기본 스타일로 돌아간다.**
  `.vfx-emblem`의 기본은 `opacity: 0`이라, 흡수를 시작하는 순간 빨려 들어가는 게 아니라
  통째로 사라져 버렸다 → 현재 상태를 인라인으로 못 박고 **다음 프레임에** 목표를 줄 것
- `impact()`의 흔들림은 `crop`이라 프레임이 작아진다.
  **반드시 `scale`로 되돌릴 것** — 안 그러면 그 컷만 1050×1320이 되어 xfade가 크기 불일치로 죽는다
- 오버레이가 아래 UI를 덮고 있으면 그 UI를 강조해도 안 보인다.
  강조 대상이 가려져 있지 않은지 먼저 확인할 것


---

## 14. 2026-08-06 9차 수정 (완료)

| # | 지적 | 수정 |
|---|---|---|
| 1 | 점수 팝업이 카드를 가린다 | 팝업 자리를 **낸 패 아래 빈 공간**으로 옮겼다(`--cap-pop-y`). 리그가 앵커를 얼릴 때 낸 패 아래끝과 손패 위끝의 중간을 계산해 준다. 팝업 자체도 조금 줄여 그 틈에 딱 들어간다 |
| 2 | 패를 내는 장면이 이어지지 않고 화면 교차로 바뀐다 | `select` 컷과 `land` 컷 사이에 0.43초가 빠져 있었고(카드가 날아오르는 앞부분) 그 자리에 크로스페이드가 들어갔다 → **고른다 → 낸다 → 착지 팡팡팡 → 슬램을 한 컷으로** 합쳤다. 컷 길이는 `to: ['emblem', -0.12]`로 다음 마크까지 자동으로 채운다 |
| 3 | 상점 애니메이션이 별로다 | 포물선 비행을 버리고 **직선으로 쏜다**(둥실 떠다니는 느낌 제거). 뒤에 잔상 3장을 깔아 속도를 보여준다 |
| 4 | 10고가 두 번 뜬다 | 「10고!」 외침을 없앴다. **점수 게이지가 차오르는 그 자리 위에** 1고 2고 3고 …가 170 → 90ms로 빨라지며 박힌다(`vfx.goRun`). 인게임 캐스케이드는 중복이라 제거 |
| 5 | 가운데가 죽은 공간 | `#table`을 `space-between`으로 펼쳐 낸 패는 위, 손패·버튼은 아래로 벌렸다. 그 사이에 생긴 자리가 점수 팝업 자리다 |
| 6 | 글씨가 작다 | 엠블럼 글자 상한 0.16 → **0.21 stage**, 외침 0.30 → 0.34, 점수 팝업 숫자·월 표기도 키웠다 |
| 7 | 주막 엠블럼에 서양풍이 섞였다 | 초가 차양·옹기 술항아리·놋그릇·매듭 술띠·한국식 구름/물결로 **한국 민화 화풍**으로 재생성 |
| 8 | 로고가 휘어서 떨어진다 | ← **카메라 수식 버그였다.** 아래 참고 |

### 카메라 주시점 보간 버그 (전 컷에 영향)
주시점을 '크롭 창 좌상단 비율'로 **미리 환산해 두고 그 값을 이었다.**
이 환산 `(c - 1/2z) / (1 - 1/z)`는 배율에 따라 분모가 달라지는 **비선형** 변환이라,
배율이 변하는 컷에서는 같은 중심(0.5)이 배율마다 다른 비율이 된다
(z=2.35에서 0.5, z=1에서 0) → 그 사이를 직선 보간하니 화면이 옆으로 휘며 흘렀다.

→ **중심 좌표를 그대로 보간하고 크롭 좌상단은 매 프레임 계산한다.**
```
x = max(0, min(iw-iw/zoom, iw*cx - iw/zoom/2))
```
로고뿐 아니라 배율이 변하는 모든 컷의 미세한 좌우 흐름이 같이 사라졌다.

### 새 도구
- `cut.to: ['마크', 오프셋]` — 컷을 그 마크까지 채운다.
  `use`를 손으로 적으면 씬 타이밍이 조금만 바뀌어도 겹치거나 구멍이 난다

### 하드 학습 추가
- 카드 크기는 **무대 폭이 아니라 '카메라가 실제로 잡는 폭'** 기준으로 잡을 것.
  배율 ZB에서 프레임에 들어오는 건 무대의 일부뿐이라 무대에 맞춰 잡으면 좌우가 잘린다
- `.card`는 테두리가 폭에 더해진다(실측 148 → 161). `box-sizing`을 주고도
  테두리 몫을 따로 빼둘 것


---

## 15. 2026-08-06 10차 수정 (완료)

| # | 지적 | 수정 |
|---|---|---|
| 1 | 카드 뽑고 늦게 나간다 | 고른 뒤 대기 1,000ms → **420ms**. 고르자마자 낸다 |
| 2 | 점수판이 카메라 중앙이 아니다 | 팝업이 뜨는 컷(`sum`·`burst`·`go`)의 주시점을 앵커(낸 패)에서 **`pop`(팝업 자리)** 으로 바꿨다. 리그가 팝업 좌표를 실측해 넘긴다 |
| 3·7 | 카드를 따라 카메라가 빨라야 / 무엇에 포커스 | 출발점을 손패 전체가 아니라 **`sel`(지금 고른 패)** 로 잡고, 도착은 낸 칸. 이징을 `inout` → **`dash`**(6제곱, 32%에서 90% 도달)로 바꿔 카드와 같이 도착한다 |
| 4 | 여전히 점수를 가린다 | ← 원인 두 가지. ① 낸 패 영역 높이가 카드 높이밖에 안 돼 **수식이 카드 위로 겹쳤다**(250 → 312px) ② 엠블럼 딤이 흡수 내내 화면을 덮고 있어 배수가 묻혔다(아래) |
| 5 | 상점 구매 연출이 구리다 | **세 박자로 재구성.** ① 진열대에서 뽑혀 화면 한가운데로 1.85배 떠오르고(무엇을 샀는지 읽힌다) ② 금빛 링·파편과 함께 잠깐 멈추고 ③ 보유 칸으로 빨려 들어가 박힌다 |
| 6 | 춘향 컷 원복 / 로고가 느리고 다 떨어지기 전에 흔들린다 | C4 배율 1.70 → **1.50 원복**. 로고는 `dash`로 0.5초 안에 착지하고 흔들림 시각을 0.62 → **0.34초**(착지 순간)로 당겼다. 끝 전환의 순백 프레임(`fadewhite`)도 제거 |

### CSS 애니메이션 vs 인라인 스타일
`.vfx-emblem-dim`을 인라인 `opacity = 0`으로 껐다고 생각했지만,
**CSS 애니메이션이 도는 동안은 애니메이션 값이 인라인 선언을 이긴다.**
그래서 흡수 내내 화면 전체가 어두운 채였고 `×배수`가 묻혀 "0에 이펙트가 꽂힌다"로 보였다.
→ `animation: none` + 현재 값 고정 + 다음 프레임에 transition. (§13의 엠블럼 건과 같은 함정)

### 디버깅 노트
"안 보인다"를 눈으로만 쫓지 말 것. Playwright로 해당 순간의 computed style을 찍어 보니
`#jt-mult`는 opacity 1 · 60px · scale 2.5로 **정상 렌더 중이었다.**
문제는 그 위를 덮은 딤이었다 — 요소가 아니라 **가리는 것**을 의심했어야 했다


---

## 16. 2026-08-06 11차 — 수식이 안 보이던 이유

**증상**: 「칩 × 배수 = 점수」 수식이 화면에 거의 안 뜨고, 뜨더라도 다 끝난 뒤에
잠깐 스치듯 지나갔다.

**원인**: 집계 게이트를 풀어도 수식이 **바로 뜨지 않는다.**
게임은 release 뒤에 이 순서를 밟는다.

```
release → 배너 대기 340ms(큰 족보는 480ms) → 배너 정리 160ms
        → 수식 opacity 0 → 1 페이드 280ms
```

즉 **release + 약 0.8초**에야 보이기 시작한다.
그런데 씬은 release 직후 180ms에 `mark('tally')`를 찍고 있었고,
그 마크 기준 1.1초짜리 컷은 **전 구간이 opacity 0인 빈 화면**이었다.

**수정**
- `releaseTally()` 뒤 대기 180 → **860ms**. 수식이 실제로 보이는 시점에 마크를 찍는다
- 수식 컷 길이를 `to: ['sum'|'joker', …]`로 바꿔 **칩이 다 올라갈 때까지** 유지
- 칩 색이 인게임 기본(`--chip-blue`)이라 판 위에서 가라앉았다 → 밝은 하늘색 + 발광
- 카드와 수식 사이 간격 22px로 벌려 수식이 카드에 붙어 읽히게

결과: `10 → 20 → 30 → 30 × 6 → 30 × 6 = 136 → 180`이 낸 패 바로 아래에서
챡챡챡 올라간다.

### 하드 학습 추가
- 게임 연출 함수를 게이트로 붙잡았다고 끝이 아니다.
  **풀어준 뒤 그 함수가 몇 초 뒤에 무엇을 보여주는지**까지 재서 마크를 찍을 것


---

## 17. 2026-08-06 12차 — 콤보 몽타주로 개편

**의도**: 「점수 카운팅 사이클 × 3」은 같은 리듬의 반복이라 첫 폭발 뒤 몰입이 떨어진다
(§1의 미해결 항목). **다양한 조합을 빠르게 내리꽂는 몽타주**로 바꿨다.

### 구성 변경
| 씬 | 이전 | 지금 |
|---|---|---|
| A1 | 고도리 (조커 0) | **홍단** + 조커 「단골」 1개 |
| A3 | 병풍 사이클 | **몽타주**: 병풍 → 고도리 → 총통 → 오광 |
| A4 | 오광 사이클 | 삭제 (A3에 통합) |

- 앞의 세 조합은 **점수를 세지 않는다.** 조합만 빠르게 꽂고 엠블럼이 떴다 사라진다
  (조합당 약 2.4초). 매번 수식을 돌리면 같은 리듬이 네 번 반복돼 늘어진다
- 조합이 꽂힐 때마다 **특수패바와 낸 패 사이**에서 고가 뛴다: 1고 → 5고 → 10고
- 마지막 오광만 풀코스(엠블럼 → 배수 흡수 → 수식 챡챡챡 → 조커 → 합산 → 폭발)
  + **20고!**
- 카메라는 **첫 조합이 놓인 자리에 못 박고** 끝까지 움직이지 않는다.
  조합마다 재포커싱하면 몽타주가 아니라 컷 모음으로 읽힌다

### 카드 겹침 0 (조합끼리 한 장도 안 겹친다)
| 조합 | 카드 |
|---|---|
| 오광 | 1·3·8·11·12월 **광** |
| 고도리 | 2·4·8월 **열끗** |
| 총통 | **10월 4장** (열끗·청단·피×2) |
| 병풍 | 5·6·7·8·9월 — 5열·6띠·7열·**8피**·9열 |

8월이 세 조합에 걸치므로 광(오광) / 열끗(고도리) / 피(병풍)로 나눠 썼다.
병풍은 `detectHand` 우선순위상 단(3장)·삼광이 먼저 잡히면 안 되므로
청단은 6월 하나만, 광은 0장으로 구성한다.

### 숫자 정합
- A1 홍단: 칩 24 × 배수 11(=4 + 단골 7) = **264 / 160** ✓
  (맨몸 홍단은 96점이라 160을 못 넘는다 → 조커 하나가 필요했다)
- A3 최종: 시작 19,000 + 네 조합 9,958 = **28,958 / 2,200**.
  20고 문턱 2,200 × 13 = 28,600은 넘고 21고 문턱 29,920은 안 넘는다 ✓

### 새 도구 (`rig.js`)
- `skipJuice()` — 집계 시퀀스 통째로 건너뛴다.
  **주의**: `playJuiceSequence`가 시작할 때 `JUICE.skip`을 false로 되돌리므로
  시퀀스가 시작된 뒤(`JUICE.running`)에 켜야 한다
- `resetPlay(plays)` — 목표를 넘기면 게임이 `screen`을 'gostop'으로 바꿔
  다음 `playSelected`가 막힌다. 조합 사이마다 되돌린다
- `mark(name, info)` — 마크에 점수·목표를 실어 녹화 뒤 매니페스트로 검증
- `gobar` 좌표 / `--cap-go-y` — 특수패바와 낸 패 사이의 빈 띠

### 새 엠블럼
`fx_hongdan.png`(홍단 리본 3줄 + 솔·매화·벚꽃), `fx_chongtong.png`(단풍 속 사슴).
기존 5종과 같은 화풍·같은 프롬프트 골격.


---

## 18. 2026-08-06 13차

| 지적 | 수정 |
|---|---|
| **1고→5고→10고→20고 체이스 효과가 사라졌다** | 직접 만든 스탬프(`goJump`)를 버리고 **게임 자체 연출**(`runGoShatterCascade`)을 그대로 돌린다. `api.goChase(from, to)`가 사이 숫자를 전부 훑으므로 5→10이면 6·7·8·9·10이 파편을 튀기며 밀려 올라간다. 병풍 0→1, 고도리 1→5, 총통 5→10, 오광 10→20 |
| **수식이 낸 패 옆에 나오고 모양이 안 예쁘다** | 수식을 **결과 패널**로 만들었다 — 금테 + 어두운 판 + 넉넉한 패딩, `align-items: center`로 숫자 정렬, 카드와 26px 띄움. `70 × 135 + 50 = 9,352`가 낸 패 바로 아래 하나의 판으로 읽힌다 |
| **주막에서 이펙트가 겹친다** | 원인 둘. ① 배너 위에 광선(1500px 원뿔) + 파편 26개를 겹쳐 초가·항아리·글씨가 다 뒤엉켰다 → **글로우만 옅게** 남기고 제거 ② 앞 컷의 「스톱!」이 아직 떠 있는데 크로스페이드(0.34s)로 「주막 등장!」이 들어와 금박 글씨 두 개가 겹쳤다 → 외침이 사라질 때까지 컷을 잡고(use 1.35→1.60) 겹침도 0.18s로 |
| **오광 카메라 포커싱이 이상하다 / 20고는 빈 곳에서** | 몽타주는 **끝까지 카메라 고정**이 원칙인데 `sum`·`burst` 컷만 팝업(`pop`)을 보느라 살짝 내려갔다 → 전부 `anchor`로 통일. 20고는 인게임 체이스가 화면 중앙에서 처리하고, 오버레이를 앵커 높이로 옮겨(`--cap-focus-y`) 카메라를 안 움직이고도 프레임 정중앙에 온다 |

현재 길이 **49.7초**. 30초 규격이 필요하면 몽타주에서 한 조합(총통)을 빼는 게 가장 깔끔하다.


---

## 19. 2026-08-06 14차 — 타짜 패러디 + 도파민 개편

Gemini(agy)로 완성본과 타짜 원본 쇼츠를 분석해 받은 진단을 반영했다.

### 치명적 버그
- **홍단·총통 엠블럼이 통째로 안 나오고 엑박이 떴다.**
  `build-rig.mjs`의 심링크 목록에 `fx_hongdan.png`·`fx_chongtong.png`를 안 넣어
  사본에서 404가 났다 → **엠블럼을 새로 만들면 이 목록에도 반드시 추가**한다
- **고 체이스가 빈 판 위에서 놀았다.** 정산 뒤라 낸 패가 치워진 상태였다 →
  `goChase`가 `#playedarea`에 카드가 없으면 결과 화면을 다시 그린 뒤 체이스를 돌린다

### 연출
| 항목 | 내용 |
|---|---|
| **콜드 오픈** | 첫 0.9초에 20고 폭발(A3 burst 재사용)을 먼저 박는다. 릴스는 0~1초에 안 걸리면 넘긴다 |
| **타짜 패러디** | H1 「방자를 아냐고요? / 내가 아는 타짜 중 최고였어요」, H2 「손은 눈보다 빠르다」, H3 「묻고 더블로 가!」. 시나리오·프롬프트는 `higgsfield-slots.md` |
| **로고 Z축 낙하** | `pan` 제거 — pan이 있으면 화면 안에서 미끄러지는 Y축 이동으로 읽힌다. 순수 스케일 3.2 → 1.0 + 착지(0.30s)에 흔들림·플래시 |
| **타격의 문법** | 합산 착지 프레임에 shake, 폭발 컷에만 `GRADE_HOT`(채도·감마 상향)으로 화면이 금빛으로 달아오른다. 다른 컷은 기존 GRADE — **대비가 핵심** |
| **시선 한 줄** | 체이스 동안 조커바·손패를 `cap-gochase`로 눌러 숫자만 남긴다 |
| **템포** | 몽타주 전환 0.16 → **0.10s**(FTR), 수식 컷 `speed: 1.35`, 상점·정적 대기 절삭 |
| **잭팟** | `numberBurst` 파편 26 → 40 + 링 3겹 + 지연 2차 파편 |

### 새 도구
- `cut.line` / `plateLine()` — 플레이트 위 대사 자막. 줄마다 따로 그리고 두 번째 줄은
  0.55s 늦게 뜬다. SSRock에 `몇`·`깰`이 없어 AppleSDGothicNeo를 쓴다
- `cut.hot` — 그 컷만 `GRADE_HOT`
- `cut.coldOpen` — 시간 역순 컷이라 겹침 검사에서 제외
- `impact()`의 `at`·`flash` — 착지 시각에 맞춘 흔들림·플래시

### 하드 학습 추가
- **`to:`는 소스 구간을 채우는 것이다.** 배속(`speed`)이 걸린 컷은
  `(to - at) / speed`로 나눠야 그 구간이 정확히 끝난다. 안 나누면 뒤 컷을 물어버린다
- 생성 영상에는 글자를 넣지 않는다(`no text, no subtitles`). 자막은 합성 단계에서 —
  폰트·위치·타이밍을 재생성 없이 고칠 수 있다

### 힉스필드 시안
`capture/plates/drafts/` — 스틸 4장(1k) → Kling 3.0 Turbo 720p 5s 무음 4개 →
슬롯별 절취본 6개(`H1-a/b`, `H2-a/b`, `H3-a/b`). 기본값으로 a안을 `plates/`에 넣어뒀다.
고화질 재생성은 사용자가 고른 뒤에.


### 14차 QA 반영 (프레임 실측 기반)

Gemini는 개인 쿼터 소진(약 6일 리셋)으로 못 돌렸다 → 서브에이전트가 ffmpeg로
프레임을 직접 뽑아 검수했다. 거기서 나온 것 중 실제로 고친 것:

| 지적 | 수정 |
|---|---|
| 콜드오픈이 정적인 점수판이라 훅이 안 됨 | `burst +0.35` → **`+0.50`**(numberBurst가 터지는 시점) + `hot` 그레이딩. 폭발 → 11고까지 이어져 "이게 뭔데?"가 걸린다 |
| 절정 직후 「카드를 고르고 내기를 누르세요」 안내 문구 노출 | 캡처 CSS로 `#playedarea .hint` 숨김 |
| 고 체이스 구간이 "빈 어두운 판"으로 보임 | `cap-gochase` 딤을 0.42 → **0.58**(판은 0.72 → 0.86)로 완화 |
| 조합 결과(족보·칩×배·+점수)가 잔글씨 | `#playresult`를 영상용으로 확대(족보 40px / 수식 26px / 점수 46px). 체이스 중에는 숨겨 고 숫자와 안 싸우게 |
| 상점 리빌에서 글자가 이중 인쇄 | 복제본이 원본 위를 지나가고 있었다 → **원본 상품을 숨긴 뒤** 날린다. 자리가 비어야 '뽑혀 나왔다'로 읽힌다 |
| 20고 뒤 빈 판 0.9초 | `go` 컷 4.00 → 3.30s |
| 고 숫자와 점수 팝업이 같은 자리에서 겹침 | 체이스 시작에 `.vfx-scorepop`을 걷어낸다 (금박 오버레이는 항상 하나) |

검수에서 **이미 좋다고 확인된 것**(건드리지 않음): 오광 풀 스코어 시퀀스,
엠블럼 아트 5종, A1 수식 정합(24×11=264 / 160), H2·C4·C6 마감.


---

## 20. 2026-08-06 15차 — 겹침 제거 · 그림체 유지 · 패러디 템포

Gemini는 개인 쿼터 소진(리셋 ~6일)이라 2fps 전장 콘택트시트와 의심 프레임을
직접 눈으로 검수했다.

### 겹침 — 원인 네 가지를 각각 다르게 고쳤다

| 증상 | 원인 | 수정 |
|---|---|---|
| 홍단 엠블럼이 세 프레임 연속, 「스톱!」·「주막 등장!」이 두 겹 | **같은 씬의 이웃 컷을 크로스페이드**했다. 화면이 거의 같아서 페이드하는 0.1~0.16초 동안 앞 컷의 금박이 뒤 컷 위에 겹친다 | `transOf()` — **같은 `src`의 이웃 컷은 하드컷**(2프레임). 페이드는 씬 경계에만 |
| 엠블럼이 다음 컷 첫 프레임에 또 보임 | 엠블럼 수명(1050ms)이 컷 간격(760ms)보다 길어 살아 있는 채로 컷이 끝났다 | 수명 700ms — **다음 마크 전에 죽게** |
| 점수 팝업이 오광 낸 패 위에 얹힘 | 팝업 y를 앵커 시점(첫 조합, 3장)에 계산했다. 오광은 낸 패가 크고 수식 패널까지 붙어 그 y가 카드 한복판이 된다 | `scorePop()`이 **뜨는 순간** 낸 패 바닥과 손패 천장 사이 빈 띠를 실측. 좁으면 `--sp-scale`로 축소 |
| 흡수 배수(×N)가 카드 아래단을 뭄 | `transform-origin: center`라 위아래로 같이 자란다 | `top center` — 아래로만 자란다 |
| 「160 +264 160」으로 읽힘 | `flyGain`이 카운트업 중인 숫자 **위에** 착지 | 착지점을 팝업 월 표기 줄로 |
| 고 숫자가 두 겹으로 뭉갬 | 인게임 체이스는 이전 숫자가 0.32초에 걸쳐 밀려나는 동안 다음이 들이친다. 원작 화면은 넓어 괜찮지만 4:5에선 포개진다 | 캡처 CSS로 `push-out` 0.32 → 0.15s |

### 콜드오픈 제거
"결말을 먼저"는 내 제안이었지만 실패했다 — 맥락 없이 잘라 붙인 0.9초가
"폭발"이 아니라 정지된 점수판으로 읽혔다. **삭제하고 H1으로 바로 시작.**

### 힉스필드 — GPT 키프레임 → 양끝 고정 (파이프라인 변경)

```
① gpt-image(codex, 무료)로 키프레임: 슬롯당 start / mid / end
   → 그림체·구도를 여기서 거른다 (생성이 공짜라 몇 번이든)
② kling3_0 (turbo 아님 — end_image를 받는다)
   --start-image kf-start --end-image kf-end --sound off
   → 양끝이 못 박혀 중간 드리프트(실사화·얼굴 변형)가 크게 준다
```

- **실사로 나왔던 원인**: 스틸 프롬프트의 `cinematic noir` · `35mm film grain`이
  포토리얼로 끌고 갔다 → `2D anime illustration, EXACTLY the same painted anime art
  style as the attached character sheet ... NOT photorealistic, NOT 3D, NOT live action`
  으로 교체하고 조명은 스타일 중립어(`warm candlelight, deep shadows`)로
- 모션 프롬프트에도 `keep the 2D anime illustration style, no photorealism`

### 원작(타짜 정마담 인터뷰) 재현 — 그림체·템포만이 아니었다
빠져 있던 것: **인터뷰 발화**(입이 움직인다), 몸을 젖히고 팔을 얹은 여유 자세,
끝에 **시선이 카메라로 돌아오며 냉소적 미소**, 얼굴 반쪽이 잠기는 강한 명암,
배경 강한 보케. 전부 키프레임·모션 프롬프트에 넣었다.
템포도 3.2s → **5.0s 풀클립**(원작은 대사 한 호흡을 통째로 준다), H3 1.6 → 2.2s.

### 자막
H1·H2·H3 대사 자막 제거(사용자 지시). C4의 「몇월까지 깰수있으세요?」만 유지 —
이건 자막이 아니라 CTA다.

현재 길이 **55.8초**.


### 15차 후속 — 수식 위치 두 건

| 증상 | 원인 | 수정 |
|---|---|---|
| 수식 뒤 검은 박스가 카드 아래단을 물었다 | 내가 넣은 금테 판(`.jt-row` 배경) | **판 제거.** 글자에 검은 외곽선 + 발광만 줘서 판 위에서 그냥 읽히게. 카드와 간격 34px |
| 수식이 카드 **오른쪽 옆**으로 나왔다 | ← flexbox 함정. 결과 문구가 옆으로 붙는 걸 고치려 `#playedarea`에 `flex-wrap: wrap`을 열었는데, 집계 중에는 방향이 **세로**다. **세로 방향에서 wrap을 열면 아래로 넘치는 게 아니라 옆에 새 '열'이 생긴다** | 세로일 때만 `nowrap`: `#playedarea.juice-stage { flex-wrap: nowrap }`. 가로일 때(카드 + 결과 문구)는 wrap이라 `width:100%`인 문구가 다음 줄로 내려간다 |
| 점수 팝업이 결과 문구를 덮어 꼬리만 삐져나옴 | 같은 자리에 둘 다 떠 있었다 | 팝업이 뜰 때 `#playresult`를 걷어낸다 — 점수는 팝업이 말한다 |
| 오광 팝업이 가로로 프레임을 넘음 | 축소 판정을 `getBoundingClientRect()`로 했는데 **등장 애니메이션(scale .78)이 걸린 채라 실제보다 작게** 측정됐다 | `offsetWidth/offsetHeight`로 잰다 — transform의 영향을 받지 않는다 |

**하드 학습**: `flex-direction: column`에 `flex-wrap: wrap`을 같이 주면
넘치는 아이템이 **옆 열로 간다**. 세로 배치에서 wrap은 거의 항상 버그다.

---

## 16차 — 타짜 패러디로 서사 재편 (실패 → 조력 → 각성 → 베팅 → 폭발)

### 왜 바꿨나
15차까지는 "잘하는 플레이 모음"이라 서사가 없었다. 타짜 패러디를 얹으면서
**유저 = 방자**, **춘향 = 조연이자 인게임 조력자**로 배역을 고정하고
플로우를 사용자가 지정한 순서로 재편했다.

| # | 구간 | 소스 |
|---|---|---|
| 1 | H1 인터뷰 「방자를 아냐고요?」 | 생성(유지) |
| 2 | A1 **무조합 목표 미달 — 진다** | 녹화 |
| 3 | 「주막 등장!」 | 녹화 |
| 4 | H2 「손은 눈보다 빠르다」 — 춘향이 특수패 두 장을 **돌린다** | 생성 |
| 5 | 주막 **뒷거래** → 특수패 구매 | 녹화 |
| 6 | 몽타주 고도리 → 총통 (병풍 제외) | 녹화 |
| 7 | H3 「그만 빼시겠어요?」 → 「묻고 더블로 가!」 | 생성 |
| 8 | 오광 → 20고 폭발 | 녹화 |
| 9 | C4 인터뷰 회귀 「몇월까지 깰수있으세요?」 | 생성 |
| 10 | 로고 Z축 낙하 | 기존 |

### R13 — 방자의 얼굴은 한 프레임도 나오면 안 된다
플레이어의 환상을 깬다. 방자가 등장하는 컷은 **손과 소매만**.
프롬프트에 `His face is never visible. No head, no chin, no neck, no shoulders`를
명시하고, 키프레임 단계에서 육안으로 전수 검수한다.
부수 효과로 원작의 **오버 더 숄더 걸치기**(단독 샷 구석에 상대의 어깨 실루엣)가
그대로 쓸모 있어진다 — 얼굴 없이 방자의 존재감을 만드는 유일한 장치다.

### 「패를 돌린다」 = 딜(deal)이다
손가락으로 카드를 굴리는 게 아니라 **덱에서 플레이어에게 나눠 주는** 동작이다.
H2·H3의 프롬프트를 전부 `deals / flicks it away across the table` 계열로 바꿨다.
그리고 **춘향이 돌린 두 장이 곧 상점의 그 두 장**이어야 서사가 붙으므로,
주막 진열 연출을 같은 동작으로 각운을 맞췄다(아래).

### 실패는 「빼는 것」으로 만든다
성공 컷(`scoreShow`)에서 관객을 흥분시키는 장치를 하나씩 제거한 것이 `failShow`다.
cardPops 없음 · 엠블럼 없음 · 흡수 없음 · 조커 없음 · gaugeBlast 없음 · numberBurst 없음.
남는 건 힘없는 슬램, `20 × 1` 수식, 바닥에 붙은 게이지뿐.
- `callout(str, d, { cold: true })` — 링·스파크·플래시 전부 빼고 탁한 적색 글자만
- `scorePop({ cold: true })` — 금테·금빛 게이지를 적갈색으로
- 컷에 `cold: true` → **`GRADE_COLD`** (채도 0.52, 감마 0.90, 푸른 쪽, 비네트 강화).
  `GRADE_HOT`의 정확한 반대다. 같은 게임 화면인데 색이 죽어 있어야 자막 없이 읽힌다
- **게임의 `gameOver()`는 부르지 않는다** — `screen`이 'gameover'로 바뀌면 판이 통째로
  지워져 방금 낸 패가 사라진다. `setPlays(3)`으로 남겨두고 숫자와 색으로만 말한다

무조합 3장은 **1월 피 · 5월 열끗 · 9월 청단**(엔진 실측: `none`, ×1, 20칩 → **20/160**).
손패에도 홍단·고도리 재료를 섞지 않는다 — 섞으면 "왜 저걸 안 내지?"가 되어
실패가 각본이 아니라 방자의 무능으로 읽힌다.

### 주막 = 「춘향의 뒷거래」 (`vfx.dealIn`)
살 두 장은 그냥 진열돼 있지 않고 **화면 오른쪽 밖에서 돌려져 들어온다.**
- `body.cap-deal` — 상점 전체 `brightness(.40)`, 딜되는 칸만 `cap-lit`으로 복구.
  원작 손 클로즈업의 「배경 완전 암전 + 대상에만 탑라이트」를 UI로 옮긴 것
- 뒷면(게임 facedown과 같은 붉은 사선 무늬)이 미끄러져 들어와 꽂히고 →
  **소매 그림자**가 훑고 → `scaleX` 접기로 촥 뒤집힌다
- `flyToJoker(..., { seal: true })` — 보유 칸에 박힐 때 금빛이 아니라 **붉은 인장**
- 세 번째 상품(`pi_merchant`)에는 걸지 않는다. 대비가 있어야 두 장이 특별해진다

### 병풍 제외 (엔진 실측으로 확인)
고도리 180 + 총통 140 + 오광 9,500 = **9,820**. 병풍은 138점뿐이라
`setScore(19000)`을 안 건드려도 20고 밴드(28,600 ≤ x < 29,920)에 남는다 → 최종 **28,820**.
허용 범위는 18,780 ~ 20,099. 조합을 바꾸면 `capture/`에서 엔진을 직접 불러 재계산할 것 —
눈대중으로 맞추면 마지막 「20고!」가 19고나 21고로 어긋난다.

### R14 — 원작 컷 길이를 지킨다 (패러디는 템포가 전부)
사용자가 돌려준 Gemini 컷 분해 실측:

| 원작 | 컷 구조 |
|---|---|
| 손은 눈보다 빠르다 | 미디엄샷 **4.0s(손이 프레임 밖에 감춰짐)** → **하드컷** → 손 익스트림 클로즈업 하이앵글 3.0s(프레임의 70~80%, 덱 **밑에서** 빼 화면 밖으로 던짐). 탑라이트 + 배경 암전. **카메라 응시 없음** |
| 묻고 더블로 가 | 질문 **5.0s**(느린 달리 인, 몸을 앞으로 기울여 압박) → 답 **1.5s**(고정, 손은 미동 없음) → 정적 5.0s → 베팅 인서트 2.0s(판돈이 **우측에서 중앙으로**). 대사 순간 **컷이 끊긴다**, 베팅은 대사와 **다른 컷** |

→ H2 1.5s → **3.6s(2비트)**, H3 1.6s → **4.8s(3비트)**. 슬롯을 쪼갠 이유는 아래.
카메라 응시는 **인터뷰 액자(H1·C4)에만** 둔다 — 이 두 씬은 제4의 벽을 안 깬다.

### R15 — 하드컷이 필요한 비트는 슬롯을 쪼갠다 (+ 생성 비용 절감)
한 클립에 `hard cut to`를 적어도 모델이 무시하고 이어버린다.
→ `H2a`/`H2b`, `H3a`/`H3b`로 나누고 `transDur: 0.033`(하드컷)으로 우리가 붙인다.

동시에 **움직임이 없는 비트는 스틸 + 느린 푸시인**으로 처리한다.
H2①(손 감춘 미디엄샷)과 H3①(춘향 질문)이 그렇다 — 영상 생성이 3클립으로 줄었다.
H3②(손 정지)와 ③(4장 딜)은 **한 클립에 이어져 있다**: 원작도 그 정적이 곧 답이라
자르지 않고 정지 → 딜을 통째로 쓴다.

### 파이프라인 (그대로 유지, 스크립트화)
`capture/make-keyframes.sh` — 슬롯당 3장(첫/키/마지막)을 gpt-image로 뽑는다.
크레딧을 안 쓰므로 **여기서 그림체를 거르고** 통과한 것만 힉스필드로 넘긴다.
- 얼굴 나오는 컷: 캐릭터 시트 + 완성 일러스트 (+ 카드가 손에 들리면 실물 화투)
- 얼굴 없는 손 컷: 완성 일러스트(톤) + 실물 화투 ×2 — 시트를 빼야 손 묘사가 산다
- 영상: `kling3_0` **std**(저화질 시안) · `--sound off` · `--start-image`/`--end-image`

현재 길이 **57.9초**.

---

## 17차 — 영상 품질 피드백 반영

16차 결과물에 대한 사용자 지적 7건. 전부 "왜 그렇게 보였는가"가 핵심이다.

### 실패감은 색이 아니라 **어둠**으로 만든다
`GRADE_COLD`를 채도 0.52 + 청색 시프트로 잡았더니 실패가 아니라 **필터 낀 화면**으로
읽혔다. 색을 건드리면 관객은 "연출"이 아니라 "후보정"을 본다.
→ `saturation 0.88 · gamma 0.92 · brightness −0.05`. 색상은 거의 그대로 두고
밝기만 내린다. 게임의 초록 판이 초록으로 남아 있어야 같은 세계로 읽힌다.

### 클라이맥스 뒤에 `fadeblack`을 두지 말 것
20고 직후 C4로 넘어가는 전환이 `fadeblack`이라 1~2프레임 완전 암전이 생겼고,
이게 "영상 오류"로 보였다. 회상→현재 전환은 **컷 안의 조명 변화가 이미 말한다** —
검은 화면까지 끼울 이유가 없다. → 크로스페이드 0.34s.
검증은 눈이 아니라 수치로: `signalstats`의 `YAVG`를 전 프레임 훑어 최저값을 본다.
(현재 3,651프레임 최저 35.7 — 암전 0장)

### R16 — 정적 2D UI 위에서 글로벌 카메라를 움직이지 않는다
상점·결산처럼 평면 UI만 있는 구간에서 카메라가 패닝·줌인하면 안구 운동을 강제해
멀미와 피로를 준다. 무빙은 "무언가 일어나는 곳"에서만 쓴다.
→ 상점 전 컷을 `ZS = Z×1.12` 한 배율로 고정(`look:'center'`, 이동 0).
**강조는 카메라가 아니라 UI 노드가 한다**: `rig.js`의 `.offer.cap-aim`이
`scale(1.32)` + 금빛 글로우 + `z-index`로 스스로 튀어나온다. 이름·설명도 이걸로 읽힌다.
(기존 `api.aim()`이 클래스를 붙였다 떼므로 스크립트는 손대지 않았다)

### 점멸에는 카메라가 같이 반응해야 한다
「4고!!!」「10고!!!」가 아무리 튀어도 프레임이 죽어 있으면 타격감이 없다.
줌 왕복은 금지(R4)이므로 **셰이크로 프레임을 친다** — 고 체이스 컷에 `fx.shake` 5~6.
오광은 평면 확대뿐이라 공간감이 없었다 → 엠블럼 컷에서 `ZB → ZB2`로 **한 번만**
밀고 들어가고 20고까지 그 배율을 유지한다(단방향, R4 준수).

### R17 — 합성되는 에셋끼리 시점(perspective)을 맞춘다
H3b에서 바닥(게임 UI)은 완전한 탑뷰인데 생성된 손·그림자는 대각 앵글이라
손이 허공에 뜬 것처럼 보였다. 시점이 다른 두 에셋은 한 화면에 못 붙인다.
→ 키프레임 프롬프트에 `PERFECTLY TOP-DOWN, the table surface is exactly parallel
to the frame, no perspective` + `light comes straight down, so all shadows are
tight and directly under the objects`를 명시해 재생성.

### 화투 뒷면 에셋 (`capture/cardback.png`)
상점의 뒷거래 딜과 H3의 「묻고 더블로 가」가 **같은 카드 뒷면**을 써야 한 세계로 읽힌다.
시제 화투 뒷면(진홍 단색)을 근거로, 카드 원본 2장을 레퍼런스로 gpt-image 생성:
진홍 바탕 · 톤온톤 격자/구름 · 금테 · 중앙 금매화.
- `vfx.js` `.vfx-dealback`이 CSS 빗금 대신 이 이미지를 쓴다
- H3 키프레임은 이 파일을 **첫 레퍼런스**로 붙여 뒷면 디자인을 못 박는다
- **`build-rig.mjs` 심링크 목록에 반드시 추가** — 빠뜨리면 사본에서 404(엠블럼 때와 동일)

### R18 — 캐릭터 일관성의 앵커는 「시트」가 아니라 「채택된 프레임」이다
C4에서 얼굴이 무너졌다. 캐릭터 시트만 주면 모델이 "비슷한 사람"을 그린다.
→ **이미 채택된 H1 키프레임(`kf/H1-end.png`)을 첫 레퍼런스로** 붙이고,
프롬프트에 "첫 번째 첨부는 같은 영화의 같은 방·같은 인물이다. 눈·헤어라인·머리장식·
한복을 그대로 재현하라"를 명시. 같은 방·같은 조명이 함께 고정돼 드리프트가 크게 준다.

### 오광 = 히트스톱 → Z축 부상 → 병합 → 물리 파티클 (`vfx.ogwangLift`)
광 5장을 모으는 희소성에 엠블럼 확대 하나로 답하면 안 된다.
1. **히트스톱 0.2s** — `body.cap-hitstop`으로 카드 외 UI를 눌러 시간이 멎게 한다.
   **밝기를 너무 내리면 안 된다** — 원래 어두운 상단바에 `.34`를 주면 새까만 띠가 되어
   "렌더링 오류"로 읽힌다. `.66`까지만
2. **Z축 부상** — 카드를 복제해 위로 띄우고, **바닥 그림자를 같이 키운다**.
   그림자가 없으면 그냥 확대로 보인다. 가운데 장을 가장 높이(부채꼴)
3. **중앙 병합** + `slam`
4. **물리 파티클** — keyframe이 아니라 **rAF 물리 루프**(속도 + 중력 + 회전).
   금화 64개 + 매화 꽃잎 96개. 느리게 찍어 빨리 감는 방식이라 rAF 프레임이 전부
   실제로 렌더된다 — 지어낸 프레임 0장(R5). 흰 섬광 없음(R8)
   파편이 프레임을 벗어난 뒤에도 루프가 돌면 정적 구간이 꼬리로 남는다 → `burst` 1.1s

### 로고 플레이트의 카피
OG 아트에는 로고 아래 「몇월까지 깰수있으세요?」가 **구워져 있다.**
C4 자막을 「당신은 몇월까지 깰 수 있으세요?」로 바꾸면 같은 질문이 두 번, 그것도
다른 문장으로 뜬다 → `make-plates.mjs`가 **로고만** 잘라 오게 했다(y 0.24~0.58).
로고는 y 0.28~0.55, 카피는 0.61부터다. **0.50에서 자르면 「로」의 아랫부분이 잘린다.**

현재 길이 **60.9초**.

---

## 18차 — 원작 컷 분해를 정본으로 (+ 방자 캐릭터 확립 · 12월 클리어)

사용자가 타짜 두 장면의 **컷별 실측 분해**를 직접 전달했다. 이제 그것이 정본이다.
추측으로 재현하지 말 것 — 컷 길이·앵글·조명·시선 처리가 전부 그 표에 있다.

### R19 — 「고정 카메라」와 「정지 화면」은 다르다
17차에서 H2①·H3①을 **스틸 + 느린 푸시인**으로 처리했더니
"왜 여기서 화면이 멈추냐"가 됐다. 원작의 그 비트는 **카메라만 고정**돼 있고
인물은 계속 말하고 움직인다(고니 4.0s 독백 / 고니 5.0s 달리인 도발).
→ 두 슬롯 모두 영상으로 재생성. 카메라 무빙은 얹지 않는다(원작이 픽스거나,
클립 자체가 달리인을 이미 갖고 있어 이중 줌이 된다).

**영상 생성비를 아끼려고 정지 비트를 스틸로 대체하지 말 것.** 정지처럼 보이는 비트가
사실은 "말하는 컷"인 경우가 대부분이다.

### R20 — 캐릭터가 둘이면 시트도 둘이어야 한다 (`char/banja_sheet.png`)
17차 H3의 손 컷 그림체가 무너진 원인은 단순했다 — **방자 레퍼런스가 아예 없었다.**
춘향 시트만 붙이면 모델이 매번 다른 손을 그린다.
→ 방자 캐릭터 시트를 **먼저** 만들고 방자가 나오는 모든 생성에 첨부한다. 시트 구성:
- 전신(남색 한복) · 바스트
- **탑뷰 손 스터디 3종**(평평히 편 손 / 쥐는 손 / 화투패 든 손) ← 가장 크게
- 소매 커프스 질감 · **역광 실루엣 두상**
손 컷과 실루엣 컷이 곧 영상에 쓸 프레임이므로 시트에 그 앵글을 미리 넣어 둔다.
색은 춘향(따뜻한 적·백)의 반대인 **차가운 남색**으로 — 원작의 웜/쿨 대립 구조다.

### R13 완화 — 방자는 「실루엣」으로 등장한다
원작 [01:25] 곽철용 클로즈업 일갈은 이 패러디의 키 컷이라 **치환하면 안 된다**.
얼굴을 안 보여주는 게 목적이지 등장 자체를 막는 게 아니다.
→ 역광 실루엣으로 넣는다. 프롬프트 가드:
`he is BACKLIT, a pure dark silhouette. Only the OUTLINE of his jaw, hair and open
mouth are visible. NO facial features at all - no eyes, no nose, no visible skin.`
몸은 미동 없이 **입만** 열린다(원작도 그렇다). 검수는 프레임 단위로.

### H2·H3 최종 구조 (원작 대응)

| 슬롯 | 원작 | 길이 |
|---|---|---|
| H2a | [00:00-00:04] 미디엄샷 고정, 손은 프레임 밖, 대사 | 2.2s |
| H2b | [00:04-00:07] 손 익스트림 클로즈업 하이앵글, 덱 밑에서 빼 던짐 | 2.0s |
| H3a | [01:20-01:25] 타이트 바스트, 아주 느린 달리인, 상체를 앞으로 | 2.2s |
| H3b | [01:25-01:26] 클로즈업, 앉은 채 미동 없이 일갈 (실루엣) | 1.5s |
| H3c | [01:31-01:33] 인서트, 판돈이 우측→중앙 (우리는 패 4장 뒷면) | 2.6s |

H2a▸H2b, H3a▸H3b▸H3c 사이는 전부 하드컷(`transDur 0.033`).

### R21 — 정적 UI 위 세로 팬 금지
카드 내기~결과 구간에서 카메라가 손패(아래) ↔ 낸 패(위) ↔ 팝업(아래)을 오르내려
시선이 계속 끌려다녔다. 손패와 낸 패는 **이미 한 프레임 안에** 있다 —
카메라가 따라다닐 이유가 없다.
→ `look: ['sel','played']`(세로 팬) → `look:'center'` + **줌만**.
→ 점수 팝업 컷의 `look:'pop'` → `'anchor'` (팝업은 앵커 프레임 안에서 읽힌다).
R16(상점 카메라 고정)의 연장이다. 무빙은 사건이 있는 곳에서만.

### R22 — `record.mjs`는 `.build`를 다시 만들지 않는다
`build-rig.mjs`의 심링크 목록에 파일을 추가해도 **`node build-rig.mjs`를 다시 돌리기
전까지 사본에서 404**다. rig.js/vfx.js/scenes.js는 심링크라 수정이 바로 먹으므로
"코드는 반영되는데 새 이미지만 안 나오는" 형태로 나타난다 —
`cardback.png`가 실제로 그렇게 **민무늬 빨간 판**으로 나왔다.
에셋을 추가했으면 `node build-rig.mjs && node record.mjs` 순서로 돌린다.

또 `background: url(...) center/cover`는 세로로 긴 뒷면 원본을 가로로 넓은 상품 박스에
맞추느라 금테·매화를 통째로 잘라낸다 → `center/100% 100%`.

### 상점 강조 크기
`.cap-aim`을 `scale(1.32)` + `transform-origin 50% 46%`로 줬더니 위로 자라며
**먼저 산 특수패(보유 칸)를 덮었다** → `scale(1.18)` + `origin 50% 78%`.
읽히기만 하면 되지 화면을 다 먹을 이유가 없다.

### 12월 클리어 + 승리 화면
「당신은 몇월까지 깰 수 있으세요?」가 꽂히려면 방자가 **끝까지 간 걸** 보여줘야 한다.
→ A3를 9월(2,200) → **12월(5,500, 마지막 판)**로.
- 엔진 실측: 고도리 144 · 총통 168 · **오광 9,770**
- 20고 문턱 5,500×13 = **71,500** / 21고 74,800 → 오광 직전 `setScore` 허용 범위
  61,730 ~ 65,029 → **63,000** → 최종 **72,770**
- 몽타주 구간은 점수를 0에서 쌓는다. 12월은 목표를 넘기는 즉시 게임이 승리 화면을
  띄우려 들기 때문(`checkAfterPlay` → `victory`). 누적 312점이라 안전
- **`window.victory`를 리그가 막아 둔다.** 그대로 두면 점수 연출 한복판에 모달이
  끼어든다 → `api.showVictory()`로 씬이 원하는 박자에 연다
- 광고에 「새 런 시작」 버튼은 필요 없다 → `body.cap-victory`에서 `.modal-actions` 숨기고
  오버레이를 `rgba(0,0,0,.86)`으로 눌러 뒤의 손패·버튼을 지운다

### 로고 플레이트
17차에서 옛 카피를 지우려 로고만 잘랐다가 글자가 잘렸다 → **원복**(y 0.22~0.70).
OG 아트의 「몇월까지 깰수있으세요?」는 로고와 한 덩어리라 분리하면 구도가 깨진다.

현재 길이 **64.7초** · 암전 프레임 0개(3,883프레임 최저 YAVG 21.0).

---

## 19차 — 캡처 리그 좌표계 단일화

카메라 무빙이 컷마다 튀는 게 연출 문제인 줄 알았는데, 리그를 전수 감사해 보니
**좌표계가 세 개로 갈라져 있는 구조 문제**였다.

### 무엇이 어긋나 있었나 (실측)

| `look` 값 | 실제 세로 중심 `cy` |
|---|---|
| `'center'` | **0.500** 고정 — `assemble.mjs`의 상수. **DOM에 그런 rect는 없다** |
| `'anchor'` | **A1 0.386 / A3 0.471** — 낸 패를 얼린 값이라 **씬마다 다르다** |
| `'cards'` | 0.463~0.601 — 판이 진행되는 동안 13.8% 움직인다 |

A1의 컷 순서가 `cards(0.601) → center(0.500) → anchor(0.386)`이라
**한 판 안에서 프레임이 10.1% → 11.4% 두 번 수직으로 튀었다.**
게다가 A1은 `cy 0.386 · zoom 1.293`이라 크롭 상단이 음수가 되어 **ffmpeg 클램프**에
걸렸다 — 요청한 중심이 아예 안 지켜지는데 아무 경고도 없었다.

### R23 — 카메라 기준선은 측정하지 않는다

씬 전체 `cy` 변동폭을 재 보면 **안정적인 측정 rect가 하나도 없다**:
`gameui` 17~23% · `cards`/`action` 14% · `gobar` 14% · `stage` 6~10%.
0%인 건 `anchor`(얼린 값)와 `char`(합성 박스)뿐.
→ **무대 기하에서 결정론적으로 만든다.** `rig.js`:

```js
const FRAME_CY = 0.50;   // 무대(860×1075)가 뷰포트(1080×1350) 정중앙이므로
R.frame = { cx: 0.5, cy: FRAME_CY, w: STAGE_W/innerWidth, h: STAGE_H/innerHeight };
```

기본 배율 ZB(1.293)에서 크롭 창이 **0.113~0.887** — 낸 패(0.35~0.51)·수식·
손패(0.59~0.70)가 다 들어오고 클램프에도 안 걸린다.
**게임 컷 31개 전부 `look:'frame'`.** `center`/`anchor`/`cards`/`hand` 혼용을 없앴다.

`anchor()`는 이제 **카메라용이 아니다** — 카드를 실제로 따라가야 하는 이펙트
(`ogwangLift`의 x 정렬 등)만 쓴다. `POP` rect와 `gobar`는 삭제.

### R24 — 오버레이 기준선도 같은 값에서 나온다

`--cap-focus-y` / `--cap-pop-y` / `--cap-go-y`를 **`clean()`에서 한 번 확정**하고
씬 내내 안 바꾼다(예전엔 `anchor()`가 낸 패 위치에서 계산 → 씬·카드 수마다 달랐다).

| 변수 | 정의 |
|---|---|
| `--cap-focus-y` | `FRAME_CY` — 엠블럼·족보 글자·callout·고 체이스·numberBurst |
| `--cap-pop-y` | `FRAME_CY + 0.115` — 점수 팝업 |
| `--cap-go-y` | `FRAME_CY − 0.20` — 고 스탬프 |

**점수 팝업의 y가 두 벌이었다** — 리그는 `ANCHOR` 카드 bbox 바닥 기준,
vfx는 `#playedarea` **컨테이너** 바닥 기준. 컨테이너 패딩만큼 어긋나서
카메라가 겨냥한 곳과 실제 렌더 위치가 달랐다 → vfx의 자체 측정을 삭제하고
`popY()`(= `--cap-pop-y`)만 쓴다.

같은 함수 안에서 기준이 갈리던 것들도 정리:
- `callout()` 스파크가 뷰포트 중앙 (글자·링은 `focusY()`)
- `gaugeBlast()` 빔은 `#gauge` 폴백인데 스파크는 뷰포트 중앙
- `jokerFire()`가 `at(['.vfx-scorepop','#jt-score'])` — **두 selector의 합집합 중점**이라
  둘 중 어디에도 안 맞았다 → 하나만 겨냥
- `text(str, sel, …)`가 `sel`을 받고 **안 썼다**(`boom()`이 이걸 믿고 넘겼다) → 인자 삭제
- `flyToJoker()` 체공 `innerHeight*0.46` → `focusY()`
- `glow()` 그라디언트 `at 50% 46%` → 프레임 기준선

### R25 — 줌은 두 번만, 되돌아오지 않는다
`ZA(1.256) → ZB(1.293) → ZB2(1.358)`. 영상 전체에서 배율 변화는 딱 두 번.
예전엔 상점만 `ZS(1.407)`로 튀었다가 몽타주에서 `ZA`로 떨어져 경계에서 12%가 튀었고,
승리 화면은 `ZB2 → Z`로 **되돌아왔다**(R4 위반). 상점도 `ZB`로 통일.

### R26 — 폴백은 시끄러워야 한다 + 검산기 (`capture/check-camera.mjs`)
`one()`이 이름을 못 찾으면 `focus → cards → played → hand → CENTER`로 **조용히**
떨어졌다. 이제 `console.warn`.

합성 전에 숫자로 잡는 검산기를 붙였다(`assemble.mjs`가 자동 호출):
① 이웃 컷 크롭 중심이 2% 넘게 차이나면 **세로 점프** ② 배율이 내려가면 **줌 역행**
③ 크롭이 화면 밖이면 **클램프**(요청 중심이 무시되는 상태) ④ **rect 부재**.
컷별 `cy / zoom / crop` 표도 같이 찍는다 — 60초짜리를 다시 뽑아 눈으로 찾을 일이 없다.

### 상점 UI 겹침
- **`.cap-aim`을 확대하지 않는다.** scale은 어느 방향으로 자라든 이웃 상품이나
  위쪽 보유 칸을 먹는다(1.32배 위 기준도, 1.18배 아래 기준도 겹쳤다).
  강조는 기하가 아니라 **대비**로: 조준된 것만 금테+밝게, 나머지 상품과 보유 칸은
  `brightness(.52)`. 레이아웃이 1px도 안 움직이므로 겹침이 원천적으로 불가능하다
- 상품 **이름 pulse 제거** — 이름이 커지면서 **자기 패널 안의 설명 위로 자랐다**
- **팝업이 뜰 때 수식(`#juice-tally`)도 걷어낸다.** 수식은 제 역할을 끝냈는데
  프레임 한복판에서 팝업과 숫자 두 벌이 겹쳐 읽혔다(`#playresult`는 이미 그렇게 하고 있었다)

현재 길이 **64.7초** · 카메라 검산 경고 0건 · 암전 0장(3,879프레임 최저 YAVG 21.0).
