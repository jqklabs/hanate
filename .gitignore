# Firebase 로컬 설정 (키 포함)
firebase-config.js

# npm (이 프로젝트는 Firebase CDN 사용 — npm 불필요)
node_modules/
package-lock.json

# macOS
.DS_Store

# 로컬 에이전트 스킬 (npx skills add — 프로젝트 의존성 아님)
.agents/
.claude/
skills-lock.json
