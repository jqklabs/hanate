# Firebase 로컬 설정 (키 포함)
firebase-config.js

node_modules/

# 골든 대조 실패 산출물
test/golden/__actual__/

# Vite
dist/

# 봇 학습 산출물 (코드는 커밋, 가중치·로그는 로컬)
bot/weights.json
bot/weights.best.json
bot/runs/

# macOS
.DS_Store

# 로컬 에이전트 스킬 (npx skills add — 프로젝트 의존성 아님)
.agents/
.claude/
skills-lock.json
