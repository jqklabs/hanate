# 십이화 — 에이전트 공통 지침 (Cursor 등)

이 저장소에서 작업하기 전에 다음 두 문서를 반드시 읽을 것:

1. **CLAUDE.md** — 모듈 구조(`src/engine` 말단·순수성), 구현 원칙 체크리스트, 디자인 방향(사용자 확정 사항). 여기 규칙은 절대 위반 금지.
2. **HANDOFF.md** — 최근 세션까지의 변경 내역, 시뮬레이션 캘리브레이션, 합의된 다음 작업.

핵심 요약: Vite + TypeScript 모듈. `src/engine/`은 DOM 없는 순수 로직이며 다른 층을 import 하지 않는다. 엔진 수정 시 `npm test` 필수, UI·스타일 이동 시 `npm run golden`. 밸런스는 빡빡하게(하향보다 상향 우선), 네이밍은 고스톱/한국 전통 용어.
